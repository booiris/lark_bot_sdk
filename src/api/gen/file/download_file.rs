//! doc url: <https://open.larkoffice.com/document/uAjLw4CM/ukTMukTMukTM/reference/im-v1/file/get>
// Code generated by gen_api. DO NOT EDIT.

use crate::api::DownloadResp;
use crate::core::model::{ApiRequest, CommonResponse, MethodOption, ReqParam, StreamReqParam};
use crate::{
    core::{http_client::HttpClient, store::Store},
    error::Error,
};
use serde::{Deserialize, Serialize};

use crate::api::gen::file::FileService;

impl<'c, IStore: Store, IClient: HttpClient> FileService<'c, IStore, IClient> {
    /// **api 版本: 2024-01-24T02:24:29+00:00**
    ///
    /// ## 下载文件
    ///
    /// 下载文件接口，只能下载应用自己上传的文件。
    ///
    /// 注意事项:
    ///
    /// - 需要开启[机器人能力](https://open.feishu.cn/document/uAjLw4CM/ugTN1YjL4UTN24CO1UjN/trouble-shooting/how-to-enable-bot-ability)
    ///
    /// - 只能下载机器人自己上传的文件
    ///
    /// - 下载用户发送的资源，请使用[获取消息中的资源文件](https://open.feishu.cn/document/uAjLw4CM/ukTMukTMukTM/reference/im-v1/message-resource/get)接口
    ///
    /// - 下载的资源大小不能超过100M
    ///
    /// - 如果需要Content-Disposition header，发起请求的时候需要在header中设置Content-Type为application/json
    ///
    /// doc: <https://open.larkoffice.com/document/uAjLw4CM/ukTMukTMukTM/reference/im-v1/file/get>
    ///
    /// new doc: <https://open.larkoffice.com/document/server-docs/im-v1/file/get>
    ///
    /// api url: <https://open.feishu.cn/document_portal/v1/document/get_detail?fullPath=%2Fserver-docs%2Fim-v1%2Ffile%2Fget>
    pub async fn download_file(
        &self,
        req: DownloadFileReq,
    ) -> Result<(DownloadResp<IClient>, CommonResponse), Error> {
        self.download_file_with_opt(req, Default::default()).await
    }

    /// 参见 [download_file](#method.download_file) 函数
    pub async fn download_file_with_opt(
        &self,
        req: DownloadFileReq,
        method_option: MethodOption,
    ) -> Result<(DownloadResp<IClient>, CommonResponse), Error> {
        #[cfg(feature = "test-util")]
        {
            if let Some(f) = self.mock().get_mock_download_file(&req) {
                tracing::info!("[lark] File#DownloadFile **mocking** api");
                return f(req);
            }
        }

        tracing::info!("[lark] File#DownloadFile call api");

        let req = ApiRequest {
            scope: "File",
            api: "DownloadFile",
            method: http::Method::GET,
            url: String::new()
                + self.cli.open_base_url.as_ref()
                + "/open-apis/im/v1/files/:file_key",
            param_data: req.gen_param(),
            method_option,
            need_tenant_access_token: true,
            ..Default::default()
        };

        let (bin_data, bin_name, common_resp) = self.cli.do_download_req(req).await?;
        let resp = DownloadResp {
            data: bin_data,
            name: bin_name,
        };

        Ok((resp, common_resp))
    }
}

#[derive(Debug, Serialize, Deserialize, Clone, Default, lark_bot_sdk_macros::ApiReqParams)]
pub struct DownloadFileReq {
    /// 文件的key，通过[上传文件](	https://open.feishu.cn/document/uAjLw4CM/ukTMukTMukTM/reference/im-v1/file/create)接口上传图片后获得
    ///
    /// **是否必填**: 是
    ///
    /// **示例值**: "file_456a92d6-c6ea-4de4-ac3f-7afcf44ac78g"
    #[api(kind = "path", name = "file_key")]
    pub file_key: String,
}

#[cfg(feature = "test-util")]
mod test_utils {
    use std::sync::Arc;

    use self::gen::file::FileServiceMocker;
    use crate::core::mocker::*;

    use super::*;
    use crate::api::*;
    use crate::core::model::*;

    pub struct Mocker;

    pub trait MockFunc<IClient: HttpClient>:
        Fn(DownloadFileReq) -> Result<(DownloadResp<IClient>, CommonResponse), Error>
        + Send
        + Sync
        + 'static
    {
    }
    impl<
            IClient: HttpClient,
            T: Fn(DownloadFileReq) -> Result<(DownloadResp<IClient>, CommonResponse), Error>
                + Send
                + Sync
                + 'static,
        > MockFunc<IClient> for T
    {
    }

    impl<'c, IStore: Store, IClient: HttpClient> FileServiceMocker<'c, IStore, IClient> {
        pub fn mock_download_file<F: MockFunc<IClient>>(
            &self,
            f: F,
        ) -> MockerBuilder<Mocker, DownloadFileReq, DownloadResp<IClient>, Arc<dyn MockFunc<IClient>>>
        {
            MockerBuilder::new(self.cli.instance_id, Arc::new(f))
        }

        pub(super) fn get_mock_download_file(
            &self,
            req: &DownloadFileReq,
        ) -> Option<Arc<dyn MockFunc<IClient>>> {
            do_mock::<Mocker, DownloadFileReq, DownloadResp<IClient>, Arc<dyn MockFunc<IClient>>>(
                self.cli.instance_id,
                req,
            )
        }
    }
}
