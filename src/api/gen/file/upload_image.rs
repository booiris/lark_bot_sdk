//! doc url: <https://open.feishu.cn/document/uAjLw4CM/ukTMukTMukTM/reference/im-v1/image/create>
// Code generated by gen_api. DO NOT EDIT.

use serde::{Deserialize, Serialize};

use crate::api::BaseResp;
use crate::api::HasBaseResp;
use crate::core::model::{
    ApiRequest, CommonResponse, MethodOption, ReqParam, StreamReqData, StreamReqParam,
};
use crate::{
    core::{http_client::HttpClient, store::Store},
    error::Error,
};

use crate::api::gen::file::FileService;

impl<'c, IStore: Store, IClient: HttpClient> FileService<'c, IStore, IClient> {
    /// **api 版本: 2024-01-24T02:24:28+00:00**
    ///
    /// ## 上传图片
    ///
    /// 上传图片接口，支持上传 JPEG、PNG、WEBP、GIF、TIFF、BMP、ICO格式图片。
    ///
    /// 注意事项:
    ///
    /// - 需要开启[机器人能力](https://open.feishu.cn/document/uAjLw4CM/ugTN1YjL4UTN24CO1UjN/trouble-shooting/how-to-enable-bot-ability)
    ///
    /// - 图片大小不得超过10M，且不支持上传大小为0的图片
    ///
    /// doc: <https://open.feishu.cn/document/uAjLw4CM/ukTMukTMukTM/reference/im-v1/image/create>
    ///
    /// new doc: <https://open.feishu.cn/document/server-docs/im-v1/image/create>
    ///
    /// api url: <https://open.feishu.cn/document_portal/v1/document/get_detail?fullPath=%2Fserver-docs%2Fim-v1%2Fimage%2Fcreate>
    pub async fn upload_image<Data: StreamReqData>(
        &self,
        req: UploadImageReq<Data>,
    ) -> Result<(UploadImageResp, CommonResponse), Error> {
        self.upload_image_with_opt(req, Default::default()).await
    }

    /// 参见 [upload_image](#method.upload_image) 函数
    pub async fn upload_image_with_opt<Data: StreamReqData>(
        &self,
        req: UploadImageReq<Data>,
        method_option: MethodOption,
    ) -> Result<(UploadImageResp, CommonResponse), Error> {
        #[cfg(feature = "test-util")]
        {
            if let Some(f) = self.mock().get_mock_upload_image(&req) {
                tracing::info!("[lark] File#UploadImage **mocking** api");
                return f(req);
            }
        }

        tracing::info!("[lark] File#UploadImage call api");

        let req = ApiRequest::<()> {
            scope: "File",
            api: "UploadImage",
            method: http::Method::POST,
            url: String::new() + self.cli.open_base_url.as_ref() + "/open-apis/im/v1/images",
            stream_param_data: req.gen_stream_param(),
            method_option,
            need_tenant_access_token: true,
            ..Default::default()
        };

        let (resp, common_resp): (UploadImageRespInner, _) = self.cli.do_req(req).await?;
        let data = match resp.data {
            Some(data) => data,
            None => {
                return Err(Error::ErrResponse(
                    anyhow::anyhow!("missing response data"),
                    common_resp,
                ));
            }
        };
        Ok((data, common_resp))
    }
}

#[derive(Debug, lark_bot_sdk_macros::ApiReqParams)]
pub struct UploadImageReq<Data: StreamReqData> {
    /// 图片类型
    ///
    /// **是否必填**: 是
    ///
    /// **示例值**: "message"
    ///
    /// **可选值**:
    ///
    /// `message`: 用于发送消息
    ///
    /// `avatar`: 用于设置头像
    #[api(kind = "stream", name = "image_type", option = "false")]
    pub image_type: String,
    /// 图片内容
    ///
    /// **注意：** 上传的图片大小不能超过10MB
    ///
    /// **是否必填**: 是
    ///
    /// **示例值**: "二进制文件"
    #[api(kind = "stream", name = "image", option = "false")]
    pub image: String,
    #[api(kind = "stream", name = "#data#", option = "false")]
    pub data: Data,
}

#[derive(Debug, Serialize, Deserialize, Clone, lark_bot_sdk_macros::ApiBaseResp)]
struct UploadImageRespInner {
    #[serde(flatten)]
    data: Option<UploadImageResp>,
    #[serde(flatten)]
    base: BaseResp,
}

#[derive(Debug, Serialize, Deserialize, Clone, Default)]
pub struct UploadImageResp {
    /// \-
    #[serde(
        rename = "data",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub data: DataSubResp,
}

#[derive(Debug, Serialize, Deserialize, Clone, Default)]
#[serde(default)]
pub struct DataSubResp {
    /// 图片的key
    ///
    /// **示例值**: "img_8d5181ca-0aed-40f0-b0d1-b1452132afbg"
    #[serde(
        rename = "image_key",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub image_key: String,
}

#[cfg(feature = "test-util")]
mod test_utils {
    use std::sync::Arc;

    use self::gen::file::FileServiceMocker;
    use crate::core::mocker::*;

    use super::*;
    use crate::api::*;
    use crate::core::model::*;

    pub struct Mocker;

    pub trait MockFunc<D: StreamReqData>:
        Fn(UploadImageReq<D>) -> Result<(UploadImageResp, CommonResponse), Error>
        + Send
        + Sync
        + 'static
    {
    }
    impl<
            D: StreamReqData,
            T: Fn(UploadImageReq<D>) -> Result<(UploadImageResp, CommonResponse), Error>
                + Send
                + Sync
                + 'static,
        > MockFunc<D> for T
    {
    }

    impl<'c, IStore: Store, IClient: HttpClient> FileServiceMocker<'c, IStore, IClient> {
        pub fn mock_upload_image<T: StreamReqData, F: MockFunc<T>>(
            &self,
            f: F,
        ) -> MockerBuilder<Mocker, UploadImageReq<T>, UploadImageResp, Arc<dyn MockFunc<T>>>
        {
            MockerBuilder::new(self.cli.instance_id, Arc::new(f))
        }

        pub(super) fn get_mock_upload_image<T: StreamReqData>(
            &self,
            req: &UploadImageReq<T>,
        ) -> Option<Arc<dyn MockFunc<T>>> {
            do_mock::<Mocker, UploadImageReq<T>, UploadImageResp, Arc<dyn MockFunc<T>>>(
                self.cli.instance_id,
                req,
            )
        }
    }
}
