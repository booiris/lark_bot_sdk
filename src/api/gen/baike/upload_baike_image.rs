//! doc url: <https://open.feishu.cn/document/uAjLw4CM/ukTMukTMukTM/reference/baike-v1/file/upload>
// Code generated by gen_api. DO NOT EDIT.

use serde::{Deserialize, Serialize};

use crate::api::BaseResp;
use crate::api::HasBaseResp;
use crate::core::model::{
    ApiRequest, CommonResponse, MethodOption, ReqParam, StreamReqData, StreamReqParam,
};
use crate::{
    core::{http_client::HttpClient, store::Store},
    error::Error,
};

use crate::api::gen::baike::BaikeService;

impl<'c, IStore: Store, IClient: HttpClient> BaikeService<'c, IStore, IClient> {
    /// **api 版本: 2023-10-13T02:23:10+00:00**
    ///
    /// ## 上传图片
    ///
    /// 词条图片资源上传。
    ///
    /// 为了更好地提升接口文档的的易理解性，我们对文档进行了升级，请尽快迁移至[新版本>>](https://open.feishu.cn/document/uAjLw4CM/ukTMukTMukTM/lingo-v1/file/upload)
    ///
    /// doc: <https://open.feishu.cn/document/uAjLw4CM/ukTMukTMukTM/reference/baike-v1/file/upload>
    ///
    /// new doc: <https://open.feishu.cn/document/server-docs/baike-v1/file/upload>
    ///
    /// api url: <https://open.feishu.cn/document_portal/v1/document/get_detail?fullPath=%2Fserver-docs%2Fbaike-v1%2Ffile%2Fupload>
    pub async fn upload_baike_image<Data: StreamReqData>(
        &self,
        req: UploadBaikeImageReq<Data>,
    ) -> Result<(UploadBaikeImageResp, CommonResponse), Error> {
        self.upload_baike_image_with_opt(req, Default::default())
            .await
    }

    /// 参见 [upload_baike_image](#method.upload_baike_image) 函数
    pub async fn upload_baike_image_with_opt<Data: StreamReqData>(
        &self,
        req: UploadBaikeImageReq<Data>,
        method_option: MethodOption,
    ) -> Result<(UploadBaikeImageResp, CommonResponse), Error> {
        #[cfg(feature = "test-util")]
        {
            if let Some(f) = self.mock().get_mock_upload_baike_image(&req) {
                tracing::info!("[lark] Baike#UploadBaikeImage **mocking** api");
                return f(req);
            }
        }

        tracing::info!("[lark] Baike#UploadBaikeImage call api");

        let req = ApiRequest::<()> {
            scope: "Baike",
            api: "UploadBaikeImage",
            method: http::Method::POST,
            url: String::new()
                + self.cli.open_base_url.as_ref()
                + "/open-apis/baike/v1/files/upload",
            stream_param_data: req.gen_stream_param(),
            method_option,
            need_tenant_access_token: true,
            need_user_access_token: true,
            ..Default::default()
        };

        let (resp, common_resp): (UploadBaikeImageRespInner, _) = self.cli.do_req(req).await?;
        let data = match resp.data {
            Some(data) => data,
            None => {
                return Err(Error::ErrResponse(
                    anyhow::anyhow!("missing response data"),
                    common_resp,
                ));
            }
        };
        Ok((data, common_resp))
    }
}

#[derive(Debug, lark_bot_sdk_macros::ApiReqParams)]
pub struct UploadBaikeImageReq<Data: StreamReqData> {
    /// 文件名称，当前仅支持上传图片且图片格式为以下六种：icon、bmp、gif、png、jpeg、webp
    ///
    /// **是否必填**: 是
    ///
    /// **示例值**: "示例图片.png"
    ///
    /// **数据校验规则**：
    ///
    /// - **长度范围**: `1` 字符- `100` 字符
    #[api(kind = "stream", name = "name", option = "false")]
    pub name: String,
    #[api(kind = "stream", name = "#data#", option = "false")]
    pub data: Data,
}

#[derive(Debug, Serialize, Deserialize, Clone, lark_bot_sdk_macros::ApiBaseResp)]
struct UploadBaikeImageRespInner {
    #[serde(flatten)]
    data: Option<UploadBaikeImageResp>,
    #[serde(flatten)]
    base: BaseResp,
}

#[derive(Debug, Serialize, Deserialize, Clone, Default)]
pub struct UploadBaikeImageResp {
    /// \-
    #[serde(
        rename = "data",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub data: DataSubResp,
}

#[derive(Debug, Serialize, Deserialize, Clone, Default)]
#[serde(default)]
pub struct DataSubResp {
    /// 文件 token
    ///
    /// **示例值**: "boxbcEcmKiD****vgqWTpvdc7jc"
    #[serde(
        rename = "file_token",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub file_token: String,
}

#[cfg(feature = "test-util")]
mod test_utils {
    use std::sync::Arc;

    use self::gen::baike::BaikeServiceMocker;
    use crate::core::mocker::*;

    use super::*;
    use crate::api::*;
    use crate::core::model::*;

    pub struct Mocker;

    pub trait MockFunc<D: StreamReqData>:
        Fn(UploadBaikeImageReq<D>) -> Result<(UploadBaikeImageResp, CommonResponse), Error>
        + Send
        + Sync
        + 'static
    {
    }
    impl<
            D: StreamReqData,
            T: Fn(UploadBaikeImageReq<D>) -> Result<(UploadBaikeImageResp, CommonResponse), Error>
                + Send
                + Sync
                + 'static,
        > MockFunc<D> for T
    {
    }

    impl<'c, IStore: Store, IClient: HttpClient> BaikeServiceMocker<'c, IStore, IClient> {
        pub fn mock_upload_baike_image<T: StreamReqData, F: MockFunc<T>>(
            &self,
            f: F,
        ) -> MockerBuilder<Mocker, UploadBaikeImageReq<T>, UploadBaikeImageResp, Arc<dyn MockFunc<T>>>
        {
            MockerBuilder::new(self.cli.instance_id, Arc::new(f))
        }

        pub(super) fn get_mock_upload_baike_image<T: StreamReqData>(
            &self,
            req: &UploadBaikeImageReq<T>,
        ) -> Option<Arc<dyn MockFunc<T>>> {
            do_mock::<Mocker, UploadBaikeImageReq<T>, UploadBaikeImageResp, Arc<dyn MockFunc<T>>>(
                self.cli.instance_id,
                req,
            )
        }
    }
}
