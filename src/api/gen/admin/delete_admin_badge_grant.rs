//! doc url: <https://open.larkoffice.com/document/uAjLw4CM/ukTMukTMukTM/reference/admin-v1/badge-grant/delete>
// Code generated by gen_api. DO NOT EDIT.

use serde::{Deserialize, Serialize};

use crate::api::BaseResp;
use crate::api::HasBaseResp;
use crate::core::model::{ApiRequest, CommonResponse, MethodOption, ReqParam, StreamReqParam};
use crate::{
    core::{http_client::HttpClient, store::Store},
    error::Error,
};

use crate::api::gen::admin::AdminService;

impl<'c, IStore: Store, IClient: HttpClient> AdminService<'c, IStore, IClient> {
    /// **api 版本: 2023-05-16T09:07:24+00:00**
    ///
    /// ## 删除授予名单
    ///
    /// 通过该接口可以删除特定授予名单的信息。
    ///

    ///
    /// doc: <https://open.larkoffice.com/document/uAjLw4CM/ukTMukTMukTM/reference/admin-v1/badge-grant/delete>
    ///
    /// new doc: <https://open.larkoffice.com/document/server-docs/admin-v1/badge/badge-grant/delete>
    ///
    /// api url: <https://open.feishu.cn/document_portal/v1/document/get_detail?fullPath=%2Fserver-docs%2Fadmin-v1%2Fbadge%2Fbadge-grant%2Fdelete>
    pub async fn delete_admin_badge_grant(
        &self,
        req: DeleteAdminBadgeGrantReq,
    ) -> Result<(DeleteAdminBadgeGrantResp, CommonResponse), Error> {
        self.delete_admin_badge_grant_with_opt(req, Default::default())
            .await
    }

    /// 参见 [delete_admin_badge_grant](#method.delete_admin_badge_grant) 函数
    pub async fn delete_admin_badge_grant_with_opt(
        &self,
        req: DeleteAdminBadgeGrantReq,
        method_option: MethodOption,
    ) -> Result<(DeleteAdminBadgeGrantResp, CommonResponse), Error> {
        #[cfg(feature = "test-util")]
        {
            if let Some(f) = self.mock().get_mock_delete_admin_badge_grant(&req) {
                tracing::info!("[lark] Admin#DeleteAdminBadgeGrant **mocking** api");
                return f(req);
            }
        }

        tracing::info!("[lark] Admin#DeleteAdminBadgeGrant call api");

        let req = ApiRequest {
            scope: "Admin",
            api: "DeleteAdminBadgeGrant",
            method: http::Method::DELETE,
            url: String::new()
                + self.cli.open_base_url.as_ref()
                + "/open-apis/admin/v1/badges/:badge_id/grants/:grant_id",
            param_data: req.gen_param(),
            method_option,
            need_tenant_access_token: true,
            ..Default::default()
        };

        let (resp, common_resp): (DeleteAdminBadgeGrantRespInner, _) = self.cli.do_req(req).await?;
        let data = match resp.data {
            Some(data) => data,
            None => {
                return Err(Error::ErrResponse(
                    anyhow::anyhow!("missing response data"),
                    common_resp,
                ));
            }
        };
        Ok((data, common_resp))
    }
}

#[derive(Debug, Serialize, Deserialize, Clone, Default, lark_bot_sdk_macros::ApiReqParams)]
pub struct DeleteAdminBadgeGrantReq {
    /// 企业勋章的唯一ID
    ///
    /// **是否必填**: 是
    ///
    /// **示例值**: "m_DjMzaK"
    ///
    /// **数据校验规则**：
    ///
    /// - **长度范围**: `1` 字符- `64` 字符
    #[api(kind = "path", name = "badge_id")]
    pub badge_id: String,
    /// 租户内授予名单的唯一标识，该值由系统随机生成。
    ///
    /// **是否必填**: 是
    ///
    /// **示例值**: "g_uS4yux"
    ///
    /// **数据校验规则**：
    ///
    /// - **长度范围**: `1` 字符- `64` 字符
    #[api(kind = "path", name = "grant_id")]
    pub grant_id: String,
}

#[derive(Debug, Serialize, Deserialize, Clone, lark_bot_sdk_macros::ApiBaseResp)]
struct DeleteAdminBadgeGrantRespInner {
    #[serde(flatten)]
    data: Option<DeleteAdminBadgeGrantResp>,
    #[serde(flatten)]
    base: BaseResp,
}

#[derive(Debug, Serialize, Deserialize, Clone, Default)]
pub struct DeleteAdminBadgeGrantResp {
    /// \-
    #[serde(
        rename = "data",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub data: (),
}

#[cfg(feature = "test-util")]
mod test_utils {
    use std::sync::Arc;

    use self::gen::admin::AdminServiceMocker;
    use crate::core::mocker::*;

    use super::*;
    use crate::api::*;
    use crate::core::model::*;

    pub struct Mocker;

    pub trait MockFunc:
        Fn(DeleteAdminBadgeGrantReq) -> Result<(DeleteAdminBadgeGrantResp, CommonResponse), Error>
        + Send
        + Sync
        + 'static
    {
    }
    impl<
            T: Fn(
                    DeleteAdminBadgeGrantReq,
                ) -> Result<(DeleteAdminBadgeGrantResp, CommonResponse), Error>
                + Send
                + Sync
                + 'static,
        > MockFunc for T
    {
    }

    impl<'c, IStore: Store, IClient: HttpClient> AdminServiceMocker<'c, IStore, IClient> {
        pub fn mock_delete_admin_badge_grant<F: MockFunc>(
            &self,
            f: F,
        ) -> MockerBuilder<
            Mocker,
            DeleteAdminBadgeGrantReq,
            DeleteAdminBadgeGrantResp,
            Arc<dyn MockFunc>,
        > {
            MockerBuilder::new(self.cli.instance_id, Arc::new(f))
        }

        pub(super) fn get_mock_delete_admin_badge_grant(
            &self,
            req: &DeleteAdminBadgeGrantReq,
        ) -> Option<Arc<dyn MockFunc>> {
            do_mock::<Mocker, DeleteAdminBadgeGrantReq, DeleteAdminBadgeGrantResp, Arc<dyn MockFunc>>(
                self.cli.instance_id,
                req,
            )
        }
    }
}

#[cfg(feature = "test-util")]
#[cfg(test)]
mod test {
    use crate::{
        api::gen::admin::delete_admin_badge_grant::{
            DeleteAdminBadgeGrantReq, DeleteAdminBadgeGrantResp, DeleteAdminBadgeGrantRespInner,
        },
        core::{model::CommonResponse, Lark},
    };

    #[tokio::test]
    async fn test_mock() {
        let lark = Lark::new("".to_owned(), "".into());
        let mocker = lark
            .admin()
            .mock()
            .mock_delete_admin_badge_grant(|_| {
                Ok((
                    DeleteAdminBadgeGrantResp::default(),
                    CommonResponse::default(),
                ))
            })
            .build();
        let res = lark
            .admin()
            .delete_admin_badge_grant(DeleteAdminBadgeGrantReq::default())
            .await;
        assert!(res.is_ok());
        mocker.clear();
        let res = lark
            .admin()
            .delete_admin_badge_grant(DeleteAdminBadgeGrantReq::default())
            .await;
        assert!(res.is_err());
    }

    const REQ: &str = "{}";

    #[test]
    fn test_req() {
        if REQ == "{}" {
            return;
        }
        if let Err(e) = serde_json::from_str::<()>(REQ) {
            panic!("{}", e);
        }
    }

    const RESP: &str = r#"{
    "code": 0,
    "msg": "success",
    "data": {}
}"#;
    #[test]
    fn test_resp() {
        let res = serde_json::from_str::<DeleteAdminBadgeGrantRespInner>(RESP);
        if let Err(e) = res {
            panic!("{}", e);
        }
        if let Ok(v) = serde_json::from_str::<serde_json::Value>(RESP) {
            if v.get("data").is_some() {
                assert!(res.unwrap().data.is_some());
            }
        }
    }
}
