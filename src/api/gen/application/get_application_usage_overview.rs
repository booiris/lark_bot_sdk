//! doc url: <https://open.larkoffice.com/document/uAjLw4CM/ukTMukTMukTM/application-v6/application-app_usage/overview>
// Code generated by gen_api. DO NOT EDIT.

use serde::{Deserialize, Serialize};

use crate::api::BaseResp;
use crate::api::HasBaseResp;
use crate::core::model::{ApiRequest, CommonResponse, MethodOption, ReqParam, StreamReqParam};
use crate::{
    core::{http_client::HttpClient, store::Store},
    error::Error,
};

use crate::api::gen::application::ApplicationService;

impl<'c, IStore: Store, IClient: HttpClient> ApplicationService<'c, IStore, IClient> {
    /// **api 版本: 2024-07-23T07:32:42+00:00**
    ///
    /// ## 获取应用使用概览
    ///
    /// 查看应用在某一天/某一周/某一个月的使用数据，可以查看租户整体对应用的使用情况，也可以分部门查看。
    ///
    /// 1. 仅支持企业版/旗舰版租户使用
    ///
    /// 2. 一般每天早上10点产出前一天的数据
    ///
    /// 3. 已经支持的指标包括：应用的活跃用户数、累计用户数、新增用户数、访问页面数、打开次数
    ///
    /// 4. 数据从飞书4.10版本开始统计，使用飞书版本4.10及以下版本的用户数据不会被统计到
    ///
    /// 5. 按照部门查看数据时，会展示当前部门以及其子部门的整体使用情况
    ///
    /// 6. 调用频控为100次/分
    ///
    /// doc: <https://open.larkoffice.com/document/uAjLw4CM/ukTMukTMukTM/application-v6/application-app_usage/overview>
    ///
    /// new doc: <https://open.larkoffice.com/document/server-docs/application-v6/app-usage/overview>
    ///
    /// api url: <https://open.feishu.cn/document_portal/v1/document/get_detail?fullPath=%2Fserver-docs%2Fapplication-v6%2Fapp-usage%2Foverview>
    pub async fn get_application_usage_overview(
        &self,
        req: GetApplicationUsageOverviewReq,
    ) -> Result<(GetApplicationUsageOverviewResp, CommonResponse), Error> {
        self.get_application_usage_overview_with_opt(req, Default::default())
            .await
    }

    /// 参见 [get_application_usage_overview](#method.get_application_usage_overview) 函数
    pub async fn get_application_usage_overview_with_opt(
        &self,
        req: GetApplicationUsageOverviewReq,
        method_option: MethodOption,
    ) -> Result<(GetApplicationUsageOverviewResp, CommonResponse), Error> {
        #[cfg(feature = "test-util")]
        {
            if let Some(f) = self.mock().get_mock_get_application_usage_overview(&req) {
                tracing::info!("[lark] Application#GetApplicationUsageOverview **mocking** api");
                return f(req);
            }
        }

        tracing::info!("[lark] Application#GetApplicationUsageOverview call api");

        let req = ApiRequest {
            scope: "Application",
            api: "GetApplicationUsageOverview",
            method: http::Method::POST,
            url: String::new()
                + self.cli.open_base_url.as_ref()
                + "/open-apis/application/v6/applications/:app_id/app_usage/overview",
            param_data: req.gen_param(),
            method_option,
            need_tenant_access_token: true,
            ..Default::default()
        };

        let (resp, common_resp): (GetApplicationUsageOverviewRespInner, _) =
            self.cli.do_req(req).await?;
        let data = match resp.data {
            Some(data) => data,
            None => {
                return Err(Error::ErrResponse(
                    anyhow::anyhow!("missing response data"),
                    common_resp,
                ));
            }
        };
        Ok((data, common_resp))
    }
}

#[derive(Debug, Serialize, Deserialize, Clone, Default, lark_bot_sdk_macros::ApiReqParams)]
pub struct GetApplicationUsageOverviewReq {
    /// 目标应用 ID
    ///
    /// **是否必填**: 是
    ///
    /// **示例值**: "cli_9f115af860f7901b"
    #[api(kind = "path", name = "app_id")]
    pub app_id: String,
    /// 调用中使用的部门ID的类型
    ///
    /// **示例值**: "open_department_id"
    ///
    /// **可选值**:
    ///
    /// `DepartmentId`: 以自定义department_id来标识部门
    ///
    /// `OpenDepartmentId`: 以open_department_id来标识部门
    #[api(
        kind = "query",
        name = "department_id_type",
        v_type = "var",
        option = "false"
    )]
    pub department_id_type: String,
    /// 查询日期，格式为yyyy-mm-dd，若cycle_type为1，date可以为任何自然日；若cycle_type为2，则输入的date必须为周一； 若cycle_type为3，则输入的date必须为每月1号
    ///
    /// **是否必填**: 是
    ///
    /// **示例值**: "2021-07-08"
    #[api(kind = "body", name = "date")]
    pub date: String,
    /// 活跃周期的统计类型
    ///
    /// **是否必填**: 是
    ///
    /// **示例值**: "1"
    ///
    /// **可选值**:
    ///
    /// `Day`: 日活，指自然日，返回当前日期所在日的数据
    ///
    /// `Week`: 周活，指自然周，返回当前日期所在周的数据。若到查询时当周还没结束，则返回周一到当前日期的数值。例如在2021/7/15 查询2021/7/5 这一周的数据，则代表的是2021/7/5 ~ 2021/7/11。但若是在2021/7/8 查询2021/7/5 这一周的数据，则返回的是2021/7/5 ~ 2021/7/7 的数据
    ///
    /// `Month`: 月活，指自然月，返回当前日期所在月的数据。若不满一个月则返回当月1日到截止日期前的数据。例如在2021/8/15 查询 7月的数据，则代表2021/7/1~2021/7/31。 若在2021/8/15 查询8月的数据，则代表2021/8/1~2021/8/14的数据
    #[api(kind = "body", name = "cycle_type")]
    pub cycle_type: i64,
    /// 查询的部门id，获取方法可参考[部门ID概述](https://open.feishu.cn/document/uAjLw4CM/ukTMukTMukTM/reference/contact-v3/department/field-overview)
    ///
    /// -  若部门id为空，则返回当前租户的使用数据；若填写部门id，则返回当前部门的使用数据（包含子部门的用户）；
    ///
    /// -  若路径参数中department_id_type为空或者为open_department_id，则此处应该填写部门的 open_department_id；若路径参数中department_id_type为department_id，则此处应该填写部门的 department_id。
    ///
    /// **示例值**: "od-4e6ac4d14bcd5071a37a39de902c7141"
    #[api(kind = "body", name = "department_id")]
    pub department_id: Option<String>,
    /// 能力类型，按能力类型进行筛选，返回对应能力的活跃数据
    ///
    /// **是否必填**: 是
    ///
    /// **示例值**: "app"
    ///
    /// **可选值**:
    ///
    /// `App`: 返回应用整体的数据，指标值包括：uv：活跃用户数，total_users：累计用户数，new_users：新增用户数，pv：在应用（小程序或网页）中访问的页面数，lifecycle：打开应用（小程序或网页）的次数
    ///
    /// `Mp`: 返回小程序能力的数据，指标值包括：uv（小程序活跃用户数）、pv（用户在小程序中的访问页面数）、lifecycle（小程序的打开次数）
    ///
    /// `H5`: 返回网页能力的数据，指标值包括：uv（网页应用活跃用户数）、pv（用户在网页应用中的访问页面数）、lifecycle（网页应用的打开次数）
    ///
    /// `Bot`: 返回机器人能力的数据，指标值包括：uv（机器人的活跃用户数）
    #[api(kind = "body", name = "ability")]
    pub ability: String,
}

#[derive(Debug, Serialize, Deserialize, Clone, lark_bot_sdk_macros::ApiBaseResp)]
struct GetApplicationUsageOverviewRespInner {
    #[serde(flatten)]
    data: Option<GetApplicationUsageOverviewResp>,
    #[serde(flatten)]
    base: BaseResp,
}

#[derive(Debug, Serialize, Deserialize, Clone, Default)]
pub struct GetApplicationUsageOverviewResp {
    /// \-
    #[serde(
        rename = "data",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub data: DataSubResp,
}

#[derive(Debug, Serialize, Deserialize, Clone, Default)]
#[serde(default)]
pub struct DataSubResp {
    /// 员工使用应用概览数据
    #[serde(
        rename = "items",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub items: Vec<ApplicationAppUsageSubResp>,
}

#[derive(Debug, Serialize, Deserialize, Clone, Default)]
#[serde(default)]
pub struct ApplicationAppUsageSubResp {
    /// 指标名称
    ///
    /// **是否必填**: 是
    ///
    /// **示例值**: "pv"
    #[serde(
        rename = "metric_name",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub metric_name: String,
    /// 指标值
    ///
    /// **是否必填**: 是
    ///
    /// **示例值**: "100"
    #[serde(
        rename = "metric_value",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub metric_value: i64,
}

#[cfg(feature = "test-util")]
mod test_utils {
    use std::sync::Arc;

    use self::gen::application::ApplicationServiceMocker;
    use crate::core::mocker::*;

    use super::*;
    use crate::api::*;
    use crate::core::model::*;

    pub struct Mocker;

    pub trait MockFunc:
        Fn(
            GetApplicationUsageOverviewReq,
        ) -> Result<(GetApplicationUsageOverviewResp, CommonResponse), Error>
        + Send
        + Sync
        + 'static
    {
    }
    impl<
            T: Fn(
                    GetApplicationUsageOverviewReq,
                )
                    -> Result<(GetApplicationUsageOverviewResp, CommonResponse), Error>
                + Send
                + Sync
                + 'static,
        > MockFunc for T
    {
    }

    impl<'c, IStore: Store, IClient: HttpClient> ApplicationServiceMocker<'c, IStore, IClient> {
        pub fn mock_get_application_usage_overview<F: MockFunc>(
            &self,
            f: F,
        ) -> MockerBuilder<
            Mocker,
            GetApplicationUsageOverviewReq,
            GetApplicationUsageOverviewResp,
            Arc<dyn MockFunc>,
        > {
            MockerBuilder::new(self.cli.instance_id, Arc::new(f))
        }

        pub(super) fn get_mock_get_application_usage_overview(
            &self,
            req: &GetApplicationUsageOverviewReq,
        ) -> Option<Arc<dyn MockFunc>> {
            do_mock::<
                Mocker,
                GetApplicationUsageOverviewReq,
                GetApplicationUsageOverviewResp,
                Arc<dyn MockFunc>,
            >(self.cli.instance_id, req)
        }
    }
}

#[cfg(feature = "test-util")]
#[cfg(test)]
mod test {
    use crate::{
        api::gen::application::get_application_usage_overview::{
            GetApplicationUsageOverviewReq, GetApplicationUsageOverviewResp,
            GetApplicationUsageOverviewRespInner,
        },
        core::{model::CommonResponse, Lark},
    };

    #[tokio::test]
    async fn test_mock() {
        let lark = Lark::new("".to_owned(), "".into());
        let mocker = lark
            .application()
            .mock()
            .mock_get_application_usage_overview(|_| {
                Ok((
                    GetApplicationUsageOverviewResp::default(),
                    CommonResponse::default(),
                ))
            })
            .build();
        let res = lark
            .application()
            .get_application_usage_overview(GetApplicationUsageOverviewReq::default())
            .await;
        assert!(res.is_ok());
        mocker.clear();
        let res = lark
            .application()
            .get_application_usage_overview(GetApplicationUsageOverviewReq::default())
            .await;
        assert!(res.is_err());
    }

    const REQ: &str = r#"{
    "date": "2021-07-26",
    "cycle_type": 1,
    "department_id": "od-bb009b4df5431400dc31d3bc8a37e069",
    "ability": "app"
}"#;

    #[test]
    fn test_req() {
        if REQ == "{}" {
            return;
        }
        if let Err(e) = serde_json::from_str::<super::GetApplicationUsageOverviewReqBody>(REQ) {
            panic!("{}", e);
        }
    }

    const RESP: &str = r#"{
    "code": 0,
    "data": {
        "items": [
            {
                "metric_name": "uv",
                "metric_value": 0
            },
            {
                "metric_name": "total_users",
                "metric_value": 2
            },
            {
                "metric_name": "new_users",
                "metric_value": 0
            },
            {
                "metric_name": "pv",
                "metric_value": 0
            },
            {
                "metric_name": "lifecycle",
                "metric_value": 0
            }
        ]
    },
    "msg": "success"
}"#;
    #[test]
    fn test_resp() {
        let res = serde_json::from_str::<GetApplicationUsageOverviewRespInner>(RESP);
        if let Err(e) = res {
            panic!("{}", e);
        }
        if let Ok(v) = serde_json::from_str::<serde_json::Value>(RESP) {
            if v.get("data").is_some() {
                assert!(res.unwrap().data.is_some());
            }
        }
    }
}
