//! doc url: <https://open.larkoffice.com/document/uAjLw4CM/ukTMukTMukTM/application-v6/application/patch>
// Code generated by gen_api. DO NOT EDIT.

use serde::{Deserialize, Serialize};

use crate::api::BaseResp;
use crate::api::HasBaseResp;
use crate::core::model::{ApiRequest, CommonResponse, MethodOption, ReqParam, StreamReqParam};
use crate::{
    core::{http_client::HttpClient, store::Store},
    error::Error,
};

use crate::api::gen::application::ApplicationService;

impl<'c, IStore: Store, IClient: HttpClient> ApplicationService<'c, IStore, IClient> {
    /// **api 版本: 2023-07-21T07:21:41+00:00**
    ///
    /// ## 更新应用分组信息
    ///
    /// 更新应用的分组信息（分组会影响应用在工作台中的分类情况，请谨慎更新）
    ///

    ///
    /// doc: <https://open.larkoffice.com/document/uAjLw4CM/ukTMukTMukTM/application-v6/application/patch>
    ///
    /// new doc: <https://open.larkoffice.com/document/server-docs/application-v6/application/patch>
    ///
    /// api url: <https://open.feishu.cn/document_portal/v1/document/get_detail?fullPath=%2Fserver-docs%2Fapplication-v6%2Fapplication%2Fpatch>
    pub async fn update_application(
        &self,
        req: UpdateApplicationReq,
    ) -> Result<(UpdateApplicationResp, CommonResponse), Error> {
        self.update_application_with_opt(req, Default::default())
            .await
    }

    /// 参见 [update_application](#method.update_application) 函数
    pub async fn update_application_with_opt(
        &self,
        req: UpdateApplicationReq,
        method_option: MethodOption,
    ) -> Result<(UpdateApplicationResp, CommonResponse), Error> {
        #[cfg(feature = "test-util")]
        {
            if let Some(f) = self.mock().get_mock_update_application(&req) {
                tracing::info!("[lark] Application#UpdateApplication **mocking** api");
                return f(req);
            }
        }

        tracing::info!("[lark] Application#UpdateApplication call api");

        let req = ApiRequest {
            scope: "Application",
            api: "UpdateApplication",
            method: http::Method::PATCH,
            url: String::new()
                + self.cli.open_base_url.as_ref()
                + "/open-apis/application/v6/applications/:app_id",
            param_data: req.gen_param(),
            method_option,
            need_tenant_access_token: true,
            ..Default::default()
        };

        let (resp, common_resp): (UpdateApplicationRespInner, _) = self.cli.do_req(req).await?;
        let data = match resp.data {
            Some(data) => data,
            None => {
                return Err(Error::ErrResponse(
                    anyhow::anyhow!("missing response data"),
                    common_resp,
                ));
            }
        };
        Ok((data, common_resp))
    }
}

#[derive(Debug, Serialize, Deserialize, Clone, Default, lark_bot_sdk_macros::ApiReqParams)]
pub struct UpdateApplicationReq {
    /// 应用的 id
    ///
    /// **是否必填**: 是
    ///
    /// **示例值**: "cli_9b445f5258795107"
    #[api(kind = "path", name = "app_id")]
    pub app_id: String,
    /// 指定返回的语言
    ///
    /// **是否必填**: 是
    ///
    /// **示例值**: "zh_cn"
    ///
    /// **可选值**:
    ///
    /// `zh_cn`: 中文
    ///
    /// `en_us`: 英文
    ///
    /// `ja_jp`: 日文
    #[api(kind = "query", name = "lang", v_type = "var", option = "false")]
    pub lang: String,
    /// 应用分类的国际化描述
    ///
    /// **示例值**: "["办公效率"]"
    ///
    /// **数据校验规则**：
    ///
    /// - **长度范围**: `1` 字符- `3` 字符
    #[api(kind = "body", name = "common_categories")]
    pub common_categories: Vec<Option<String>>,
}

#[derive(Debug, Serialize, Deserialize, Clone, lark_bot_sdk_macros::ApiBaseResp)]
struct UpdateApplicationRespInner {
    #[serde(flatten)]
    data: Option<UpdateApplicationResp>,
    #[serde(flatten)]
    base: BaseResp,
}

#[derive(Debug, Serialize, Deserialize, Clone, Default)]
pub struct UpdateApplicationResp {
    /// \-
    #[serde(
        rename = "data",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub data: (),
}

#[cfg(feature = "test-util")]
mod test_utils {
    use std::sync::Arc;

    use self::gen::application::ApplicationServiceMocker;
    use crate::core::mocker::*;

    use super::*;
    use crate::api::*;
    use crate::core::model::*;

    pub struct Mocker;

    pub trait MockFunc:
        Fn(UpdateApplicationReq) -> Result<(UpdateApplicationResp, CommonResponse), Error>
        + Send
        + Sync
        + 'static
    {
    }
    impl<
            T: Fn(UpdateApplicationReq) -> Result<(UpdateApplicationResp, CommonResponse), Error>
                + Send
                + Sync
                + 'static,
        > MockFunc for T
    {
    }

    impl<'c, IStore: Store, IClient: HttpClient> ApplicationServiceMocker<'c, IStore, IClient> {
        pub fn mock_update_application<F: MockFunc>(
            &self,
            f: F,
        ) -> MockerBuilder<Mocker, UpdateApplicationReq, UpdateApplicationResp, Arc<dyn MockFunc>>
        {
            MockerBuilder::new(self.cli.instance_id, Arc::new(f))
        }

        pub(super) fn get_mock_update_application(
            &self,
            req: &UpdateApplicationReq,
        ) -> Option<Arc<dyn MockFunc>> {
            do_mock::<Mocker, UpdateApplicationReq, UpdateApplicationResp, Arc<dyn MockFunc>>(
                self.cli.instance_id,
                req,
            )
        }
    }
}

#[cfg(feature = "test-util")]
#[cfg(test)]
mod test {
    use crate::{
        api::gen::application::update_application::{
            UpdateApplicationReq, UpdateApplicationResp, UpdateApplicationRespInner,
        },
        core::{model::CommonResponse, Lark},
    };

    #[tokio::test]
    async fn test_mock() {
        let lark = Lark::new("".to_owned(), "".into());
        let mocker = lark
            .application()
            .mock()
            .mock_update_application(|_| {
                Ok((UpdateApplicationResp::default(), CommonResponse::default()))
            })
            .build();
        let res = lark
            .application()
            .update_application(UpdateApplicationReq::default())
            .await;
        assert!(res.is_ok());
        mocker.clear();
        let res = lark
            .application()
            .update_application(UpdateApplicationReq::default())
            .await;
        assert!(res.is_err());
    }

    const REQ: &str = r#"{
    "common_categories": [
        "分析工具"
    ]
}"#;

    #[test]
    fn test_req() {
        if REQ == "{}" {
            return;
        }
        if let Err(e) = serde_json::from_str::<super::UpdateApplicationReqBody>(REQ) {
            panic!("{}", e);
        }
    }

    const RESP: &str = r#"{
    "code": 0,
    "msg": "success",
    "data": {}
}"#;
    #[test]
    fn test_resp() {
        let res = serde_json::from_str::<UpdateApplicationRespInner>(RESP);
        if let Err(e) = res {
            panic!("{}", e);
        }
        if let Ok(v) = serde_json::from_str::<serde_json::Value>(RESP) {
            if v.get("data").is_some() {
                assert!(res.unwrap().data.is_some());
            }
        }
    }
}
