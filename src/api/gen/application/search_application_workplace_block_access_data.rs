//! doc url: <https://open.larkoffice.com/document/uAjLw4CM/ukTMukTMukTM/workplace-v1/workplace_block_access_data/search>
// Code generated by gen_api. DO NOT EDIT.

use serde::{Deserialize, Serialize};

use crate::api::BaseResp;
use crate::api::HasBaseResp;
use crate::core::model::{ApiRequest, CommonResponse, MethodOption, ReqParam, StreamReqParam};
use crate::{
    core::{http_client::HttpClient, store::Store},
    error::Error,
};

use crate::api::gen::application::ApplicationService;

impl<'c, IStore: Store, IClient: HttpClient> ApplicationService<'c, IStore, IClient> {
    /// **api 版本: 2023-07-03T11:07:55+00:00**
    ///
    /// ## 获取定制工作台小组件访问数据
    ///
    /// 获取定制工作台小组件访问数据
    ///

    ///
    /// doc: <https://open.larkoffice.com/document/uAjLw4CM/ukTMukTMukTM/workplace-v1/workplace_block_access_data/search>
    ///
    /// new doc: <https://open.larkoffice.com/document/uAjLw4CM/ukTMukTMukTM/workplace-v1/workplace_block_access_data/search>
    ///
    /// api url: <https://open.feishu.cn/document_portal/v1/document/get_detail?fullPath=%2FuAjLw4CM%2FukTMukTMukTM%2Fworkplace-v1%2Fworkplace_block_access_data%2Fsearch>
    pub async fn search_application_workplace_block_access_data(
        &self,
        req: SearchApplicationWorkplaceBlockAccessDataReq,
    ) -> Result<
        (
            SearchApplicationWorkplaceBlockAccessDataResp,
            CommonResponse,
        ),
        Error,
    > {
        self.search_application_workplace_block_access_data_with_opt(req, Default::default())
            .await
    }

    /// 参见 [search_application_workplace_block_access_data](#method.search_application_workplace_block_access_data) 函数
    pub async fn search_application_workplace_block_access_data_with_opt(
        &self,
        req: SearchApplicationWorkplaceBlockAccessDataReq,
        method_option: MethodOption,
    ) -> Result<
        (
            SearchApplicationWorkplaceBlockAccessDataResp,
            CommonResponse,
        ),
        Error,
    > {
        #[cfg(feature = "test-util")]
        {
            if let Some(f) = self
                .mock()
                .get_mock_search_application_workplace_block_access_data(&req)
            {
                tracing::info!(
                    "[lark] Application#SearchApplicationWorkplaceBlockAccessData **mocking** api"
                );
                return f(req);
            }
        }

        tracing::info!("[lark] Application#SearchApplicationWorkplaceBlockAccessData call api");

        let req = ApiRequest {
            scope: "Application",
            api: "SearchApplicationWorkplaceBlockAccessData",
            method: http::Method::POST,
            url: String::new()
                + self.cli.open_base_url.as_ref()
                + "/open-apis/workplace/v1/workplace_block_access_data/search",
            param_data: req.gen_param(),
            method_option,
            need_tenant_access_token: true,
            ..Default::default()
        };

        let (resp, common_resp): (SearchApplicationWorkplaceBlockAccessDataRespInner, _) =
            self.cli.do_req(req).await?;
        let data = match resp.data {
            Some(data) => data,
            None => {
                return Err(Error::ErrResponse(
                    anyhow::anyhow!("missing response data"),
                    common_resp,
                ));
            }
        };
        Ok((data, common_resp))
    }
}

#[derive(Debug, Serialize, Deserialize, Clone, Default, lark_bot_sdk_macros::ApiReqParams)]
pub struct SearchApplicationWorkplaceBlockAccessDataReq {
    /// 数据检索开始时间，精确到日。格式yyyy-MM-dd。
    ///
    /// **是否必填**: 是
    ///
    /// **示例值**: "2023-02-01"
    #[api(kind = "query", name = "from_date", v_type = "var", option = "false")]
    pub from_date: String,
    /// 数据检索结束时间，精确到日。格式yyyy-MM-dd。
    ///
    /// **是否必填**: 是
    ///
    /// **示例值**: "2023-03-02"
    #[api(kind = "query", name = "to_date", v_type = "var", option = "false")]
    pub to_date: String,
    /// 分页大小，最小为 1，最大为 200，默认为 20。
    ///
    /// **是否必填**: 是
    ///
    /// **示例值**: "20"
    #[api(kind = "query", name = "page_size", v_type = "var", option = "false")]
    pub page_size: i64,
    /// 分页标记，第一次请求不填，表示从头开始遍历；分页查询结果还有更多项时会同时返回新的 page_token，下次遍历可采用该 page_token 获取查询结果
    ///
    /// **示例值**: "ddowkdkl9w2d"
    #[api(kind = "query", name = "page_token", v_type = "var", option = "false")]
    pub page_token: String,
    /// 小组件id（BlockID）。
    ///
    /// 可前往 飞书管理后台 > 工作台 > 定制工作台，选择指定的工作台并进入工作台编辑器，点击某个小组件，即可查看页面右侧面板中该小组件名称下方的“BlockID”
    ///
    /// **示例值**: "283438293839422334"
    #[api(kind = "query", name = "block_id", v_type = "var", option = "false")]
    pub block_id: String,
}

#[derive(Debug, Serialize, Deserialize, Clone, lark_bot_sdk_macros::ApiBaseResp)]
struct SearchApplicationWorkplaceBlockAccessDataRespInner {
    #[serde(flatten)]
    data: Option<SearchApplicationWorkplaceBlockAccessDataResp>,
    #[serde(flatten)]
    base: BaseResp,
}

#[derive(Debug, Serialize, Deserialize, Clone, Default)]
pub struct SearchApplicationWorkplaceBlockAccessDataResp {
    /// \-
    #[serde(
        rename = "data",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub data: DataSubResp,
}

#[derive(Debug, Serialize, Deserialize, Clone, Default)]
#[serde(default)]
pub struct DataSubResp {
    /// 工作台中block的访问数据
    #[serde(
        rename = "items",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub items: Vec<BlockAccessDataSubResp>,
    /// 是否还有更多项
    ///
    /// **示例值**: "true"
    #[serde(
        rename = "has_more",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub has_more: bool,
    /// 分页标记，当 has_more 为 true 时，会同时返回新的 page_token，否则不返回 page_token
    ///
    /// **示例值**: "ddowkdkl9w2d"
    #[serde(
        rename = "page_token",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub page_token: String,
}

#[derive(Debug, Serialize, Deserialize, Clone, Default)]
#[serde(default)]
pub struct BlockAccessDataSubResp {
    /// 时间,精确到天,格式yyyy-MM-dd
    ///
    /// **示例值**: "2023-03-12"
    #[serde(
        rename = "date",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub date: String,
    /// 小组件id（BlockID）。可前往 飞书管理后台 > 工作台 > 定制工作台，选择指定的工作台并进入工作台编辑器，点击某个小组件，即可查看页面右侧面板中该小组件名称下方的“BlockID”
    ///
    /// **示例值**: "283438293839422334"
    #[serde(
        rename = "block_id",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub block_id: String,
    /// block访问数据信息。
    #[serde(
        rename = "access_data",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub access_data: AccessDataSubResp,
}

#[derive(Debug, Serialize, Deserialize, Clone, Default)]
#[serde(default)]
pub struct AccessDataSubResp {
    /// 访问次数
    ///
    /// **示例值**: "100"
    #[serde(
        rename = "pv",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub pv: i64,
    /// 访问用户数(去重)
    ///
    /// **示例值**: "30"
    #[serde(
        rename = "uv",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub uv: i64,
}

#[cfg(feature = "test-util")]
mod test_utils {
    use std::sync::Arc;

    use self::gen::application::ApplicationServiceMocker;
    use crate::core::mocker::*;

    use super::*;
    use crate::api::*;
    use crate::core::model::*;

    pub struct Mocker;

    pub trait MockFunc:
        Fn(
            SearchApplicationWorkplaceBlockAccessDataReq,
        ) -> Result<
            (
                SearchApplicationWorkplaceBlockAccessDataResp,
                CommonResponse,
            ),
            Error,
        > + Send
        + Sync
        + 'static
    {
    }
    impl<
            T: Fn(
                    SearchApplicationWorkplaceBlockAccessDataReq,
                ) -> Result<
                    (
                        SearchApplicationWorkplaceBlockAccessDataResp,
                        CommonResponse,
                    ),
                    Error,
                > + Send
                + Sync
                + 'static,
        > MockFunc for T
    {
    }

    impl<'c, IStore: Store, IClient: HttpClient> ApplicationServiceMocker<'c, IStore, IClient> {
        pub fn mock_search_application_workplace_block_access_data<F: MockFunc>(
            &self,
            f: F,
        ) -> MockerBuilder<
            Mocker,
            SearchApplicationWorkplaceBlockAccessDataReq,
            SearchApplicationWorkplaceBlockAccessDataResp,
            Arc<dyn MockFunc>,
        > {
            MockerBuilder::new(self.cli.instance_id, Arc::new(f))
        }

        pub(super) fn get_mock_search_application_workplace_block_access_data(
            &self,
            req: &SearchApplicationWorkplaceBlockAccessDataReq,
        ) -> Option<Arc<dyn MockFunc>> {
            do_mock::<
                Mocker,
                SearchApplicationWorkplaceBlockAccessDataReq,
                SearchApplicationWorkplaceBlockAccessDataResp,
                Arc<dyn MockFunc>,
            >(self.cli.instance_id, req)
        }
    }
}

#[cfg(feature = "test-util")]
#[cfg(test)]
mod test {
    use crate::{
        api::gen::application::search_application_workplace_block_access_data::{
            SearchApplicationWorkplaceBlockAccessDataReq,
            SearchApplicationWorkplaceBlockAccessDataResp,
            SearchApplicationWorkplaceBlockAccessDataRespInner,
        },
        core::{model::CommonResponse, Lark},
    };

    #[tokio::test]
    async fn test_mock() {
        let lark = Lark::new("".to_owned(), "".into());
        let mocker = lark
            .application()
            .mock()
            .mock_search_application_workplace_block_access_data(|_| {
                Ok((
                    SearchApplicationWorkplaceBlockAccessDataResp::default(),
                    CommonResponse::default(),
                ))
            })
            .build();
        let res = lark
            .application()
            .search_application_workplace_block_access_data(
                SearchApplicationWorkplaceBlockAccessDataReq::default(),
            )
            .await;
        assert!(res.is_ok());
        mocker.clear();
        let res = lark
            .application()
            .search_application_workplace_block_access_data(
                SearchApplicationWorkplaceBlockAccessDataReq::default(),
            )
            .await;
        assert!(res.is_err());
    }

    const REQ: &str = "{}";

    #[test]
    fn test_req() {
        if REQ == "{}" {
            return;
        }
        if let Err(e) = serde_json::from_str::<()>(REQ) {
            panic!("{}", e);
        }
    }

    const RESP: &str = r#"{
    "code": 0,
    "msg": "success",
    "data": {
        "items": [
            {
                "date": "2023-03-12",
                "block_id": "283438293839422334",
                "access_data": {
                    "pv": 100,
                    "uv": 30
                }
            }
        ],
        "has_more": true,
        "page_token": "ddowkdkl9w2d"
    }
}"#;
    #[test]
    fn test_resp() {
        let res = serde_json::from_str::<SearchApplicationWorkplaceBlockAccessDataRespInner>(RESP);
        if let Err(e) = res {
            panic!("{}", e);
        }
        if let Ok(v) = serde_json::from_str::<serde_json::Value>(RESP) {
            if v.get("data").is_some() {
                assert!(res.unwrap().data.is_some());
            }
        }
    }
}
