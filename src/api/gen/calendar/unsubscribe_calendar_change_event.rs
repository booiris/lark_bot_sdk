//! doc url: <https://open.larkoffice.com/document/uAjLw4CM/ukTMukTMukTM/reference/calendar-v4/calendar/unsubscription>
// Code generated by gen_api. DO NOT EDIT.

use serde::{Deserialize, Serialize};

use crate::api::BaseResp;
use crate::api::HasBaseResp;
use crate::core::model::{ApiRequest, CommonResponse, MethodOption, ReqParam, StreamReqParam};
use crate::{
    core::{http_client::HttpClient, store::Store},
    error::Error,
};

use crate::api::gen::calendar::CalendarService;

impl<'c, IStore: Store, IClient: HttpClient> CalendarService<'c, IStore, IClient> {
    /// **api 版本: 2024-07-16T06:20:40+00:00**
    ///
    /// ## 取消订阅日历变更事件
    ///
    /// 调用该接口为当前用户身份取消订阅[日历变更事件](https://open.feishu.cn/document/uAjLw4CM/ukTMukTMukTM/reference/calendar-v4/calendar/events/changed)。
    ///

    ///
    /// doc: <https://open.larkoffice.com/document/uAjLw4CM/ukTMukTMukTM/reference/calendar-v4/calendar/unsubscription>
    ///
    /// new doc: <https://open.larkoffice.com/document/server-docs/calendar-v4/calendar/unsubscription>
    ///
    /// api url: <https://open.feishu.cn/document_portal/v1/document/get_detail?fullPath=%2Fserver-docs%2Fcalendar-v4%2Fcalendar%2Funsubscription>
    pub async fn unsubscribe_calendar_change_event(
        &self,
        req: UnsubscribeCalendarChangeEventReq,
    ) -> Result<(UnsubscribeCalendarChangeEventResp, CommonResponse), Error> {
        self.unsubscribe_calendar_change_event_with_opt(req, Default::default())
            .await
    }

    /// 参见 [unsubscribe_calendar_change_event](#method.unsubscribe_calendar_change_event) 函数
    pub async fn unsubscribe_calendar_change_event_with_opt(
        &self,
        req: UnsubscribeCalendarChangeEventReq,
        method_option: MethodOption,
    ) -> Result<(UnsubscribeCalendarChangeEventResp, CommonResponse), Error> {
        #[cfg(feature = "test-util")]
        {
            if let Some(f) = self.mock().get_mock_unsubscribe_calendar_change_event(&req) {
                tracing::info!("[lark] Calendar#UnsubscribeCalendarChangeEvent **mocking** api");
                return f(req);
            }
        }

        tracing::info!("[lark] Calendar#UnsubscribeCalendarChangeEvent call api");

        let req = ApiRequest {
            scope: "Calendar",
            api: "UnsubscribeCalendarChangeEvent",
            method: http::Method::POST,
            url: String::new()
                + self.cli.open_base_url.as_ref()
                + "/open-apis/calendar/v4/calendars/unsubscription",
            param_data: req.gen_param(),
            method_option,
            need_user_access_token: true,
            ..Default::default()
        };

        let (resp, common_resp): (UnsubscribeCalendarChangeEventRespInner, _) =
            self.cli.do_req(req).await?;
        let data = match resp.data {
            Some(data) => data,
            None => {
                return Err(Error::ErrResponse(
                    anyhow::anyhow!("missing response data"),
                    common_resp,
                ));
            }
        };
        Ok((data, common_resp))
    }
}

#[derive(Debug, Serialize, Deserialize, Clone, Default, lark_bot_sdk_macros::ApiReqParams)]
pub struct UnsubscribeCalendarChangeEventReq {}

#[derive(Debug, Serialize, Deserialize, Clone, lark_bot_sdk_macros::ApiBaseResp)]
struct UnsubscribeCalendarChangeEventRespInner {
    #[serde(flatten)]
    data: Option<UnsubscribeCalendarChangeEventResp>,
    #[serde(flatten)]
    base: BaseResp,
}

#[derive(Debug, Serialize, Deserialize, Clone, Default)]
pub struct UnsubscribeCalendarChangeEventResp {
    /// \-
    #[serde(
        rename = "data",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub data: (),
}

#[cfg(feature = "test-util")]
mod test_utils {
    use std::sync::Arc;

    use self::gen::calendar::CalendarServiceMocker;
    use crate::core::mocker::*;

    use super::*;
    use crate::api::*;
    use crate::core::model::*;

    pub struct Mocker;

    pub trait MockFunc:
        Fn(
            UnsubscribeCalendarChangeEventReq,
        ) -> Result<(UnsubscribeCalendarChangeEventResp, CommonResponse), Error>
        + Send
        + Sync
        + 'static
    {
    }
    impl<
            T: Fn(
                    UnsubscribeCalendarChangeEventReq,
                )
                    -> Result<(UnsubscribeCalendarChangeEventResp, CommonResponse), Error>
                + Send
                + Sync
                + 'static,
        > MockFunc for T
    {
    }

    impl<'c, IStore: Store, IClient: HttpClient> CalendarServiceMocker<'c, IStore, IClient> {
        pub fn mock_unsubscribe_calendar_change_event<F: MockFunc>(
            &self,
            f: F,
        ) -> MockerBuilder<
            Mocker,
            UnsubscribeCalendarChangeEventReq,
            UnsubscribeCalendarChangeEventResp,
            Arc<dyn MockFunc>,
        > {
            MockerBuilder::new(self.cli.instance_id, Arc::new(f))
        }

        pub(super) fn get_mock_unsubscribe_calendar_change_event(
            &self,
            req: &UnsubscribeCalendarChangeEventReq,
        ) -> Option<Arc<dyn MockFunc>> {
            do_mock::<
                Mocker,
                UnsubscribeCalendarChangeEventReq,
                UnsubscribeCalendarChangeEventResp,
                Arc<dyn MockFunc>,
            >(self.cli.instance_id, req)
        }
    }
}

#[cfg(feature = "test-util")]
#[cfg(test)]
mod test {
    use crate::{
        api::gen::calendar::unsubscribe_calendar_change_event::{
            UnsubscribeCalendarChangeEventReq, UnsubscribeCalendarChangeEventResp,
            UnsubscribeCalendarChangeEventRespInner,
        },
        core::{model::CommonResponse, Lark},
    };

    #[tokio::test]
    async fn test_mock() {
        let lark = Lark::new("".to_owned(), "".into());
        let mocker = lark
            .calendar()
            .mock()
            .mock_unsubscribe_calendar_change_event(|_| {
                Ok((
                    UnsubscribeCalendarChangeEventResp::default(),
                    CommonResponse::default(),
                ))
            })
            .build();
        let res = lark
            .calendar()
            .unsubscribe_calendar_change_event(UnsubscribeCalendarChangeEventReq::default())
            .await;
        assert!(res.is_ok());
        mocker.clear();
        let res = lark
            .calendar()
            .unsubscribe_calendar_change_event(UnsubscribeCalendarChangeEventReq::default())
            .await;
        assert!(res.is_err());
    }

    const REQ: &str = "{}";

    #[test]
    fn test_req() {
        if REQ == "{}" {
            return;
        }
        if let Err(e) = serde_json::from_str::<()>(REQ) {
            panic!("{}", e);
        }
    }

    const RESP: &str = r#"{
    "code": 0,
    "msg": "success",
    "data": {}
}"#;
    #[test]
    fn test_resp() {
        let res = serde_json::from_str::<UnsubscribeCalendarChangeEventRespInner>(RESP);
        if let Err(e) = res {
            panic!("{}", e);
        }
        if let Ok(v) = serde_json::from_str::<serde_json::Value>(RESP) {
            if v.get("data").is_some() {
                assert!(res.unwrap().data.is_some());
            }
        }
    }
}
