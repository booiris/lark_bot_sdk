//! doc url: <https://open.larkoffice.com/document/uAjLw4CM/ukTMukTMukTM/reference/calendar-v4/calendar/unsubscribe>
// Code generated by gen_api. DO NOT EDIT.

use serde::{Deserialize, Serialize};

use crate::api::BaseResp;
use crate::api::HasBaseResp;
use crate::core::model::{ApiRequest, CommonResponse, MethodOption, ReqParam, StreamReqParam};
use crate::{
    core::{http_client::HttpClient, store::Store},
    error::Error,
};

use crate::api::gen::calendar::CalendarService;

impl<'c, IStore: Store, IClient: HttpClient> CalendarService<'c, IStore, IClient> {
    /// **api 版本: 2024-07-16T06:20:40+00:00**
    ///
    /// ## 取消订阅日历
    ///
    /// 调用该接口以当前身份（应用或用户）取消指定日历的订阅状态。
    ///
    /// - 当前身份由 Header Authorization 的 Token 类型决定。tenant_access_token 指应用身份，user_access_token 指用户身份。
    ///
    /// - 如果使用应用身份调用该接口，则需要确保应用开启了[机器人能力](https://open.feishu.cn/document/uAjLw4CM/ugTN1YjL4UTN24CO1UjN/trouble-shooting/how-to-enable-bot-ability)。
    ///
    /// - 仅可操作已经被当前身份订阅的日历。
    ///
    /// doc: <https://open.larkoffice.com/document/uAjLw4CM/ukTMukTMukTM/reference/calendar-v4/calendar/unsubscribe>
    ///
    /// new doc: <https://open.larkoffice.com/document/server-docs/calendar-v4/calendar/unsubscribe>
    ///
    /// api url: <https://open.feishu.cn/document_portal/v1/document/get_detail?fullPath=%2Fserver-docs%2Fcalendar-v4%2Fcalendar%2Funsubscribe>
    pub async fn unsubscribe_calendar(
        &self,
        req: UnsubscribeCalendarReq,
    ) -> Result<(UnsubscribeCalendarResp, CommonResponse), Error> {
        self.unsubscribe_calendar_with_opt(req, Default::default())
            .await
    }

    /// 参见 [unsubscribe_calendar](#method.unsubscribe_calendar) 函数
    pub async fn unsubscribe_calendar_with_opt(
        &self,
        req: UnsubscribeCalendarReq,
        method_option: MethodOption,
    ) -> Result<(UnsubscribeCalendarResp, CommonResponse), Error> {
        #[cfg(feature = "test-util")]
        {
            if let Some(f) = self.mock().get_mock_unsubscribe_calendar(&req) {
                tracing::info!("[lark] Calendar#UnsubscribeCalendar **mocking** api");
                return f(req);
            }
        }

        tracing::info!("[lark] Calendar#UnsubscribeCalendar call api");

        let req = ApiRequest {
            scope: "Calendar",
            api: "UnsubscribeCalendar",
            method: http::Method::POST,
            url: String::new()
                + self.cli.open_base_url.as_ref()
                + "/open-apis/calendar/v4/calendars/:calendar_id/unsubscribe",
            param_data: req.gen_param(),
            method_option,
            need_tenant_access_token: true,
            need_user_access_token: true,
            ..Default::default()
        };

        let (resp, common_resp): (UnsubscribeCalendarRespInner, _) = self.cli.do_req(req).await?;
        let data = match resp.data {
            Some(data) => data,
            None => {
                return Err(Error::ErrResponse(
                    anyhow::anyhow!("missing response data"),
                    common_resp,
                ));
            }
        };
        Ok((data, common_resp))
    }
}

#[derive(Debug, Serialize, Deserialize, Clone, Default, lark_bot_sdk_macros::ApiReqParams)]
pub struct UnsubscribeCalendarReq {
    /// 日历 ID。
    ///
    /// 你可以调用以下接口获取某一日历的 ID。
    ///
    /// - [查询主日历信息](https://open.feishu.cn/document/uAjLw4CM/ukTMukTMukTM/reference/calendar-v4/calendar/primary)
    ///
    /// - [查询日历列表](https://open.feishu.cn/document/uAjLw4CM/ukTMukTMukTM/reference/calendar-v4/calendar/list)
    ///
    /// - [搜索日历](https://open.feishu.cn/document/uAjLw4CM/ukTMukTMukTM/reference/calendar-v4/calendar/search)
    ///
    /// **是否必填**: 是
    ///
    /// **示例值**: "feishu.cn_xxxxxxxxxx@group.calendar.feishu.cn"
    #[api(kind = "path", name = "calendar_id")]
    pub calendar_id: String,
}

#[derive(Debug, Serialize, Deserialize, Clone, lark_bot_sdk_macros::ApiBaseResp)]
struct UnsubscribeCalendarRespInner {
    #[serde(flatten)]
    data: Option<UnsubscribeCalendarResp>,
    #[serde(flatten)]
    base: BaseResp,
}

#[derive(Debug, Serialize, Deserialize, Clone, Default)]
pub struct UnsubscribeCalendarResp {
    /// \-
    #[serde(
        rename = "data",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub data: (),
}

#[cfg(feature = "test-util")]
mod test_utils {
    use std::sync::Arc;

    use self::gen::calendar::CalendarServiceMocker;
    use crate::core::mocker::*;

    use super::*;
    use crate::api::*;
    use crate::core::model::*;

    pub struct Mocker;

    pub trait MockFunc:
        Fn(UnsubscribeCalendarReq) -> Result<(UnsubscribeCalendarResp, CommonResponse), Error>
        + Send
        + Sync
        + 'static
    {
    }
    impl<
            T: Fn(
                    UnsubscribeCalendarReq,
                ) -> Result<(UnsubscribeCalendarResp, CommonResponse), Error>
                + Send
                + Sync
                + 'static,
        > MockFunc for T
    {
    }

    impl<'c, IStore: Store, IClient: HttpClient> CalendarServiceMocker<'c, IStore, IClient> {
        pub fn mock_unsubscribe_calendar<F: MockFunc>(
            &self,
            f: F,
        ) -> MockerBuilder<Mocker, UnsubscribeCalendarReq, UnsubscribeCalendarResp, Arc<dyn MockFunc>>
        {
            MockerBuilder::new(self.cli.instance_id, Arc::new(f))
        }

        pub(super) fn get_mock_unsubscribe_calendar(
            &self,
            req: &UnsubscribeCalendarReq,
        ) -> Option<Arc<dyn MockFunc>> {
            do_mock::<Mocker, UnsubscribeCalendarReq, UnsubscribeCalendarResp, Arc<dyn MockFunc>>(
                self.cli.instance_id,
                req,
            )
        }
    }
}

#[cfg(feature = "test-util")]
#[cfg(test)]
mod test {
    use crate::{
        api::gen::calendar::unsubscribe_calendar::{
            UnsubscribeCalendarReq, UnsubscribeCalendarResp, UnsubscribeCalendarRespInner,
        },
        core::{model::CommonResponse, Lark},
    };

    #[tokio::test]
    async fn test_mock() {
        let lark = Lark::new("".to_owned(), "".into());
        let mocker = lark
            .calendar()
            .mock()
            .mock_unsubscribe_calendar(|_| {
                Ok((
                    UnsubscribeCalendarResp::default(),
                    CommonResponse::default(),
                ))
            })
            .build();
        let res = lark
            .calendar()
            .unsubscribe_calendar(UnsubscribeCalendarReq::default())
            .await;
        assert!(res.is_ok());
        mocker.clear();
        let res = lark
            .calendar()
            .unsubscribe_calendar(UnsubscribeCalendarReq::default())
            .await;
        assert!(res.is_err());
    }

    const REQ: &str = "{}";

    #[test]
    fn test_req() {
        if REQ == "{}" {
            return;
        }
        if let Err(e) = serde_json::from_str::<()>(REQ) {
            panic!("{}", e);
        }
    }

    const RESP: &str = r#"{
    "code": 0,
    "msg": "success",
    "data": {}
}"#;
    #[test]
    fn test_resp() {
        let res = serde_json::from_str::<UnsubscribeCalendarRespInner>(RESP);
        if let Err(e) = res {
            panic!("{}", e);
        }
        if let Ok(v) = serde_json::from_str::<serde_json::Value>(RESP) {
            if v.get("data").is_some() {
                assert!(res.unwrap().data.is_some());
            }
        }
    }
}
