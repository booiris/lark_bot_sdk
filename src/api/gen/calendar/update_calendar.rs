//! doc url: <https://open.larkoffice.com/document/uAjLw4CM/ukTMukTMukTM/reference/calendar-v4/calendar/patch>
// Code generated by gen_api. DO NOT EDIT.

use serde::{Deserialize, Serialize};

use crate::api::BaseResp;
use crate::api::HasBaseResp;
use crate::core::model::{ApiRequest, CommonResponse, MethodOption, ReqParam, StreamReqParam};
use crate::{
    core::{http_client::HttpClient, store::Store},
    error::Error,
};

use crate::api::gen::calendar::CalendarService;

impl<'c, IStore: Store, IClient: HttpClient> CalendarService<'c, IStore, IClient> {
    /// **api 版本: 2024-08-02T06:20:52+00:00**
    ///
    /// ## 更新日历信息
    ///
    /// 调用该接口以当前身份（应用或用户）修改指定日历的标题、描述、公开范围等信息。
    ///
    /// - 当前身份由 Header Authorization 的 Token 类型决定。tenant_access_token 指应用身份，user_access_token 指用户身份。
    ///
    /// - 如果使用应用身份调用该接口，则需要确保应用开启了[机器人能力](https://open.feishu.cn/document/uAjLw4CM/ugTN1YjL4UTN24CO1UjN/trouble-shooting/how-to-enable-bot-ability)。
    ///
    /// - 如果当前身份具有日历的 owner 访问权限，则可以更新日历的所有属性（包括全局生效的 summary、description、permission 和仅对当前身份生效的 color、summary_alias）。
    ///
    /// - 如果当前身份不具有日历的 owner 访问权限，则只能更新对当前身份生效的 color、summary_alias。你可以调用[查询日历信息](https://open.feishu.cn/document/uAjLw4CM/ukTMukTMukTM/reference/calendar-v4/calendar/get)接口，查看当前身份对日历的访问权限。
    ///
    /// doc: <https://open.larkoffice.com/document/uAjLw4CM/ukTMukTMukTM/reference/calendar-v4/calendar/patch>
    ///
    /// new doc: <https://open.larkoffice.com/document/server-docs/calendar-v4/calendar/patch>
    ///
    /// api url: <https://open.feishu.cn/document_portal/v1/document/get_detail?fullPath=%2Fserver-docs%2Fcalendar-v4%2Fcalendar%2Fpatch>
    pub async fn update_calendar(
        &self,
        req: UpdateCalendarReq,
    ) -> Result<(UpdateCalendarResp, CommonResponse), Error> {
        self.update_calendar_with_opt(req, Default::default()).await
    }

    /// 参见 [update_calendar](#method.update_calendar) 函数
    pub async fn update_calendar_with_opt(
        &self,
        req: UpdateCalendarReq,
        method_option: MethodOption,
    ) -> Result<(UpdateCalendarResp, CommonResponse), Error> {
        #[cfg(feature = "test-util")]
        {
            if let Some(f) = self.mock().get_mock_update_calendar(&req) {
                tracing::info!("[lark] Calendar#UpdateCalendar **mocking** api");
                return f(req);
            }
        }

        tracing::info!("[lark] Calendar#UpdateCalendar call api");

        let req = ApiRequest {
            scope: "Calendar",
            api: "UpdateCalendar",
            method: http::Method::PATCH,
            url: String::new()
                + self.cli.open_base_url.as_ref()
                + "/open-apis/calendar/v4/calendars/:calendar_id",
            param_data: req.gen_param(),
            method_option,
            need_tenant_access_token: true,
            need_user_access_token: true,
            ..Default::default()
        };

        let (resp, common_resp): (UpdateCalendarRespInner, _) = self.cli.do_req(req).await?;
        let data = match resp.data {
            Some(data) => data,
            None => {
                return Err(Error::ErrResponse(
                    anyhow::anyhow!("missing response data"),
                    common_resp,
                ));
            }
        };
        Ok((data, common_resp))
    }
}

#[derive(Debug, Serialize, Deserialize, Clone, Default, lark_bot_sdk_macros::ApiReqParams)]
pub struct UpdateCalendarReq {
    /// 日历 ID。
    ///
    /// 创建共享日历时会返回日历 ID。你也可以调用以下接口获取某一日历的 ID。
    ///
    /// - [查询主日历信息](https://open.feishu.cn/document/uAjLw4CM/ukTMukTMukTM/reference/calendar-v4/calendar/primary)
    ///
    /// - [查询日历列表](https://open.feishu.cn/document/uAjLw4CM/ukTMukTMukTM/reference/calendar-v4/calendar/list)
    ///
    /// - [搜索日历](https://open.feishu.cn/document/uAjLw4CM/ukTMukTMukTM/reference/calendar-v4/calendar/search)
    ///
    /// **是否必填**: 是
    ///
    /// **示例值**: "feishu.cn_xxxxxxxxxx@group.calendar.feishu.cn"
    #[api(kind = "path", name = "calendar_id")]
    pub calendar_id: String,

    /// 日历标题。
    ///
    /// **默认值**：空，表示不更新该字段。
    ///
    /// **示例值**: "测试日历"
    ///
    /// **数据校验规则**：
    ///
    /// - **长度范围**: `1` 字符- `255` 字符
    #[api(kind = "body", name = "summary")]
    pub summary: Option<String>,
    /// 日历描述。
    ///
    /// **默认值**：空，表示不更新该字段。
    ///
    /// **示例值**: "使用开放接口创建日历"
    ///
    /// **数据校验规则**：
    ///
    /// - **最大长度**: `255` 字符
    #[api(kind = "body", name = "description")]
    pub description: Option<String>,
    /// 日历公开范围。
    ///
    /// **默认值**：空，表示不更新该字段。
    ///
    /// **示例值**: "private"
    ///
    /// **可选值**:
    ///
    /// `Private`: 私密
    ///
    /// `ShowOnlyFreeBusy`: 仅展示忙闲信息
    ///
    /// `Public`: 公开，他人可查看日程详情
    #[api(kind = "body", name = "permissions")]
    pub permissions: Option<String>,
    /// 日历颜色，取值通过颜色 RGB 值的 int32 表示，其中，24 ~ 31 位为透明度，16 ~ 23 位为红，8 ~ 15 位为绿，0 ~ 7 位为蓝。例如，-11034625 表示 RGB 值 (87, 159, 255)。
    ///
    /// **注意**：
    ///
    /// - 取值范围为 -2^31 ~ 2^31-1
    ///
    /// - 日历颜色会映射到飞书客户端色板上最接近的一种颜色进行展示。
    ///
    /// - 该颜色仅对当前身份生效。
    ///
    /// **默认值**：空，表示不更新该字段。
    ///
    /// **示例值**: "-1"
    #[api(kind = "body", name = "color")]
    pub color: Option<i64>,
    /// 日历备注名，设置该字段后（包括后续修改该字段）仅对当前身份生效。
    ///
    /// **默认值**：空，表示不更新该字段。
    ///
    /// **示例值**: "日历备注名"
    ///
    /// **数据校验规则**：
    ///
    /// - **最大长度**: `255` 字符
    #[api(kind = "body", name = "summary_alias")]
    pub summary_alias: Option<String>,
}

#[derive(Debug, Serialize, Deserialize, Clone, lark_bot_sdk_macros::ApiBaseResp)]
struct UpdateCalendarRespInner {
    #[serde(flatten)]
    data: Option<UpdateCalendarResp>,
    #[serde(flatten)]
    base: BaseResp,
}

#[derive(Debug, Serialize, Deserialize, Clone, Default)]
pub struct UpdateCalendarResp {
    /// \-
    #[serde(
        rename = "data",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub data: DataSubResp,
}

#[derive(Debug, Serialize, Deserialize, Clone, Default)]
#[serde(default)]
pub struct DataSubResp {
    /// 更新后的日历实体信息。
    #[serde(
        rename = "calendar",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub calendar: CalendarSubResp,
}

#[derive(Debug, Serialize, Deserialize, Clone, Default)]
#[serde(default)]
pub struct CalendarSubResp {
    /// 日历 ID。后续可以通过该 ID 查询、更新或删除日历信息。更多信息参见[日历 ID 字段说明](https://open.feishu.cn/document/uAjLw4CM/ukTMukTMukTM/reference/calendar-v4/calendar/introduction)。
    ///
    /// **是否必填**: 是
    ///
    /// **示例值**: "feishu.cn_xxxxxxxxxx@group.calendar.feishu.cn"
    #[serde(
        rename = "calendar_id",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub calendar_id: String,
    /// 日历标题。
    ///
    /// **示例值**: "测试日历"
    ///
    /// **数据校验规则**：
    ///
    /// - **最大长度**: `255` 字符
    #[serde(
        rename = "summary",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub summary: String,
    /// 日历描述。
    ///
    /// **示例值**: "使用开放接口创建日历"
    ///
    /// **数据校验规则**：
    ///
    /// - **最大长度**: `255` 字符
    #[serde(
        rename = "description",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub description: String,
    /// 日历公开范围。
    ///
    /// **示例值**: "private"
    ///
    /// **可选值**:
    ///
    /// `Private`: 私密
    ///
    /// `ShowOnlyFreeBusy`: 仅展示忙闲信息
    ///
    /// `Public`: 公开，他人可查看日程详情
    #[serde(
        rename = "permissions",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub permissions: String,
    /// 日历颜色，由颜色 RGB 值的 int32 表示。实际在客户端展示时会映射到色板上最接近的一种颜色，且该字段仅对当前身份生效。
    ///
    /// **示例值**: "-1"
    #[serde(
        rename = "color",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub color: i64,
    /// 日历类型。
    ///
    /// **示例值**: "shared"
    ///
    /// **可选值**:
    ///
    /// `Unknown`: 未知类型
    ///
    /// `Primary`: 用户或应用的主日历
    ///
    /// `Shared`: 由用户或应用创建的共享日历
    ///
    /// `Google`: 用户绑定的谷歌日历
    ///
    /// `Resource`: 会议室日历
    ///
    /// `Exchange`: 用户绑定的 Exchange 日历
    #[serde(
        rename = "type",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub body_type: String,
    /// 日历备注名，仅对当前身份生效。
    ///
    /// **示例值**: "日历备注名"
    ///
    /// **数据校验规则**：
    ///
    /// - **最大长度**: `255` 字符
    #[serde(
        rename = "summary_alias",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub summary_alias: String,
    /// 对于当前身份，日历是否已经被标记为删除。
    ///
    /// **示例值**: "false"
    #[serde(
        rename = "is_deleted",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub is_deleted: bool,
    /// 当前日历是否是第三方数据。三方日历及日程只支持读，不支持写入。
    ///
    /// **示例值**: "false"
    #[serde(
        rename = "is_third_party",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub is_third_party: bool,
    /// 当前身份对于该日历的访问权限。
    ///
    /// **示例值**: "owner"
    ///
    /// **可选值**:
    ///
    /// `Unkonwn`: 未知权限
    ///
    /// `FreeBusyReader`: 游客，只能看到忙碌、空闲信息
    ///
    /// `Reader`: 订阅者，可查看所有日程详情
    ///
    /// `Writer`: 编辑者，可创建及修改日程
    ///
    /// `Owner`: 管理员，可管理日历及共享设置
    #[serde(
        rename = "role",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub role: String,
}

#[cfg(feature = "test-util")]
mod test_utils {
    use std::sync::Arc;

    use self::gen::calendar::CalendarServiceMocker;
    use crate::core::mocker::*;

    use super::*;
    use crate::api::*;
    use crate::core::model::*;

    pub struct Mocker;

    pub trait MockFunc:
        Fn(UpdateCalendarReq) -> Result<(UpdateCalendarResp, CommonResponse), Error>
        + Send
        + Sync
        + 'static
    {
    }
    impl<
            T: Fn(UpdateCalendarReq) -> Result<(UpdateCalendarResp, CommonResponse), Error>
                + Send
                + Sync
                + 'static,
        > MockFunc for T
    {
    }

    impl<'c, IStore: Store, IClient: HttpClient> CalendarServiceMocker<'c, IStore, IClient> {
        pub fn mock_update_calendar<F: MockFunc>(
            &self,
            f: F,
        ) -> MockerBuilder<Mocker, UpdateCalendarReq, UpdateCalendarResp, Arc<dyn MockFunc>>
        {
            MockerBuilder::new(self.cli.instance_id, Arc::new(f))
        }

        pub(super) fn get_mock_update_calendar(
            &self,
            req: &UpdateCalendarReq,
        ) -> Option<Arc<dyn MockFunc>> {
            do_mock::<Mocker, UpdateCalendarReq, UpdateCalendarResp, Arc<dyn MockFunc>>(
                self.cli.instance_id,
                req,
            )
        }
    }
}

#[cfg(feature = "test-util")]
#[cfg(test)]
mod test {
    use crate::{
        api::gen::calendar::update_calendar::{
            UpdateCalendarReq, UpdateCalendarResp, UpdateCalendarRespInner,
        },
        core::{model::CommonResponse, Lark},
    };

    #[tokio::test]
    async fn test_mock() {
        let lark = Lark::new("".to_owned(), "".into());
        let mocker = lark
            .calendar()
            .mock()
            .mock_update_calendar(|_| {
                Ok((UpdateCalendarResp::default(), CommonResponse::default()))
            })
            .build();
        let res = lark
            .calendar()
            .update_calendar(UpdateCalendarReq::default())
            .await;
        assert!(res.is_ok());
        mocker.clear();
        let res = lark
            .calendar()
            .update_calendar(UpdateCalendarReq::default())
            .await;
        assert!(res.is_err());
    }

    const REQ: &str = r#"{
    "summary": "测试日历",
    "description": "使用开放接口创建日历",
    "permissions": "private",
    "color": -1,
    "summary_alias": "日历备注名"
}"#;

    #[test]
    fn test_req() {
        if REQ == "{}" {
            return;
        }
        if let Err(e) = serde_json::from_str::<super::UpdateCalendarReqBody>(REQ) {
            panic!("{}", e);
        }
    }

    const RESP: &str = r#"{
    "code": 0,
    "msg": "success",
    "data": {
        "calendar": {
            "calendar_id": "feishu.cn_xxxxxxxxxx@group.calendar.feishu.cn",
            "summary": "测试日历",
            "description": "使用开放接口创建日历",
            "permissions": "private",
            "color": -1,
            "type": "shared",
            "summary_alias": "日历备注名",
            "is_deleted": false,
            "is_third_party": false,
            "role": "owner"
        }
    }
}"#;
    #[test]
    fn test_resp() {
        let res = serde_json::from_str::<UpdateCalendarRespInner>(RESP);
        if let Err(e) = res {
            panic!("{}", e);
        }
        if let Ok(v) = serde_json::from_str::<serde_json::Value>(RESP) {
            if v.get("data").is_some() {
                assert!(res.unwrap().data.is_some());
            }
        }
    }
}
