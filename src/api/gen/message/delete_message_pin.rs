//! doc url: <https://open.larkoffice.com/document/uAjLw4CM/ukTMukTMukTM/reference/im-v1/pin/delete>
// Code generated by gen_api. DO NOT EDIT.

use serde::{Deserialize, Serialize};

use crate::api::BaseResp;
use crate::api::HasBaseResp;
use crate::core::model::{ApiRequest, CommonResponse, MethodOption, ReqParam, StreamReqParam};
use crate::{
    core::{http_client::HttpClient, store::Store},
    error::Error,
};

use crate::api::gen::message::MessageService;

impl<'c, IStore: Store, IClient: HttpClient> MessageService<'c, IStore, IClient> {
    /// **api 版本: 2024-06-21T09:04:21+00:00**
    ///
    /// ## 移除 Pin 消息
    ///
    /// 移除一条指定消息的 Pin。
    ///
    /// 注意事项：
    ///
    /// - 需要开启[机器人能力](https://open.feishu.cn/document/uAjLw4CM/ugTN1YjL4UTN24CO1UjN/trouble-shooting/how-to-enable-bot-ability)
    ///
    /// - 移除Pin消息时，机器人必须在对应的群组中
    ///
    /// - 若消息未被Pin或已被撤回，返回成功信息
    ///
    /// - 不能移除一条对操作者不可见的Pin消息
    ///
    /// - 对同一条消息移除Pin的操作不能超过==5 QPS==
    ///
    /// doc: <https://open.larkoffice.com/document/uAjLw4CM/ukTMukTMukTM/reference/im-v1/pin/delete>
    ///
    /// new doc: <https://open.larkoffice.com/document/server-docs/im-v1/pin/delete>
    ///
    /// api url: <https://open.feishu.cn/document_portal/v1/document/get_detail?fullPath=%2Fserver-docs%2Fim-v1%2Fpin%2Fdelete>
    pub async fn delete_message_pin(
        &self,
        req: DeleteMessagePinReq,
    ) -> Result<(DeleteMessagePinResp, CommonResponse), Error> {
        self.delete_message_pin_with_opt(req, Default::default())
            .await
    }

    /// 参见 [delete_message_pin](#method.delete_message_pin) 函数
    pub async fn delete_message_pin_with_opt(
        &self,
        req: DeleteMessagePinReq,
        method_option: MethodOption,
    ) -> Result<(DeleteMessagePinResp, CommonResponse), Error> {
        #[cfg(feature = "test-util")]
        {
            if let Some(f) = self.mock().get_mock_delete_message_pin(&req) {
                tracing::info!("[lark] Message#DeleteMessagePin **mocking** api");
                return f(req);
            }
        }

        tracing::info!("[lark] Message#DeleteMessagePin call api");

        let req = ApiRequest {
            scope: "Message",
            api: "DeleteMessagePin",
            method: http::Method::DELETE,
            url: String::new()
                + self.cli.open_base_url.as_ref()
                + "/open-apis/im/v1/pins/:message_id",
            param_data: req.gen_param(),
            method_option,
            need_tenant_access_token: true,
            need_user_access_token: true,
            ..Default::default()
        };

        let (resp, common_resp): (DeleteMessagePinRespInner, _) = self.cli.do_req(req).await?;
        let data = match resp.data {
            Some(data) => data,
            None => {
                return Err(Error::ErrResponse(
                    anyhow::anyhow!("missing response data"),
                    common_resp,
                ));
            }
        };
        Ok((data, common_resp))
    }
}

#[derive(Debug, Serialize, Deserialize, Clone, Default, lark_bot_sdk_macros::ApiReqParams)]
pub struct DeleteMessagePinReq {
    /// 待移除Pin的消息ID，详情参见[消息ID说明](https://open.feishu.cn/document/uAjLw4CM/ukTMukTMukTM/reference/im-v1/message/intro#ac79c1c2)
    ///
    /// **是否必填**: 是
    ///
    /// **示例值**: "om_dc13264520392913993dd051dba21dcf"
    #[api(kind = "path", name = "message_id")]
    pub message_id: String,
}

#[derive(Debug, Serialize, Deserialize, Clone, lark_bot_sdk_macros::ApiBaseResp)]
struct DeleteMessagePinRespInner {
    #[serde(flatten)]
    data: Option<DeleteMessagePinResp>,
    #[serde(flatten)]
    base: BaseResp,
}

#[derive(Debug, Serialize, Deserialize, Clone, Default)]
pub struct DeleteMessagePinResp {
    /// \-
    #[serde(
        rename = "data",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub data: (),
}

#[cfg(feature = "test-util")]
mod test_utils {
    use std::sync::Arc;

    use self::gen::message::MessageServiceMocker;
    use crate::core::mocker::*;

    use super::*;
    use crate::api::*;
    use crate::core::model::*;

    pub struct Mocker;

    pub trait MockFunc:
        Fn(DeleteMessagePinReq) -> Result<(DeleteMessagePinResp, CommonResponse), Error>
        + Send
        + Sync
        + 'static
    {
    }
    impl<
            T: Fn(DeleteMessagePinReq) -> Result<(DeleteMessagePinResp, CommonResponse), Error>
                + Send
                + Sync
                + 'static,
        > MockFunc for T
    {
    }

    impl<'c, IStore: Store, IClient: HttpClient> MessageServiceMocker<'c, IStore, IClient> {
        pub fn mock_delete_message_pin<F: MockFunc>(
            &self,
            f: F,
        ) -> MockerBuilder<Mocker, DeleteMessagePinReq, DeleteMessagePinResp, Arc<dyn MockFunc>>
        {
            MockerBuilder::new(self.cli.instance_id, Arc::new(f))
        }

        pub(super) fn get_mock_delete_message_pin(
            &self,
            req: &DeleteMessagePinReq,
        ) -> Option<Arc<dyn MockFunc>> {
            do_mock::<Mocker, DeleteMessagePinReq, DeleteMessagePinResp, Arc<dyn MockFunc>>(
                self.cli.instance_id,
                req,
            )
        }
    }
}

#[cfg(feature = "test-util")]
#[cfg(test)]
mod test {
    use crate::{
        api::gen::message::delete_message_pin::{
            DeleteMessagePinReq, DeleteMessagePinResp, DeleteMessagePinRespInner,
        },
        core::{model::CommonResponse, Lark},
    };

    #[tokio::test]
    async fn test_mock() {
        let lark = Lark::new("".to_owned(), "".into());
        let mocker = lark
            .message()
            .mock()
            .mock_delete_message_pin(|_| {
                Ok((DeleteMessagePinResp::default(), CommonResponse::default()))
            })
            .build();
        let res = lark
            .message()
            .delete_message_pin(DeleteMessagePinReq::default())
            .await;
        assert!(res.is_ok());
        mocker.clear();
        let res = lark
            .message()
            .delete_message_pin(DeleteMessagePinReq::default())
            .await;
        assert!(res.is_err());
    }

    const REQ: &str = "{}";

    #[test]
    fn test_req() {
        if REQ == "{}" {
            return;
        }
        if let Err(e) = serde_json::from_str::<()>(REQ) {
            panic!("{}", e);
        }
    }

    const RESP: &str = r#"{
    "code": 0,
    "msg": "success",
    "data": {}
}"#;
    #[test]
    fn test_resp() {
        let res = serde_json::from_str::<DeleteMessagePinRespInner>(RESP);
        if let Err(e) = res {
            panic!("{}", e);
        }
        if let Ok(v) = serde_json::from_str::<serde_json::Value>(RESP) {
            if v.get("data").is_some() {
                assert!(res.unwrap().data.is_some());
            }
        }
    }
}
