//! doc url: <https://open.larkoffice.com/document/uAjLw4CM/ukTMukTMukTM/reference/bitable-v1/app/get>
// Code generated by gen_api. DO NOT EDIT.

use serde::{Deserialize, Serialize};

use crate::api::BaseResp;
use crate::api::HasBaseResp;
use crate::core::model::{ApiRequest, CommonResponse, MethodOption, ReqParam, StreamReqParam};
use crate::{
    core::{http_client::HttpClient, store::Store},
    error::Error,
};

use crate::api::gen::bitable::BitableService;

impl<'c, IStore: Store, IClient: HttpClient> BitableService<'c, IStore, IClient> {
    /// **api 版本: 2024-02-01T13:06:39+00:00**
    ///
    /// ## 获取多维表格元数据
    ///
    /// 获取指定多维表格的元数据信息，包括多维表格名称，多维表格版本号，多维表格是否开启高级权限等。
    ///

    ///
    /// doc: <https://open.larkoffice.com/document/uAjLw4CM/ukTMukTMukTM/reference/bitable-v1/app/get>
    ///
    /// new doc: <https://open.larkoffice.com/document/server-docs/docs/bitable-v1/app/get>
    ///
    /// api url: <https://open.feishu.cn/document_portal/v1/document/get_detail?fullPath=%2Fserver-docs%2Fdocs%2Fbitable-v1%2Fapp%2Fget>
    pub async fn get_bitable_meta(
        &self,
        req: GetBitableMetaReq,
    ) -> Result<(GetBitableMetaResp, CommonResponse), Error> {
        self.get_bitable_meta_with_opt(req, Default::default())
            .await
    }

    /// 参见 [get_bitable_meta](#method.get_bitable_meta) 函数
    pub async fn get_bitable_meta_with_opt(
        &self,
        req: GetBitableMetaReq,
        method_option: MethodOption,
    ) -> Result<(GetBitableMetaResp, CommonResponse), Error> {
        #[cfg(feature = "test-util")]
        {
            if let Some(f) = self.mock().get_mock_get_bitable_meta(&req) {
                tracing::info!("[lark] Bitable#GetBitableMeta **mocking** api");
                return f(req);
            }
        }

        tracing::info!("[lark] Bitable#GetBitableMeta call api");

        let req = ApiRequest {
            scope: "Bitable",
            api: "GetBitableMeta",
            method: http::Method::GET,
            url: String::new()
                + self.cli.open_base_url.as_ref()
                + "/open-apis/bitable/v1/apps/:app_token",
            param_data: req.gen_param(),
            method_option,
            need_tenant_access_token: true,
            need_user_access_token: true,
            ..Default::default()
        };

        let (resp, common_resp): (GetBitableMetaRespInner, _) = self.cli.do_req(req).await?;
        let data = match resp.data {
            Some(data) => data,
            None => {
                return Err(Error::ErrResponse(
                    anyhow::anyhow!("missing response data"),
                    common_resp,
                ));
            }
        };
        Ok((data, common_resp))
    }
}

#[derive(Debug, Serialize, Deserialize, Clone, Default, lark_bot_sdk_macros::ApiReqParams)]
pub struct GetBitableMetaReq {
    /// 多维表格的唯一标识符 [app_token 参数说明](https://open.feishu.cn/document/uAjLw4CM/ukTMukTMukTM/bitable/notification#8121eebe)
    ///
    /// **是否必填**: 是
    ///
    /// **示例值**: "appbcbWCzen6D8dezhoCH2RpMAh"
    #[api(kind = "path", name = "app_token")]
    pub app_token: String,
}

#[derive(Debug, Serialize, Deserialize, Clone, lark_bot_sdk_macros::ApiBaseResp)]
struct GetBitableMetaRespInner {
    #[serde(flatten)]
    data: Option<GetBitableMetaResp>,
    #[serde(flatten)]
    base: BaseResp,
}

#[derive(Debug, Serialize, Deserialize, Clone, Default)]
pub struct GetBitableMetaResp {
    /// \-
    #[serde(
        rename = "data",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub data: DataSubResp,
}

#[derive(Debug, Serialize, Deserialize, Clone, Default)]
#[serde(default)]
pub struct DataSubResp {
    /// 多维表格元数据
    #[serde(
        rename = "app",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub app: DisplayAppSubResp,
}

#[derive(Debug, Serialize, Deserialize, Clone, Default)]
#[serde(default)]
pub struct DisplayAppSubResp {
    /// 多维表格的 app_token
    ///
    /// [app_token 参数说明](https://open.feishu.cn/document/uAjLw4CM/ukTMukTMukTM/bitable/notification#8121eebe)
    ///
    /// **示例值**: "\-"
    #[serde(
        rename = "app_token",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub app_token: String,
    /// 多维表格的名字
    ///
    /// **示例值**: "\-"
    #[serde(
        rename = "name",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub name: String,
    /// 多维表格的版本号（对多维表格进行修改时更新，如新增、删除数据表，修改数据表名等，初始为1，每次更新+1）
    ///
    /// **示例值**: "\-"
    #[serde(
        rename = "revision",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub revision: i64,
    /// 多维表格是否开启了高级权限。取值包括：
    ///
    /// - true：表示开启了高级权限
    ///
    /// - false：表示关闭了高级权限
    ///
    /// [了解更多：使用多维表格高级权限](https://www.feishu.cn/hc/zh-CN/articles/588604550568)
    ///
    /// **示例值**: "\-"
    #[serde(
        rename = "is_advanced",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub is_advanced: bool,
    /// 文档时区
    ///
    /// **示例值**: "Asia/Beijing"
    #[serde(
        rename = "time_zone",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub time_zone: String,
}

#[cfg(feature = "test-util")]
mod test_utils {
    use std::sync::Arc;

    use self::gen::bitable::BitableServiceMocker;
    use crate::core::mocker::*;

    use super::*;
    use crate::api::*;
    use crate::core::model::*;

    pub struct Mocker;

    pub trait MockFunc:
        Fn(GetBitableMetaReq) -> Result<(GetBitableMetaResp, CommonResponse), Error>
        + Send
        + Sync
        + 'static
    {
    }
    impl<
            T: Fn(GetBitableMetaReq) -> Result<(GetBitableMetaResp, CommonResponse), Error>
                + Send
                + Sync
                + 'static,
        > MockFunc for T
    {
    }

    impl<'c, IStore: Store, IClient: HttpClient> BitableServiceMocker<'c, IStore, IClient> {
        pub fn mock_get_bitable_meta<F: MockFunc>(
            &self,
            f: F,
        ) -> MockerBuilder<Mocker, GetBitableMetaReq, GetBitableMetaResp, Arc<dyn MockFunc>>
        {
            MockerBuilder::new(self.cli.instance_id, Arc::new(f))
        }

        pub(super) fn get_mock_get_bitable_meta(
            &self,
            req: &GetBitableMetaReq,
        ) -> Option<Arc<dyn MockFunc>> {
            do_mock::<Mocker, GetBitableMetaReq, GetBitableMetaResp, Arc<dyn MockFunc>>(
                self.cli.instance_id,
                req,
            )
        }
    }
}

#[cfg(feature = "test-util")]
#[cfg(test)]
mod test {
    use crate::{
        api::gen::bitable::get_bitable_meta::{
            GetBitableMetaReq, GetBitableMetaResp, GetBitableMetaRespInner,
        },
        core::{model::CommonResponse, Lark},
    };

    #[tokio::test]
    async fn test_mock() {
        let lark = Lark::new("".to_owned(), "".into());
        let mocker = lark
            .bitable()
            .mock()
            .mock_get_bitable_meta(|_| {
                Ok((GetBitableMetaResp::default(), CommonResponse::default()))
            })
            .build();
        let res = lark
            .bitable()
            .get_bitable_meta(GetBitableMetaReq::default())
            .await;
        assert!(res.is_ok());
        mocker.clear();
        let res = lark
            .bitable()
            .get_bitable_meta(GetBitableMetaReq::default())
            .await;
        assert!(res.is_err());
    }

    const REQ: &str = "{}";

    #[test]
    fn test_req() {
        if REQ == "{}" {
            return;
        }
        if let Err(e) = serde_json::from_str::<()>(REQ) {
            panic!("{}", e);
        }
    }

    const RESP: &str = r#"{
	"code": 0,
	"msg": "success",
	"data": {
		"app": {
			"app_token": "appbcbWCzen6D8dezhoCH2RpMAh",
			"name": "mybase",
			"revision": 1,
			"is_advanced": false,
			"time_zone": "Asia/Beijing"
		}
	}
}"#;
    #[test]
    fn test_resp() {
        let res = serde_json::from_str::<GetBitableMetaRespInner>(RESP);
        if let Err(e) = res {
            panic!("{}", e);
        }
        if let Ok(v) = serde_json::from_str::<serde_json::Value>(RESP) {
            if v.get("data").is_some() {
                assert!(res.unwrap().data.is_some());
            }
        }
    }
}
