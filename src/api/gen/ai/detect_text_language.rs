//! doc url: <https://open.larkoffice.com/document/uAjLw4CM/ukTMukTMukTM/reference/ai/translation-v1/text/detect>
// Code generated by gen_api. DO NOT EDIT.

use serde::{Deserialize, Serialize};

use crate::api::BaseResp;
use crate::api::HasBaseResp;
use crate::core::model::{ApiRequest, CommonResponse, MethodOption, ReqParam, StreamReqParam};
use crate::{
    core::{http_client::HttpClient, store::Store},
    error::Error,
};

use crate::api::gen::ai::AiService;

impl<'c, IStore: Store, IClient: HttpClient> AiService<'c, IStore, IClient> {
    /// **api 版本: 2024-01-03T03:20:41+00:00**
    ///
    /// ## 识别文本语种
    ///
    /// 机器翻译 (MT)，支持 100 多种语言识别，返回符合 ISO 639-1 标准。
    ///
    /// 单租户限流：20QPS，同租户下的应用没有限流，共享本租户的 20QPS 限流。免费版不支持调用。
    ///
    /// doc: <https://open.larkoffice.com/document/uAjLw4CM/ukTMukTMukTM/reference/ai/translation-v1/text/detect>
    ///
    /// new doc: <https://open.larkoffice.com/document/server-docs/ai/translation-v1/detect>
    ///
    /// api url: <https://open.feishu.cn/document_portal/v1/document/get_detail?fullPath=%2Fserver-docs%2Fai%2Ftranslation-v1%2Fdetect>
    pub async fn detect_text_language(
        &self,
        req: DetectTextLanguageReq,
    ) -> Result<(DetectTextLanguageResp, CommonResponse), Error> {
        self.detect_text_language_with_opt(req, Default::default())
            .await
    }

    /// 参见 [detect_text_language](#method.detect_text_language) 函数
    pub async fn detect_text_language_with_opt(
        &self,
        req: DetectTextLanguageReq,
        method_option: MethodOption,
    ) -> Result<(DetectTextLanguageResp, CommonResponse), Error> {
        #[cfg(feature = "test-util")]
        {
            if let Some(f) = self.mock().get_mock_detect_text_language(&req) {
                tracing::info!("[lark] Ai#DetectTextLanguage **mocking** api");
                return f(req);
            }
        }

        tracing::info!("[lark] Ai#DetectTextLanguage call api");

        let req = ApiRequest {
            scope: "Ai",
            api: "DetectTextLanguage",
            method: http::Method::POST,
            url: String::new()
                + self.cli.open_base_url.as_ref()
                + "/open-apis/translation/v1/text/detect",
            param_data: req.gen_param(),
            method_option,
            need_tenant_access_token: true,
            ..Default::default()
        };

        let (resp, common_resp): (DetectTextLanguageRespInner, _) = self.cli.do_req(req).await?;
        let data = match resp.data {
            Some(data) => data,
            None => {
                return Err(Error::ErrResponse(
                    anyhow::anyhow!("missing response data"),
                    common_resp,
                ));
            }
        };
        Ok((data, common_resp))
    }
}

#[derive(Debug, Serialize, Deserialize, Clone, Default, lark_bot_sdk_macros::ApiReqParams)]
pub struct DetectTextLanguageReq {
    /// 需要被识别语种的文本
    ///
    /// **是否必填**: 是
    ///
    /// **示例值**: "你好"
    #[api(kind = "body", name = "text")]
    pub text: String,
}

#[derive(Debug, Serialize, Deserialize, Clone, lark_bot_sdk_macros::ApiBaseResp)]
struct DetectTextLanguageRespInner {
    #[serde(flatten)]
    data: Option<DetectTextLanguageResp>,
    #[serde(flatten)]
    base: BaseResp,
}

#[derive(Debug, Serialize, Deserialize, Clone, Default)]
pub struct DetectTextLanguageResp {
    /// \-
    #[serde(
        rename = "data",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub data: DataSubResp,
}

#[derive(Debug, Serialize, Deserialize, Clone, Default)]
#[serde(default)]
pub struct DataSubResp {
    /// 识别的文本语种，返回符合 ISO 639-1 标准
    ///
    /// **是否必填**: 是
    ///
    /// **示例值**: "zh"
    #[serde(
        rename = "language",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub language: String,
}

#[cfg(feature = "test-util")]
mod test_utils {
    use std::sync::Arc;

    use self::gen::ai::AiServiceMocker;
    use crate::core::mocker::*;

    use super::*;
    use crate::api::*;
    use crate::core::model::*;

    pub struct Mocker;

    pub trait MockFunc:
        Fn(DetectTextLanguageReq) -> Result<(DetectTextLanguageResp, CommonResponse), Error>
        + Send
        + Sync
        + 'static
    {
    }
    impl<
            T: Fn(DetectTextLanguageReq) -> Result<(DetectTextLanguageResp, CommonResponse), Error>
                + Send
                + Sync
                + 'static,
        > MockFunc for T
    {
    }

    impl<'c, IStore: Store, IClient: HttpClient> AiServiceMocker<'c, IStore, IClient> {
        pub fn mock_detect_text_language<F: MockFunc>(
            &self,
            f: F,
        ) -> MockerBuilder<Mocker, DetectTextLanguageReq, DetectTextLanguageResp, Arc<dyn MockFunc>>
        {
            MockerBuilder::new(self.cli.instance_id, Arc::new(f))
        }

        pub(super) fn get_mock_detect_text_language(
            &self,
            req: &DetectTextLanguageReq,
        ) -> Option<Arc<dyn MockFunc>> {
            do_mock::<Mocker, DetectTextLanguageReq, DetectTextLanguageResp, Arc<dyn MockFunc>>(
                self.cli.instance_id,
                req,
            )
        }
    }
}

#[cfg(feature = "test-util")]
#[cfg(test)]
mod test {
    use crate::{
        api::gen::ai::detect_text_language::{
            DetectTextLanguageReq, DetectTextLanguageResp, DetectTextLanguageRespInner,
        },
        core::{model::CommonResponse, Lark},
    };

    #[tokio::test]
    async fn test_mock() {
        let lark = Lark::new("".to_owned(), "".into());
        let mocker = lark
            .ai()
            .mock()
            .mock_detect_text_language(|_| {
                Ok((DetectTextLanguageResp::default(), CommonResponse::default()))
            })
            .build();
        let res = lark
            .ai()
            .detect_text_language(DetectTextLanguageReq::default())
            .await;
        assert!(res.is_ok());
        mocker.clear();
        let res = lark
            .ai()
            .detect_text_language(DetectTextLanguageReq::default())
            .await;
        assert!(res.is_err());
    }

    const REQ: &str = r#"{
    "text": "你好"
}"#;

    #[test]
    fn test_req() {
        if REQ == "{}" {
            return;
        }
        if let Err(e) = serde_json::from_str::<super::DetectTextLanguageReqBody>(REQ) {
            panic!("{}", e);
        }
    }

    const RESP: &str = r#"{
    "code": 0,
    "msg": "success",
    "data": {
        "language": "zh"
    }
}"#;
    #[test]
    fn test_resp() {
        let res = serde_json::from_str::<DetectTextLanguageRespInner>(RESP);
        if let Err(e) = res {
            panic!("{}", e);
        }
        if let Ok(v) = serde_json::from_str::<serde_json::Value>(RESP) {
            if v.get("data").is_some() {
                assert!(res.unwrap().data.is_some());
            }
        }
    }
}
