//! doc url: <https://open.larkoffice.com/document/ukTMukTMukTM/uUDN04SN0QjL1QDN/wiki-v2/space/get>
// Code generated by gen_api. DO NOT EDIT.

use serde::{Deserialize, Serialize};

use crate::api::BaseResp;
use crate::api::HasBaseResp;
use crate::core::model::{ApiRequest, CommonResponse, MethodOption, ReqParam, StreamReqParam};
use crate::{
    core::{http_client::HttpClient, store::Store},
    error::Error,
};

use crate::api::gen::drive::DriveService;

impl<'c, IStore: Store, IClient: HttpClient> DriveService<'c, IStore, IClient> {
    /// **api 版本: 2024-07-31T11:17:43+00:00**
    ///
    /// ## 获取知识空间信息
    ///
    /// 此接口用于根据知识空间 ID 查询知识空间的信息，包括空间的类型、可见性、分享状态等。
    ///

    ///
    /// doc: <https://open.larkoffice.com/document/ukTMukTMukTM/uUDN04SN0QjL1QDN/wiki-v2/space/get>
    ///
    /// new doc: <https://open.larkoffice.com/document/server-docs/docs/wiki-v2/space/get>
    ///
    /// api url: <https://open.feishu.cn/document_portal/v1/document/get_detail?fullPath=%2Fserver-docs%2Fdocs%2Fwiki-v2%2Fspace%2Fget>
    pub async fn get_wiki_space(
        &self,
        req: GetWikiSpaceReq,
    ) -> Result<(GetWikiSpaceResp, CommonResponse), Error> {
        self.get_wiki_space_with_opt(req, Default::default()).await
    }

    /// 参见 [get_wiki_space](#method.get_wiki_space) 函数
    pub async fn get_wiki_space_with_opt(
        &self,
        req: GetWikiSpaceReq,
        method_option: MethodOption,
    ) -> Result<(GetWikiSpaceResp, CommonResponse), Error> {
        #[cfg(feature = "test-util")]
        {
            if let Some(f) = self.mock().get_mock_get_wiki_space(&req) {
                tracing::info!("[lark] Drive#GetWikiSpace **mocking** api");
                return f(req);
            }
        }

        tracing::info!("[lark] Drive#GetWikiSpace call api");

        let req = ApiRequest {
            scope: "Drive",
            api: "GetWikiSpace",
            method: http::Method::GET,
            url: String::new()
                + self.cli.open_base_url.as_ref()
                + "/open-apis/wiki/v2/spaces/:space_id",
            param_data: req.gen_param(),
            method_option,
            need_tenant_access_token: true,
            need_user_access_token: true,
            ..Default::default()
        };

        let (resp, common_resp): (GetWikiSpaceRespInner, _) = self.cli.do_req(req).await?;
        let data = match resp.data {
            Some(data) => data,
            None => {
                return Err(Error::ErrResponse(
                    anyhow::anyhow!("missing response data"),
                    common_resp,
                ));
            }
        };
        Ok((data, common_resp))
    }
}

#[derive(Debug, Serialize, Deserialize, Clone, Default, lark_bot_sdk_macros::ApiReqParams)]
pub struct GetWikiSpaceReq {
    /// 知识空间 ID。可通过以下两种方式获取。了解更多，参考[知识库概述](https://open.feishu.cn/document/ukTMukTMukTM/uUDN04SN0QjL1QDN/wiki-overview)。
    ///
    /// - 调用 [获取知识空间列表](https://open.feishu.cn/document/ukTMukTMukTM/uUDN04SN0QjL1QDN/wiki-v2/space/list)获取
    ///
    /// - 如果你是知识库管理员，可以进入知识库设置页面，复制地址栏的数字部分：https://sample.feishu.cn/wiki/settings/==6870403571079249922==
    ///
    /// **示例值**: "6870403571079249922"
    #[api(kind = "path", name = "space_id")]
    pub space_id: String,
    /// 当查询个人文档库时，指定返回的文档库名称展示语言。
    ///
    /// **示例值**: "zh"
    ///
    /// **可选值**:
    ///
    /// `LangZH`: 简体中文
    ///
    /// `LangID`: 印尼语
    ///
    /// `LangDE`: 德语
    ///
    /// `LangEN`: 英语
    ///
    /// `LangES`: 西班牙语
    ///
    /// `LangFR`: 法语
    ///
    /// `LangIT`: 意大利语
    ///
    /// `LangPT`: 葡萄牙语
    ///
    /// `LangVI`: 越南语
    ///
    /// `LangRU`: 俄语
    ///
    /// `LangHI`: 印地语
    ///
    /// `LangTH`: 泰语
    ///
    /// `LangKO`: 韩语
    ///
    /// `LangJA`: 日语
    ///
    /// `LangZHHK`: 繁体中文（中国香港）
    ///
    /// `LangZHTW`: 繁体中文（中国台湾）
    #[api(kind = "query", name = "lang", v_type = "var", option = "false")]
    pub lang: String,
}

#[derive(Debug, Serialize, Deserialize, Clone, lark_bot_sdk_macros::ApiBaseResp)]
struct GetWikiSpaceRespInner {
    #[serde(flatten)]
    data: Option<GetWikiSpaceResp>,
    #[serde(flatten)]
    base: BaseResp,
}

#[derive(Debug, Serialize, Deserialize, Clone, Default)]
pub struct GetWikiSpaceResp {
    /// \-
    #[serde(
        rename = "data",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub data: DataSubResp,
}

#[derive(Debug, Serialize, Deserialize, Clone, Default)]
#[serde(default)]
pub struct DataSubResp {
    /// 知识空间
    #[serde(
        rename = "space",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub space: SpaceSubResp,
}

#[derive(Debug, Serialize, Deserialize, Clone, Default)]
#[serde(default)]
pub struct SpaceSubResp {
    /// 知识空间名称
    ///
    /// **示例值**: "workspace name"
    #[serde(
        rename = "name",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub name: String,
    /// 知识空间描述
    ///
    /// **示例值**: "workspace description"
    #[serde(
        rename = "description",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub description: String,
    /// 知识空间 ID
    ///
    /// **示例值**: "123456"
    #[serde(
        rename = "space_id",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub space_id: String,
    /// 表示知识空间类型
    ///
    /// **示例值**: "team"
    ///
    /// **可选值**:
    ///
    /// `Team`: 团队空间，归团队（多人)管理，可添加多个管理员
    ///
    /// `Person`: 个人空间，归个人管理。一人仅可拥有一个个人空间，无法添加其他管理员
    #[serde(
        rename = "space_type",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub space_type: String,
    /// 表示知识空间可见性
    ///
    /// **示例值**: "private"
    ///
    /// **可选值**:
    ///
    /// `Public`: 公开空间。租户所有用户可见，默认为成员权限。无法额外添加成员，但可以添加管理员
    ///
    /// `Private`: 私有空间。仅对知识空间管理员、成员可见，需要手动添加管理员、成员
    #[serde(
        rename = "visibility",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub visibility: String,
    /// 表示知识空间的分享状态
    ///
    /// **示例值**: "open"
    ///
    /// **可选值**:
    ///
    /// `Open`: 打开，即知识空间发布到互联网
    ///
    /// `Closed`: 关闭，即知识空间未发布到互联网
    ///
    /// **数据校验规则**：
    ///
    /// - **长度范围**: `0` 字符- `99` 字符
    #[serde(
        rename = "open_sharing",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub open_sharing: String,
}

#[cfg(feature = "test-util")]
mod test_utils {
    use std::sync::Arc;

    use self::gen::drive::DriveServiceMocker;
    use crate::core::mocker::*;

    use super::*;
    use crate::api::*;
    use crate::core::model::*;

    pub struct Mocker;

    pub trait MockFunc:
        Fn(GetWikiSpaceReq) -> Result<(GetWikiSpaceResp, CommonResponse), Error>
        + Send
        + Sync
        + 'static
    {
    }
    impl<
            T: Fn(GetWikiSpaceReq) -> Result<(GetWikiSpaceResp, CommonResponse), Error>
                + Send
                + Sync
                + 'static,
        > MockFunc for T
    {
    }

    impl<'c, IStore: Store, IClient: HttpClient> DriveServiceMocker<'c, IStore, IClient> {
        pub fn mock_get_wiki_space<F: MockFunc>(
            &self,
            f: F,
        ) -> MockerBuilder<Mocker, GetWikiSpaceReq, GetWikiSpaceResp, Arc<dyn MockFunc>> {
            MockerBuilder::new(self.cli.instance_id, Arc::new(f))
        }

        pub(super) fn get_mock_get_wiki_space(
            &self,
            req: &GetWikiSpaceReq,
        ) -> Option<Arc<dyn MockFunc>> {
            do_mock::<Mocker, GetWikiSpaceReq, GetWikiSpaceResp, Arc<dyn MockFunc>>(
                self.cli.instance_id,
                req,
            )
        }
    }
}

#[cfg(feature = "test-util")]
#[cfg(test)]
mod test {
    use crate::{
        api::gen::drive::get_wiki_space::{
            GetWikiSpaceReq, GetWikiSpaceResp, GetWikiSpaceRespInner,
        },
        core::{model::CommonResponse, Lark},
    };

    #[tokio::test]
    async fn test_mock() {
        let lark = Lark::new("".to_owned(), "".into());
        let mocker = lark
            .drive()
            .mock()
            .mock_get_wiki_space(|_| Ok((GetWikiSpaceResp::default(), CommonResponse::default())))
            .build();
        let res = lark
            .drive()
            .get_wiki_space(GetWikiSpaceReq::default())
            .await;
        assert!(res.is_ok());
        mocker.clear();
        let res = lark
            .drive()
            .get_wiki_space(GetWikiSpaceReq::default())
            .await;
        assert!(res.is_err());
    }

    const REQ: &str = "{}";

    #[test]
    fn test_req() {
        if REQ == "{}" {
            return;
        }
        if let Err(e) = serde_json::from_str::<()>(REQ) {
            panic!("{}", e);
        }
    }

    const RESP: &str = r#"{
    "code": 0,
    "msg": "success",
    "data": {
        "space": {
            "name": "知识空间",
            "description": "知识空间描述",
            "space_id": "1565676577122621"
        }
    }
}"#;
    #[test]
    fn test_resp() {
        let res = serde_json::from_str::<GetWikiSpaceRespInner>(RESP);
        if let Err(e) = res {
            panic!("{}", e);
        }
        if let Ok(v) = serde_json::from_str::<serde_json::Value>(RESP) {
            if v.get("data").is_some() {
                assert!(res.unwrap().data.is_some());
            }
        }
    }
}
