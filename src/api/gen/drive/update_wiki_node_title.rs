//! doc url: <https://open.larkoffice.com/document/ukTMukTMukTM/uUDN04SN0QjL1QDN/wiki-v2/space-node/update_title>
// Code generated by gen_api. DO NOT EDIT.

use serde::{Deserialize, Serialize};

use crate::api::BaseResp;
use crate::api::HasBaseResp;
use crate::core::model::{ApiRequest, CommonResponse, MethodOption, ReqParam, StreamReqParam};
use crate::{
    core::{http_client::HttpClient, store::Store},
    error::Error,
};

use crate::api::gen::drive::DriveService;

impl<'c, IStore: Store, IClient: HttpClient> DriveService<'c, IStore, IClient> {
    /// **api 版本: 2024-07-31T11:17:57+00:00**
    ///
    /// ## 更新知识空间节点标题
    ///
    /// 此接口用于更新节点标题
    ///
    /// 此接口目前仅支持文档(doc)、新版文档(docx)和快捷方式。
    ///
    /// doc: <https://open.larkoffice.com/document/ukTMukTMukTM/uUDN04SN0QjL1QDN/wiki-v2/space-node/update_title>
    ///
    /// new doc: <https://open.larkoffice.com/document/server-docs/docs/wiki-v2/space-node/update_title>
    ///
    /// api url: <https://open.feishu.cn/document_portal/v1/document/get_detail?fullPath=%2Fserver-docs%2Fdocs%2Fwiki-v2%2Fspace-node%2Fupdate_title>
    pub async fn update_wiki_node_title(
        &self,
        req: UpdateWikiNodeTitleReq,
    ) -> Result<(UpdateWikiNodeTitleResp, CommonResponse), Error> {
        self.update_wiki_node_title_with_opt(req, Default::default())
            .await
    }

    /// 参见 [update_wiki_node_title](#method.update_wiki_node_title) 函数
    pub async fn update_wiki_node_title_with_opt(
        &self,
        req: UpdateWikiNodeTitleReq,
        method_option: MethodOption,
    ) -> Result<(UpdateWikiNodeTitleResp, CommonResponse), Error> {
        #[cfg(feature = "test-util")]
        {
            if let Some(f) = self.mock().get_mock_update_wiki_node_title(&req) {
                tracing::info!("[lark] Drive#UpdateWikiNodeTitle **mocking** api");
                return f(req);
            }
        }

        tracing::info!("[lark] Drive#UpdateWikiNodeTitle call api");

        let req = ApiRequest {
            scope: "Drive",
            api: "UpdateWikiNodeTitle",
            method: http::Method::POST,
            url: String::new()
                + self.cli.open_base_url.as_ref()
                + "/open-apis/wiki/v2/spaces/:space_id/nodes/:node_token/update_title",
            param_data: req.gen_param(),
            method_option,
            need_tenant_access_token: true,
            need_user_access_token: true,
            ..Default::default()
        };

        let (resp, common_resp): (UpdateWikiNodeTitleRespInner, _) = self.cli.do_req(req).await?;
        let data = match resp.data {
            Some(data) => data,
            None => {
                return Err(Error::ErrResponse(
                    anyhow::anyhow!("missing response data"),
                    common_resp,
                ));
            }
        };
        Ok((data, common_resp))
    }
}

#[derive(Debug, Serialize, Deserialize, Clone, Default, lark_bot_sdk_macros::ApiReqParams)]
pub struct UpdateWikiNodeTitleReq {
    /// 知识空间ID
    ///
    /// **示例值**: "6946843325487912356"
    #[api(kind = "path", name = "space_id")]
    pub space_id: String,
    /// 节点token
    ///
    /// **示例值**: "wikcnKQ1k3pcuo5uSK4t8Vabcef"
    #[api(kind = "path", name = "node_token")]
    pub node_token: String,

    /// 节点新标题
    ///
    /// **是否必填**: 是
    ///
    /// **示例值**: "新标题"
    #[api(kind = "body", name = "title")]
    pub title: String,
}

#[derive(Debug, Serialize, Deserialize, Clone, lark_bot_sdk_macros::ApiBaseResp)]
struct UpdateWikiNodeTitleRespInner {
    #[serde(flatten)]
    data: Option<UpdateWikiNodeTitleResp>,
    #[serde(flatten)]
    base: BaseResp,
}

#[derive(Debug, Serialize, Deserialize, Clone, Default)]
pub struct UpdateWikiNodeTitleResp {
    /// \-
    #[serde(
        rename = "data",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub data: (),
}

#[cfg(feature = "test-util")]
mod test_utils {
    use std::sync::Arc;

    use self::gen::drive::DriveServiceMocker;
    use crate::core::mocker::*;

    use super::*;
    use crate::api::*;
    use crate::core::model::*;

    pub struct Mocker;

    pub trait MockFunc:
        Fn(UpdateWikiNodeTitleReq) -> Result<(UpdateWikiNodeTitleResp, CommonResponse), Error>
        + Send
        + Sync
        + 'static
    {
    }
    impl<
            T: Fn(
                    UpdateWikiNodeTitleReq,
                ) -> Result<(UpdateWikiNodeTitleResp, CommonResponse), Error>
                + Send
                + Sync
                + 'static,
        > MockFunc for T
    {
    }

    impl<'c, IStore: Store, IClient: HttpClient> DriveServiceMocker<'c, IStore, IClient> {
        pub fn mock_update_wiki_node_title<F: MockFunc>(
            &self,
            f: F,
        ) -> MockerBuilder<Mocker, UpdateWikiNodeTitleReq, UpdateWikiNodeTitleResp, Arc<dyn MockFunc>>
        {
            MockerBuilder::new(self.cli.instance_id, Arc::new(f))
        }

        pub(super) fn get_mock_update_wiki_node_title(
            &self,
            req: &UpdateWikiNodeTitleReq,
        ) -> Option<Arc<dyn MockFunc>> {
            do_mock::<Mocker, UpdateWikiNodeTitleReq, UpdateWikiNodeTitleResp, Arc<dyn MockFunc>>(
                self.cli.instance_id,
                req,
            )
        }
    }
}

#[cfg(feature = "test-util")]
#[cfg(test)]
mod test {
    use crate::{
        api::gen::drive::update_wiki_node_title::{
            UpdateWikiNodeTitleReq, UpdateWikiNodeTitleResp, UpdateWikiNodeTitleRespInner,
        },
        core::{model::CommonResponse, Lark},
    };

    #[tokio::test]
    async fn test_mock() {
        let lark = Lark::new("".to_owned(), "".into());
        let mocker = lark
            .drive()
            .mock()
            .mock_update_wiki_node_title(|_| {
                Ok((
                    UpdateWikiNodeTitleResp::default(),
                    CommonResponse::default(),
                ))
            })
            .build();
        let res = lark
            .drive()
            .update_wiki_node_title(UpdateWikiNodeTitleReq::default())
            .await;
        assert!(res.is_ok());
        mocker.clear();
        let res = lark
            .drive()
            .update_wiki_node_title(UpdateWikiNodeTitleReq::default())
            .await;
        assert!(res.is_err());
    }

    const REQ: &str = r#"{
    "title": "新标题"
}"#;

    #[test]
    fn test_req() {
        if REQ == "{}" {
            return;
        }
        if let Err(e) = serde_json::from_str::<super::UpdateWikiNodeTitleReqBody>(REQ) {
            panic!("{}", e);
        }
    }

    const RESP: &str = r#"{
    "code": 0,
    "msg": "success",
    "data": {}
}"#;
    #[test]
    fn test_resp() {
        let res = serde_json::from_str::<UpdateWikiNodeTitleRespInner>(RESP);
        if let Err(e) = res {
            panic!("{}", e);
        }
        if let Ok(v) = serde_json::from_str::<serde_json::Value>(RESP) {
            if v.get("data").is_some() {
                assert!(res.unwrap().data.is_some());
            }
        }
    }
}
