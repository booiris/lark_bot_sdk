//! doc url: <https://open.larkoffice.com/document/ukTMukTMukTM/uUDN04SN0QjL1QDN/wiki-v2/space-member/delete>
// Code generated by gen_api. DO NOT EDIT.

use serde::{Deserialize, Serialize};

use crate::api::BaseResp;
use crate::api::HasBaseResp;
use crate::core::model::{ApiRequest, CommonResponse, MethodOption, ReqParam, StreamReqParam};
use crate::{
    core::{http_client::HttpClient, store::Store},
    error::Error,
};

use crate::api::gen::drive::DriveService;

impl<'c, IStore: Store, IClient: HttpClient> DriveService<'c, IStore, IClient> {
    /// **api 版本: 2024-07-31T11:17:56+00:00**
    ///
    /// ## 删除知识空间成员
    ///
    /// 此接口用于删除知识空间成员或管理员。
    ///
    /// 使用tenant access token操作时，无法使用部门ID(opendepartmentid)删除知识空间成员。
    ///
    /// 知识空间具有[类型](https://open.feishu.cn/document/ukTMukTMukTM/uUDN04SN0QjL1QDN/wiki-overview)和[可见性](https://open.feishu.cn/document/ukTMukTMukTM/uUDN04SN0QjL1QDN/wiki-overview)的概念。不同的类型或可见性可以对本操作做出限制：
    ///
    /// - 可见性限制：公开知识空间（visibility为public）对租户所有用户可见，因此不支持再删除成员，但可以删除管理员。
    ///
    /// - 类型限制：个人知识空间 （type为person）为个人管理的知识空间，不支持删除管理员。但可以删除成员。
    ///
    /// 知识空间权限要求，当前用户或应用：
    ///
    /// - 为知识空间管理员
    ///
    /// doc: <https://open.larkoffice.com/document/ukTMukTMukTM/uUDN04SN0QjL1QDN/wiki-v2/space-member/delete>
    ///
    /// new doc: <https://open.larkoffice.com/document/server-docs/docs/wiki-v2/space-member/delete>
    ///
    /// api url: <https://open.feishu.cn/document_portal/v1/document/get_detail?fullPath=%2Fserver-docs%2Fdocs%2Fwiki-v2%2Fspace-member%2Fdelete>
    pub async fn delete_wiki_space_member(
        &self,
        req: DeleteWikiSpaceMemberReq,
    ) -> Result<(DeleteWikiSpaceMemberResp, CommonResponse), Error> {
        self.delete_wiki_space_member_with_opt(req, Default::default())
            .await
    }

    /// 参见 [delete_wiki_space_member](#method.delete_wiki_space_member) 函数
    pub async fn delete_wiki_space_member_with_opt(
        &self,
        req: DeleteWikiSpaceMemberReq,
        method_option: MethodOption,
    ) -> Result<(DeleteWikiSpaceMemberResp, CommonResponse), Error> {
        #[cfg(feature = "test-util")]
        {
            if let Some(f) = self.mock().get_mock_delete_wiki_space_member(&req) {
                tracing::info!("[lark] Drive#DeleteWikiSpaceMember **mocking** api");
                return f(req);
            }
        }

        tracing::info!("[lark] Drive#DeleteWikiSpaceMember call api");

        let req = ApiRequest {
            scope: "Drive",
            api: "DeleteWikiSpaceMember",
            method: http::Method::DELETE,
            url: String::new()
                + self.cli.open_base_url.as_ref()
                + "/open-apis/wiki/v2/spaces/:space_id/members/:member_id",
            param_data: req.gen_param(),
            method_option,
            need_tenant_access_token: true,
            need_user_access_token: true,
            ..Default::default()
        };

        let (resp, common_resp): (DeleteWikiSpaceMemberRespInner, _) = self.cli.do_req(req).await?;
        let data = match resp.data {
            Some(data) => data,
            None => {
                return Err(Error::ErrResponse(
                    anyhow::anyhow!("missing response data"),
                    common_resp,
                ));
            }
        };
        Ok((data, common_resp))
    }
}

#[derive(Debug, Serialize, Deserialize, Clone, Default, lark_bot_sdk_macros::ApiReqParams)]
pub struct DeleteWikiSpaceMemberReq {
    /// 知识空间id
    ///
    /// **是否必填**: 是
    ///
    /// **示例值**: "7008061636015554580"
    #[api(kind = "path", name = "space_id")]
    pub space_id: String,
    /// 成员id，值的类型由请求体的 member_type 参数决定
    ///
    /// **是否必填**: 是
    ///
    /// **示例值**: "g64fb7g7"
    #[api(kind = "path", name = "member_id")]
    pub member_id: String,

    /// “openchat” - 群id
    ///
    /// “userid” - 用户id
    ///
    /// “email” - 邮箱
    ///
    /// “opendepartmentid” - 部门id
    ///
    /// “openid” - 应用openid
    ///
    /// “unionid” - [unionid](/:ssltoken/home/user-identity-introduction/union-id
    ///
    /// )
    ///
    /// **是否必填**: 是
    ///
    /// **示例值**: "userid"
    #[api(kind = "body", name = "member_type")]
    pub member_type: String,
    /// 角色:
    ///
    /// “admin” - 管理员
    ///
    /// “member” - 成员
    ///
    /// **是否必填**: 是
    ///
    /// **示例值**: "admin"
    #[api(kind = "body", name = "member_role")]
    pub member_role: String,
    /// 知识库协作者类型（暂不支持）
    ///
    /// **示例值**: "user"
    ///
    /// **可选值**:
    ///
    /// `user`: 用户
    ///
    /// `chat`: 群组
    ///
    /// `department`: 组织架构
    #[api(kind = "body", name = "type")]
    pub body_type: Option<String>,
}

#[derive(Debug, Serialize, Deserialize, Clone, lark_bot_sdk_macros::ApiBaseResp)]
struct DeleteWikiSpaceMemberRespInner {
    #[serde(flatten)]
    data: Option<DeleteWikiSpaceMemberResp>,
    #[serde(flatten)]
    base: BaseResp,
}

#[derive(Debug, Serialize, Deserialize, Clone, Default)]
pub struct DeleteWikiSpaceMemberResp {
    /// \-
    #[serde(
        rename = "data",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub data: DataSubResp,
}

#[derive(Debug, Serialize, Deserialize, Clone, Default)]
#[serde(default)]
pub struct DataSubResp {
    /// 成员信息
    ///
    /// **是否必填**: 是
    #[serde(
        rename = "member",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub member: MemberSubResp,
}

#[derive(Debug, Serialize, Deserialize, Clone, Default)]
#[serde(default)]
pub struct MemberSubResp {
    /// “openchat” - 群id
    ///
    /// “userid” - 用户id
    ///
    /// “email” - 邮箱
    ///
    /// “opendepartmentid” - 部门id
    ///
    /// “openid” - 应用openid
    ///
    /// “unionid” - [unionid](/:ssltoken/home/user-identity-introduction/union-id
    ///
    /// )
    ///
    /// **是否必填**: 是
    ///
    /// **示例值**: "userid"
    #[serde(
        rename = "member_type",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub member_type: String,
    /// 用户id，值的类型由上面的 member_type 参数决定
    ///
    /// **是否必填**: 是
    ///
    /// **示例值**: "1565676577122621"
    #[serde(
        rename = "member_id",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub member_id: String,
    /// 角色:
    ///
    /// “admin” - 管理员
    ///
    /// “member” - 成员
    ///
    /// **是否必填**: 是
    ///
    /// **示例值**: "admin"
    #[serde(
        rename = "member_role",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub member_role: String,
    /// 知识库协作者类型（暂不支持）
    ///
    /// **示例值**: "user"
    ///
    /// **可选值**:
    ///
    /// `user`: 用户
    ///
    /// `chat`: 群组
    ///
    /// `department`: 组织架构
    #[serde(
        rename = "type",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub body_type: String,
}

#[cfg(feature = "test-util")]
mod test_utils {
    use std::sync::Arc;

    use self::gen::drive::DriveServiceMocker;
    use crate::core::mocker::*;

    use super::*;
    use crate::api::*;
    use crate::core::model::*;

    pub struct Mocker;

    pub trait MockFunc:
        Fn(DeleteWikiSpaceMemberReq) -> Result<(DeleteWikiSpaceMemberResp, CommonResponse), Error>
        + Send
        + Sync
        + 'static
    {
    }
    impl<
            T: Fn(
                    DeleteWikiSpaceMemberReq,
                ) -> Result<(DeleteWikiSpaceMemberResp, CommonResponse), Error>
                + Send
                + Sync
                + 'static,
        > MockFunc for T
    {
    }

    impl<'c, IStore: Store, IClient: HttpClient> DriveServiceMocker<'c, IStore, IClient> {
        pub fn mock_delete_wiki_space_member<F: MockFunc>(
            &self,
            f: F,
        ) -> MockerBuilder<
            Mocker,
            DeleteWikiSpaceMemberReq,
            DeleteWikiSpaceMemberResp,
            Arc<dyn MockFunc>,
        > {
            MockerBuilder::new(self.cli.instance_id, Arc::new(f))
        }

        pub(super) fn get_mock_delete_wiki_space_member(
            &self,
            req: &DeleteWikiSpaceMemberReq,
        ) -> Option<Arc<dyn MockFunc>> {
            do_mock::<Mocker, DeleteWikiSpaceMemberReq, DeleteWikiSpaceMemberResp, Arc<dyn MockFunc>>(
                self.cli.instance_id,
                req,
            )
        }
    }
}

#[cfg(feature = "test-util")]
#[cfg(test)]
mod test {
    use crate::{
        api::gen::drive::delete_wiki_space_member::{
            DeleteWikiSpaceMemberReq, DeleteWikiSpaceMemberResp, DeleteWikiSpaceMemberRespInner,
        },
        core::{model::CommonResponse, Lark},
    };

    #[tokio::test]
    async fn test_mock() {
        let lark = Lark::new("".to_owned(), "".into());
        let mocker = lark
            .drive()
            .mock()
            .mock_delete_wiki_space_member(|_| {
                Ok((
                    DeleteWikiSpaceMemberResp::default(),
                    CommonResponse::default(),
                ))
            })
            .build();
        let res = lark
            .drive()
            .delete_wiki_space_member(DeleteWikiSpaceMemberReq::default())
            .await;
        assert!(res.is_ok());
        mocker.clear();
        let res = lark
            .drive()
            .delete_wiki_space_member(DeleteWikiSpaceMemberReq::default())
            .await;
        assert!(res.is_err());
    }

    const REQ: &str = r#"{
    "member_type": "userid",
    "member_role": "admin",
    "type": "user"
}"#;

    #[test]
    fn test_req() {
        if REQ == "{}" {
            return;
        }
        if let Err(e) = serde_json::from_str::<super::DeleteWikiSpaceMemberReqBody>(REQ) {
            panic!("{}", e);
        }
    }

    const RESP: &str = r#"{
    "code": 0,
    "msg": "success",
    "data": {
        "member": {
            "member_type": "userid",
            "member_id": "1565676577122621",
            "member_role": "admin",
            "type": "user"
        }
    }
}"#;
    #[test]
    fn test_resp() {
        let res = serde_json::from_str::<DeleteWikiSpaceMemberRespInner>(RESP);
        if let Err(e) = res {
            panic!("{}", e);
        }
        if let Ok(v) = serde_json::from_str::<serde_json::Value>(RESP) {
            if v.get("data").is_some() {
                assert!(res.unwrap().data.is_some());
            }
        }
    }
}
