//! doc url: <https://open.larkoffice.com/document/ukTMukTMukTM/uUDN04SN0QjL1QDN/document-docx/docx-v1/document/raw_content>
// Code generated by gen_api. DO NOT EDIT.

use serde::{Deserialize, Serialize};

use crate::api::BaseResp;
use crate::api::HasBaseResp;
use crate::core::model::{ApiRequest, CommonResponse, MethodOption, ReqParam, StreamReqParam};
use crate::{
    core::{http_client::HttpClient, store::Store},
    error::Error,
};

use crate::api::gen::drive::DriveService;

impl<'c, IStore: Store, IClient: HttpClient> DriveService<'c, IStore, IClient> {
    /// **api 版本: 2024-07-31T09:16:49+00:00**
    ///
    /// ## 获取文档纯文本内容
    ///
    /// 获取文档的纯文本内容。
    ///
    /// **应用频率限制**：单个应用调用频率上限为每秒 5 次，超过该频率限制，接口将返回 HTTP 状态码 <font color="blue">400</font> 及错误码 <font color="blue">99991400</font>。当请求被限频，应用需要处理限频状态码，并使用指数退避算法或其它一些频控策略降低对 API 的调用速率。
    ///
    /// doc: <https://open.larkoffice.com/document/ukTMukTMukTM/uUDN04SN0QjL1QDN/document-docx/docx-v1/document/raw_content>
    ///
    /// new doc: <https://open.larkoffice.com/document/server-docs/docs/docs/docx-v1/document/raw_content>
    ///
    /// api url: <https://open.feishu.cn/document_portal/v1/document/get_detail?fullPath=%2Fserver-docs%2Fdocs%2Fdocs%2Fdocx-v1%2Fdocument%2Fraw_content>
    pub async fn get_docx_document_raw_content(
        &self,
        req: GetDocxDocumentRawContentReq,
    ) -> Result<(GetDocxDocumentRawContentResp, CommonResponse), Error> {
        self.get_docx_document_raw_content_with_opt(req, Default::default())
            .await
    }

    /// 参见 [get_docx_document_raw_content](#method.get_docx_document_raw_content) 函数
    pub async fn get_docx_document_raw_content_with_opt(
        &self,
        req: GetDocxDocumentRawContentReq,
        method_option: MethodOption,
    ) -> Result<(GetDocxDocumentRawContentResp, CommonResponse), Error> {
        #[cfg(feature = "test-util")]
        {
            if let Some(f) = self.mock().get_mock_get_docx_document_raw_content(&req) {
                tracing::info!("[lark] Drive#GetDocxDocumentRawContent **mocking** api");
                return f(req);
            }
        }

        tracing::info!("[lark] Drive#GetDocxDocumentRawContent call api");

        let req = ApiRequest {
            scope: "Drive",
            api: "GetDocxDocumentRawContent",
            method: http::Method::GET,
            url: String::new()
                + self.cli.open_base_url.as_ref()
                + "/open-apis/docx/v1/documents/:document_id/raw_content",
            param_data: req.gen_param(),
            method_option,
            need_tenant_access_token: true,
            need_user_access_token: true,
            ..Default::default()
        };

        let (resp, common_resp): (GetDocxDocumentRawContentRespInner, _) =
            self.cli.do_req(req).await?;
        let data = match resp.data {
            Some(data) => data,
            None => {
                return Err(Error::ErrResponse(
                    anyhow::anyhow!("missing response data"),
                    common_resp,
                ));
            }
        };
        Ok((data, common_resp))
    }
}

#[derive(Debug, Serialize, Deserialize, Clone, Default, lark_bot_sdk_macros::ApiReqParams)]
pub struct GetDocxDocumentRawContentReq {
    /// 文档的唯一标识。点击[这里](https://open.feishu.cn/document/ukTMukTMukTM/uUDN04SN0QjL1QDN/document-docx/docx-overview)了解如何获取文档的 `document_id`
    ///
    /// **是否必填**: 是
    ///
    /// **示例值**: "doxbcmEtbFrbbq10nPNu8gabcef"
    ///
    /// **数据校验规则**：
    ///
    /// - **长度范围**: `27` 字符- `27` 字符
    #[api(kind = "path", name = "document_id")]
    pub document_id: String,
    /// 指定返回的 MentionUser 即 @用户 的语言
    ///
    /// **示例值**: "0"
    ///
    /// **可选值**:
    ///
    /// `ZH`: 该用户的默认名称。如：@张敏
    ///
    /// `EN`: 该用户的英文名称。如：@Min Zhang
    ///
    /// `JP`: 暂不支持该枚举，使用时返回该用户的默认名称
    #[api(kind = "query", name = "lang", v_type = "var", option = "false")]
    pub lang: i64,
}

#[derive(Debug, Serialize, Deserialize, Clone, lark_bot_sdk_macros::ApiBaseResp)]
struct GetDocxDocumentRawContentRespInner {
    #[serde(flatten)]
    data: Option<GetDocxDocumentRawContentResp>,
    #[serde(flatten)]
    base: BaseResp,
}

#[derive(Debug, Serialize, Deserialize, Clone, Default)]
pub struct GetDocxDocumentRawContentResp {
    /// \-
    #[serde(
        rename = "data",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub data: DataSubResp,
}

#[derive(Debug, Serialize, Deserialize, Clone, Default)]
#[serde(default)]
pub struct DataSubResp {
    /// 文档纯文本
    ///
    /// **示例值**: "云文档\n多人实时协同，插入一切元素。不仅是在线文档，更是强大的创作和互动工具\n云文档：专为协作而生\n"
    #[serde(
        rename = "content",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub content: String,
}

#[cfg(feature = "test-util")]
mod test_utils {
    use std::sync::Arc;

    use self::gen::drive::DriveServiceMocker;
    use crate::core::mocker::*;

    use super::*;
    use crate::api::*;
    use crate::core::model::*;

    pub struct Mocker;

    pub trait MockFunc:
        Fn(
            GetDocxDocumentRawContentReq,
        ) -> Result<(GetDocxDocumentRawContentResp, CommonResponse), Error>
        + Send
        + Sync
        + 'static
    {
    }
    impl<
            T: Fn(
                    GetDocxDocumentRawContentReq,
                )
                    -> Result<(GetDocxDocumentRawContentResp, CommonResponse), Error>
                + Send
                + Sync
                + 'static,
        > MockFunc for T
    {
    }

    impl<'c, IStore: Store, IClient: HttpClient> DriveServiceMocker<'c, IStore, IClient> {
        pub fn mock_get_docx_document_raw_content<F: MockFunc>(
            &self,
            f: F,
        ) -> MockerBuilder<
            Mocker,
            GetDocxDocumentRawContentReq,
            GetDocxDocumentRawContentResp,
            Arc<dyn MockFunc>,
        > {
            MockerBuilder::new(self.cli.instance_id, Arc::new(f))
        }

        pub(super) fn get_mock_get_docx_document_raw_content(
            &self,
            req: &GetDocxDocumentRawContentReq,
        ) -> Option<Arc<dyn MockFunc>> {
            do_mock::<
                Mocker,
                GetDocxDocumentRawContentReq,
                GetDocxDocumentRawContentResp,
                Arc<dyn MockFunc>,
            >(self.cli.instance_id, req)
        }
    }
}

#[cfg(feature = "test-util")]
#[cfg(test)]
mod test {
    use crate::{
        api::gen::drive::get_docx_document_raw_content::{
            GetDocxDocumentRawContentReq, GetDocxDocumentRawContentResp,
            GetDocxDocumentRawContentRespInner,
        },
        core::{model::CommonResponse, Lark},
    };

    #[tokio::test]
    async fn test_mock() {
        let lark = Lark::new("".to_owned(), "".into());
        let mocker = lark
            .drive()
            .mock()
            .mock_get_docx_document_raw_content(|_| {
                Ok((
                    GetDocxDocumentRawContentResp::default(),
                    CommonResponse::default(),
                ))
            })
            .build();
        let res = lark
            .drive()
            .get_docx_document_raw_content(GetDocxDocumentRawContentReq::default())
            .await;
        assert!(res.is_ok());
        mocker.clear();
        let res = lark
            .drive()
            .get_docx_document_raw_content(GetDocxDocumentRawContentReq::default())
            .await;
        assert!(res.is_err());
    }

    const REQ: &str = "{}";

    #[test]
    fn test_req() {
        if REQ == "{}" {
            return;
        }
        if let Err(e) = serde_json::from_str::<()>(REQ) {
            panic!("{}", e);
        }
    }

    const RESP: &str = r#"{
    "code": 0,
    "msg": "success",
    "data": {
        "content": "云文档\n多人实时协同，插入一切元素。不仅是在线文档，更是强大的创作和互动工具\n云文档：专为协作而生\n"
    }
}"#;
    #[test]
    fn test_resp() {
        let res = serde_json::from_str::<GetDocxDocumentRawContentRespInner>(RESP);
        if let Err(e) = res {
            panic!("{}", e);
        }
        if let Ok(v) = serde_json::from_str::<serde_json::Value>(RESP) {
            if v.get("data").is_some() {
                assert!(res.unwrap().data.is_some());
            }
        }
    }
}
