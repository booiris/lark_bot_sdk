//! doc url: <https://open.feishu.cn/document/uAjLw4CM/ukTMukTMukTM/reference/drive-v1/media/upload_all>
// Code generated by gen_api. DO NOT EDIT.

use serde::{Deserialize, Serialize};

use crate::api::BaseResp;
use crate::api::HasBaseResp;
use crate::core::model::{
    ApiRequest, CommonResponse, MethodOption, ReqParam, StreamReqData, StreamReqParam,
};
use crate::{
    core::{http_client::HttpClient, store::Store},
    error::Error,
};

use crate::api::gen::drive::DriveService;

impl<'c, IStore: Store, IClient: HttpClient> DriveService<'c, IStore, IClient> {
    /// **api 版本: 2024-06-06T13:01:50+00:00**
    ///
    /// ## 上传素材
    ///
    /// 将文件、图片、视频等素材上传到指定云文档中。素材将显示在对应云文档中，在云空间中不会显示。
    ///

    ///
    /// doc: <https://open.feishu.cn/document/uAjLw4CM/ukTMukTMukTM/reference/drive-v1/media/upload_all>
    ///
    /// new doc: <https://open.feishu.cn/document/server-docs/docs/drive-v1/media/upload_all>
    ///
    /// api url: <https://open.feishu.cn/document_portal/v1/document/get_detail?fullPath=%2Fserver-docs%2Fdocs%2Fdrive-v1%2Fmedia%2Fupload_all>
    pub async fn upload_drive_media<Data: StreamReqData>(
        &self,
        req: UploadDriveMediaReq<Data>,
    ) -> Result<(UploadDriveMediaResp, CommonResponse), Error> {
        self.upload_drive_media_with_opt(req, Default::default())
            .await
    }

    /// 参见 [upload_drive_media](#method.upload_drive_media) 函数
    pub async fn upload_drive_media_with_opt<Data: StreamReqData>(
        &self,
        req: UploadDriveMediaReq<Data>,
        method_option: MethodOption,
    ) -> Result<(UploadDriveMediaResp, CommonResponse), Error> {
        #[cfg(feature = "test-util")]
        {
            if let Some(f) = self.mock().get_mock_upload_drive_media(&req) {
                tracing::info!("[lark] Drive#UploadDriveMedia **mocking** api");
                return f(req);
            }
        }

        tracing::info!("[lark] Drive#UploadDriveMedia call api");

        let req = ApiRequest::<()> {
            scope: "Drive",
            api: "UploadDriveMedia",
            method: http::Method::POST,
            url: String::new()
                + self.cli.open_base_url.as_ref()
                + "/open-apis/drive/v1/medias/upload_all",
            stream_param_data: req.gen_stream_param(),
            method_option,
            need_tenant_access_token: true,
            need_user_access_token: true,
            ..Default::default()
        };

        let (resp, common_resp): (UploadDriveMediaRespInner, _) = self.cli.do_req(req).await?;
        let data = match resp.data {
            Some(data) => data,
            None => {
                return Err(Error::ErrResponse(
                    anyhow::anyhow!("missing response data"),
                    common_resp,
                ));
            }
        };
        Ok((data, common_resp))
    }
}

#[derive(Debug, lark_bot_sdk_macros::ApiReqParams)]
pub struct UploadDriveMediaReq<Data: StreamReqData> {
    /// 要上传的素材的名称
    ///
    /// **是否必填**: 是
    ///
    /// **示例值**: "demo.jpeg"
    ///
    /// **数据校验规则**：
    ///
    /// - **最大长度**: `250` 字符
    #[api(kind = "stream", name = "file_name", option = "false")]
    pub file_name: String,
    /// 上传点的类型。你可根据上传的素材类型与云文档类型确定上传点类型。例如，要将一张图片插入到新版文档（文件类型为 `docx`）中，需指定上传点为 `docx_image `；要将一个附件上传到新版文档中，需指定上传点为 `docx_file`。
    ///
    /// **是否必填**: 是
    ///
    /// **示例值**: "docx_image"
    ///
    /// **可选值**:
    ///
    /// `doc_image`: 旧版文档图片
    ///
    /// `docx_image`: 新版文档图片
    ///
    /// `sheet_image`: 电子表格图片
    ///
    /// `doc_file`: 旧版文档文件
    ///
    /// `docx_file`: 新版文档文件
    ///
    /// `sheet_file`: 电子表格文件
    ///
    /// `vc_virtual_background`: vc 虚拟背景（灰度中，暂未开放）
    ///
    /// `bitable_image`: 多维表格图片
    ///
    /// `bitable_file`: 多维表格文件
    ///
    /// `moments`: 同事圈（灰度中，暂未开放）
    ///
    /// `ccm_import_open`: 云文档导入文件
    #[api(kind = "stream", name = "parent_type", option = "false")]
    pub parent_type: String,
    /// 上传点的 token，即要上传的云文档的 token，用于指定素材将要上传到的云文档或位置。参考[素材概述](https://open.feishu.cn/document/uAjLw4CM/ukTMukTMukTM/reference/drive-v1/media/introduction)了解上传点类型与上传点 token 的对应关系
    ///
    /// **是否必填**: 是
    #[api(kind = "stream", name = "parent_node", option = "false")]
    pub parent_node: String,
    /// 文件的大小，单位为字节
    ///
    /// **是否必填**: 是
    ///
    /// **示例值**: "1024"
    #[api(kind = "stream", name = "size", option = "false")]
    pub size: i64,
    /// 文件的 Adler-32 校验和
    ///
    /// **示例值**: "3248270248"
    #[api(kind = "stream", name = "checksum", option = "true")]
    pub checksum: Option<String>,
    /// 其它扩展信息。详情参考[素材概述](https://open.feishu.cn/document/uAjLw4CM/ukTMukTMukTM/reference/drive-v1/media/introduction)。如果需要跨域路由，需要填写路由的云文档 token，格式为 `{"drive_route_token":"需要路由的云文档的 token"}`
    ///
    /// **示例值**: "{"test":"test"}"
    #[api(kind = "stream", name = "extra", option = "true")]
    pub extra: Option<String>,
    #[api(kind = "stream", name = "#data#", option = "false")]
    pub data: Data,
}

#[derive(Debug, Serialize, Deserialize, Clone, lark_bot_sdk_macros::ApiBaseResp)]
struct UploadDriveMediaRespInner {
    #[serde(flatten)]
    data: Option<UploadDriveMediaResp>,
    #[serde(flatten)]
    base: BaseResp,
}

#[derive(Debug, Serialize, Deserialize, Clone, Default)]
pub struct UploadDriveMediaResp {
    /// \-
    #[serde(
        rename = "data",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub data: DataSubResp,
}

#[derive(Debug, Serialize, Deserialize, Clone, Default)]
#[serde(default)]
pub struct DataSubResp {
    /// 素材文件的 token。
    ///
    /// **示例值**: "boxcnrHpsg1QDqXAAAyachabcef"
    #[serde(
        rename = "file_token",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub file_token: String,
}

#[cfg(feature = "test-util")]
mod test_utils {
    use std::sync::Arc;

    use self::gen::drive::DriveServiceMocker;
    use crate::core::mocker::*;

    use super::*;
    use crate::api::*;
    use crate::core::model::*;

    pub struct Mocker;

    pub trait MockFunc<D: StreamReqData>:
        Fn(UploadDriveMediaReq<D>) -> Result<(UploadDriveMediaResp, CommonResponse), Error>
        + Send
        + Sync
        + 'static
    {
    }
    impl<
            D: StreamReqData,
            T: Fn(UploadDriveMediaReq<D>) -> Result<(UploadDriveMediaResp, CommonResponse), Error>
                + Send
                + Sync
                + 'static,
        > MockFunc<D> for T
    {
    }

    impl<'c, IStore: Store, IClient: HttpClient> DriveServiceMocker<'c, IStore, IClient> {
        pub fn mock_upload_drive_media<T: StreamReqData, F: MockFunc<T>>(
            &self,
            f: F,
        ) -> MockerBuilder<Mocker, UploadDriveMediaReq<T>, UploadDriveMediaResp, Arc<dyn MockFunc<T>>>
        {
            MockerBuilder::new(self.cli.instance_id, Arc::new(f))
        }

        pub(super) fn get_mock_upload_drive_media<T: StreamReqData>(
            &self,
            req: &UploadDriveMediaReq<T>,
        ) -> Option<Arc<dyn MockFunc<T>>> {
            do_mock::<Mocker, UploadDriveMediaReq<T>, UploadDriveMediaResp, Arc<dyn MockFunc<T>>>(
                self.cli.instance_id,
                req,
            )
        }
    }
}
