//! doc url: <https://open.larkoffice.com/document/ukTMukTMukTM/uMzM1YjLzMTN24yMzUjN/hire-v1/eco_background_check/cancel>
// Code generated by gen_api. DO NOT EDIT.

use serde::{Deserialize, Serialize};

use crate::api::BaseResp;
use crate::api::HasBaseResp;
use crate::core::model::{ApiRequest, CommonResponse, MethodOption, ReqParam, StreamReqParam};
use crate::{
    core::{http_client::HttpClient, store::Store},
    error::Error,
};

use crate::api::gen::hire::HireService;

impl<'c, IStore: Store, IClient: HttpClient> HireService<'c, IStore, IClient> {
    /// **api 版本: 2024-07-30T08:24:26+00:00**
    ///
    /// ## 终止背调订单
    ///
    /// 调用此接口将会将背调订单状态变成已终止，已终止订单将将无法通过[更新背调订单进度](https://open.feishu.cn/document/ukTMukTMukTM/uMzM1YjLzMTN24yMzUjN/hire-v1/eco_background_check/update_progress)和[回传背调订单的最终结果](https://open.feishu.cn/document/ukTMukTMukTM/uMzM1YjLzMTN24yMzUjN/hire-v1/eco_background_check/update_result)修改订单进度和最终结果。 建议在回调终止背调订单之前，推送订单进度为「已终止」。
    ///

    ///
    /// doc: <https://open.larkoffice.com/document/ukTMukTMukTM/uMzM1YjLzMTN24yMzUjN/hire-v1/eco_background_check/cancel>
    ///
    /// new doc: <https://open.larkoffice.com/document/server-docs/hire-v1/ecological-docking/eco_background_check/cancel>
    ///
    /// api url: <https://open.feishu.cn/document_portal/v1/document/get_detail?fullPath=%2Fserver-docs%2Fhire-v1%2Fecological-docking%2Feco_background_check%2Fcancel>
    pub async fn cancel_hire_eco_background_check(
        &self,
        req: CancelHireEcoBackgroundCheckReq,
    ) -> Result<(CancelHireEcoBackgroundCheckResp, CommonResponse), Error> {
        self.cancel_hire_eco_background_check_with_opt(req, Default::default())
            .await
    }

    /// 参见 [cancel_hire_eco_background_check](#method.cancel_hire_eco_background_check) 函数
    pub async fn cancel_hire_eco_background_check_with_opt(
        &self,
        req: CancelHireEcoBackgroundCheckReq,
        method_option: MethodOption,
    ) -> Result<(CancelHireEcoBackgroundCheckResp, CommonResponse), Error> {
        #[cfg(feature = "test-util")]
        {
            if let Some(f) = self.mock().get_mock_cancel_hire_eco_background_check(&req) {
                tracing::info!("[lark] Hire#CancelHireEcoBackgroundCheck **mocking** api");
                return f(req);
            }
        }

        tracing::info!("[lark] Hire#CancelHireEcoBackgroundCheck call api");

        let req = ApiRequest {
            scope: "Hire",
            api: "CancelHireEcoBackgroundCheck",
            method: http::Method::POST,
            url: String::new()
                + self.cli.open_base_url.as_ref()
                + "/open-apis/hire/v1/eco_background_checks/cancel",
            param_data: req.gen_param(),
            method_option,
            need_tenant_access_token: true,
            ..Default::default()
        };

        let (resp, common_resp): (CancelHireEcoBackgroundCheckRespInner, _) =
            self.cli.do_req(req).await?;
        let data = match resp.data {
            Some(data) => data,
            None => {
                return Err(Error::ErrResponse(
                    anyhow::anyhow!("missing response data"),
                    common_resp,
                ));
            }
        };
        Ok((data, common_resp))
    }
}

#[derive(Debug, Serialize, Deserialize, Clone, Default, lark_bot_sdk_macros::ApiReqParams)]
pub struct CancelHireEcoBackgroundCheckReq {
    /// 背调 ID。可通过[获取背调信息列表](https://open.feishu.cn/document/ukTMukTMukTM/uMzM1YjLzMTN24yMzUjN/hire-v1/background_check_order/list)获取
    ///
    /// **是否必填**: 是
    ///
    /// **示例值**: "6931286400470354183"
    #[api(kind = "body", name = "background_check_id")]
    pub background_check_id: String,
}

#[derive(Debug, Serialize, Deserialize, Clone, lark_bot_sdk_macros::ApiBaseResp)]
struct CancelHireEcoBackgroundCheckRespInner {
    #[serde(flatten)]
    data: Option<CancelHireEcoBackgroundCheckResp>,
    #[serde(flatten)]
    base: BaseResp,
}

#[derive(Debug, Serialize, Deserialize, Clone, Default)]
pub struct CancelHireEcoBackgroundCheckResp {
    /// \-
    #[serde(
        rename = "data",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub data: (),
}

#[cfg(feature = "test-util")]
mod test_utils {
    use std::sync::Arc;

    use self::gen::hire::HireServiceMocker;
    use crate::core::mocker::*;

    use super::*;
    use crate::api::*;
    use crate::core::model::*;

    pub struct Mocker;

    pub trait MockFunc:
        Fn(
            CancelHireEcoBackgroundCheckReq,
        ) -> Result<(CancelHireEcoBackgroundCheckResp, CommonResponse), Error>
        + Send
        + Sync
        + 'static
    {
    }
    impl<
            T: Fn(
                    CancelHireEcoBackgroundCheckReq,
                )
                    -> Result<(CancelHireEcoBackgroundCheckResp, CommonResponse), Error>
                + Send
                + Sync
                + 'static,
        > MockFunc for T
    {
    }

    impl<'c, IStore: Store, IClient: HttpClient> HireServiceMocker<'c, IStore, IClient> {
        pub fn mock_cancel_hire_eco_background_check<F: MockFunc>(
            &self,
            f: F,
        ) -> MockerBuilder<
            Mocker,
            CancelHireEcoBackgroundCheckReq,
            CancelHireEcoBackgroundCheckResp,
            Arc<dyn MockFunc>,
        > {
            MockerBuilder::new(self.cli.instance_id, Arc::new(f))
        }

        pub(super) fn get_mock_cancel_hire_eco_background_check(
            &self,
            req: &CancelHireEcoBackgroundCheckReq,
        ) -> Option<Arc<dyn MockFunc>> {
            do_mock::<
                Mocker,
                CancelHireEcoBackgroundCheckReq,
                CancelHireEcoBackgroundCheckResp,
                Arc<dyn MockFunc>,
            >(self.cli.instance_id, req)
        }
    }
}

#[cfg(feature = "test-util")]
#[cfg(test)]
mod test {
    use crate::{
        api::gen::hire::cancel_hire_eco_background_check::{
            CancelHireEcoBackgroundCheckReq, CancelHireEcoBackgroundCheckResp,
            CancelHireEcoBackgroundCheckRespInner,
        },
        core::{model::CommonResponse, Lark},
    };

    #[tokio::test]
    async fn test_mock() {
        let lark = Lark::new("".to_owned(), "".into());
        let mocker = lark
            .hire()
            .mock()
            .mock_cancel_hire_eco_background_check(|_| {
                Ok((
                    CancelHireEcoBackgroundCheckResp::default(),
                    CommonResponse::default(),
                ))
            })
            .build();
        let res = lark
            .hire()
            .cancel_hire_eco_background_check(CancelHireEcoBackgroundCheckReq::default())
            .await;
        assert!(res.is_ok());
        mocker.clear();
        let res = lark
            .hire()
            .cancel_hire_eco_background_check(CancelHireEcoBackgroundCheckReq::default())
            .await;
        assert!(res.is_err());
    }

    const REQ: &str = r#"{
    "background_check_id": "6931286400470354183"
}"#;

    #[test]
    fn test_req() {
        if REQ == "{}" {
            return;
        }
        if let Err(e) = serde_json::from_str::<super::CancelHireEcoBackgroundCheckReqBody>(REQ) {
            panic!("{}", e);
        }
    }

    const RESP: &str = r#"{
    "code": 0,
    "msg": "success",
    "data": {}
}"#;
    #[test]
    fn test_resp() {
        let res = serde_json::from_str::<CancelHireEcoBackgroundCheckRespInner>(RESP);
        if let Err(e) = res {
            panic!("{}", e);
        }
        if let Ok(v) = serde_json::from_str::<serde_json::Value>(RESP) {
            if v.get("data").is_some() {
                assert!(res.unwrap().data.is_some());
            }
        }
    }
}
