//! doc url: <https://open.larkoffice.com/document/uAjLw4CM/ukTMukTMukTM/reference/contact-v3/unit/create>
// Code generated by gen_api. DO NOT EDIT.

use serde::{Deserialize, Serialize};

use crate::api::BaseResp;
use crate::api::HasBaseResp;
use crate::core::model::{ApiRequest, CommonResponse, MethodOption, ReqParam, StreamReqParam};
use crate::{
    core::{http_client::HttpClient, store::Store},
    error::Error,
};

use crate::api::gen::contact::ContactService;

impl<'c, IStore: Store, IClient: HttpClient> ContactService<'c, IStore, IClient> {
    /// **api 版本: 2024-07-05T08:16:17+00:00**
    ///
    /// ## 创建单位
    ///
    /// 调用该接口创建一个单位。
    ///

    ///
    /// doc: <https://open.larkoffice.com/document/uAjLw4CM/ukTMukTMukTM/reference/contact-v3/unit/create>
    ///
    /// new doc: <https://open.larkoffice.com/document/server-docs/contact-v3/unit/create>
    ///
    /// api url: <https://open.feishu.cn/document_portal/v1/document/get_detail?fullPath=%2Fserver-docs%2Fcontact-v3%2Funit%2Fcreate>
    pub async fn create_contact_unit(
        &self,
        req: CreateContactUnitReq,
    ) -> Result<(CreateContactUnitResp, CommonResponse), Error> {
        self.create_contact_unit_with_opt(req, Default::default())
            .await
    }

    /// 参见 [create_contact_unit](#method.create_contact_unit) 函数
    pub async fn create_contact_unit_with_opt(
        &self,
        req: CreateContactUnitReq,
        method_option: MethodOption,
    ) -> Result<(CreateContactUnitResp, CommonResponse), Error> {
        #[cfg(feature = "test-util")]
        {
            if let Some(f) = self.mock().get_mock_create_contact_unit(&req) {
                tracing::info!("[lark] Contact#CreateContactUnit **mocking** api");
                return f(req);
            }
        }

        tracing::info!("[lark] Contact#CreateContactUnit call api");

        let req = ApiRequest {
            scope: "Contact",
            api: "CreateContactUnit",
            method: http::Method::POST,
            url: String::new() + self.cli.open_base_url.as_ref() + "/open-apis/contact/v3/unit",
            param_data: req.gen_param(),
            method_option,
            need_tenant_access_token: true,
            ..Default::default()
        };

        let (resp, common_resp): (CreateContactUnitRespInner, _) = self.cli.do_req(req).await?;
        let data = match resp.data {
            Some(data) => data,
            None => {
                return Err(Error::ErrResponse(
                    anyhow::anyhow!("missing response data"),
                    common_resp,
                ));
            }
        };
        Ok((data, common_resp))
    }
}

#[derive(Debug, Serialize, Deserialize, Clone, Default, lark_bot_sdk_macros::ApiReqParams)]
pub struct CreateContactUnitReq {
    /// 自定义单位 ID，租户内唯一，创建后不可修改。
    ///
    /// **数据校验规则：** 1 ~ 64 个字符，仅支持字母、数字。
    ///
    /// **默认值**：空，若不传值则由系统自动生成一个默认 ID。
    ///
    /// **示例值**: "BU121"
    #[api(kind = "body", name = "unit_id")]
    pub unit_id: Option<String>,
    /// 单位名字。
    ///
    /// **数据校验规则：** 1 ~ 100 个字符。
    ///
    /// **注意**：在租户内，传入的 name 和 unit_type 不允许同时重复。例如，已存在一个名字 `A`、类型 `A`的单位，此时再创建一个名字 `A`、类型 `A` 的单位将会创建失败。
    ///
    /// **是否必填**: 是
    ///
    /// **示例值**: "消费者事业部"
    #[api(kind = "body", name = "name")]
    pub name: String,
    /// 自定义单位类型，创建后不可修改。
    ///
    /// **数据校验规则：** 1 ~ 100 个字符。
    ///
    /// **注意**：在租户内，传入的 name 和 unit_type 不允许同时重复。例如，已存在一个名字 `A`、类型 `A`的单位，此时再创建一个名字 `A`、类型 `A` 的单位将会创建失败。
    ///
    /// **是否必填**: 是
    ///
    /// **示例值**: "子公司"
    #[api(kind = "body", name = "unit_type")]
    pub unit_type: String,
}

#[derive(Debug, Serialize, Deserialize, Clone, lark_bot_sdk_macros::ApiBaseResp)]
struct CreateContactUnitRespInner {
    #[serde(flatten)]
    data: Option<CreateContactUnitResp>,
    #[serde(flatten)]
    base: BaseResp,
}

#[derive(Debug, Serialize, Deserialize, Clone, Default)]
pub struct CreateContactUnitResp {
    /// \-
    #[serde(
        rename = "data",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub data: DataSubResp,
}

#[derive(Debug, Serialize, Deserialize, Clone, Default)]
#[serde(default)]
pub struct DataSubResp {
    /// 单位 ID。后续可使用该 ID  对单位进行修改、删除、查询以及绑定部门等操作。
    ///
    /// **是否必填**: 是
    ///
    /// **示例值**: "BU121"
    #[serde(
        rename = "unit_id",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub unit_id: String,
}

#[cfg(feature = "test-util")]
mod test_utils {
    use std::sync::Arc;

    use self::gen::contact::ContactServiceMocker;
    use crate::core::mocker::*;

    use super::*;
    use crate::api::*;
    use crate::core::model::*;

    pub struct Mocker;

    pub trait MockFunc:
        Fn(CreateContactUnitReq) -> Result<(CreateContactUnitResp, CommonResponse), Error>
        + Send
        + Sync
        + 'static
    {
    }
    impl<
            T: Fn(CreateContactUnitReq) -> Result<(CreateContactUnitResp, CommonResponse), Error>
                + Send
                + Sync
                + 'static,
        > MockFunc for T
    {
    }

    impl<'c, IStore: Store, IClient: HttpClient> ContactServiceMocker<'c, IStore, IClient> {
        pub fn mock_create_contact_unit<F: MockFunc>(
            &self,
            f: F,
        ) -> MockerBuilder<Mocker, CreateContactUnitReq, CreateContactUnitResp, Arc<dyn MockFunc>>
        {
            MockerBuilder::new(self.cli.instance_id, Arc::new(f))
        }

        pub(super) fn get_mock_create_contact_unit(
            &self,
            req: &CreateContactUnitReq,
        ) -> Option<Arc<dyn MockFunc>> {
            do_mock::<Mocker, CreateContactUnitReq, CreateContactUnitResp, Arc<dyn MockFunc>>(
                self.cli.instance_id,
                req,
            )
        }
    }
}

#[cfg(feature = "test-util")]
#[cfg(test)]
mod test {
    use crate::{
        api::gen::contact::create_contact_unit::{
            CreateContactUnitReq, CreateContactUnitResp, CreateContactUnitRespInner,
        },
        core::{model::CommonResponse, Lark},
    };

    #[tokio::test]
    async fn test_mock() {
        let lark = Lark::new("".to_owned(), "".into());
        let mocker = lark
            .contact()
            .mock()
            .mock_create_contact_unit(|_| {
                Ok((CreateContactUnitResp::default(), CommonResponse::default()))
            })
            .build();
        let res = lark
            .contact()
            .create_contact_unit(CreateContactUnitReq::default())
            .await;
        assert!(res.is_ok());
        mocker.clear();
        let res = lark
            .contact()
            .create_contact_unit(CreateContactUnitReq::default())
            .await;
        assert!(res.is_err());
    }

    const REQ: &str = r#"{
    "unit_id": "BU121",
    "name": "消费者事业部",
    "unit_type": "子公司"
}"#;

    #[test]
    fn test_req() {
        if REQ == "{}" {
            return;
        }
        if let Err(e) = serde_json::from_str::<super::CreateContactUnitReqBody>(REQ) {
            panic!("{}", e);
        }
    }

    const RESP: &str = r#"{
    "code": 0,
    "msg": "success",
    "data": {
        "unit_id": "BU121"
    }
}"#;
    #[test]
    fn test_resp() {
        let res = serde_json::from_str::<CreateContactUnitRespInner>(RESP);
        if let Err(e) = res {
            panic!("{}", e);
        }
        if let Ok(v) = serde_json::from_str::<serde_json::Value>(RESP) {
            if v.get("data").is_some() {
                assert!(res.unwrap().data.is_some());
            }
        }
    }
}
