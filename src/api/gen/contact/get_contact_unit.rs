//! doc url: <https://open.larkoffice.com/document/uAjLw4CM/ukTMukTMukTM/reference/contact-v3/unit/get>
// Code generated by gen_api. DO NOT EDIT.

use serde::{Deserialize, Serialize};

use crate::api::BaseResp;
use crate::api::HasBaseResp;
use crate::core::model::{ApiRequest, CommonResponse, MethodOption, ReqParam, StreamReqParam};
use crate::{
    core::{http_client::HttpClient, store::Store},
    error::Error,
};

use crate::api::gen::contact::ContactService;

impl<'c, IStore: Store, IClient: HttpClient> ContactService<'c, IStore, IClient> {
    /// **api 版本: 2024-07-05T08:16:18+00:00**
    ///
    /// ## 获取单位信息
    ///
    /// 调用该接口获取指定单位的信息，包括单位 ID、名字、类型。
    ///

    ///
    /// doc: <https://open.larkoffice.com/document/uAjLw4CM/ukTMukTMukTM/reference/contact-v3/unit/get>
    ///
    /// new doc: <https://open.larkoffice.com/document/server-docs/contact-v3/unit/get>
    ///
    /// api url: <https://open.feishu.cn/document_portal/v1/document/get_detail?fullPath=%2Fserver-docs%2Fcontact-v3%2Funit%2Fget>
    pub async fn get_contact_unit(
        &self,
        req: GetContactUnitReq,
    ) -> Result<(GetContactUnitResp, CommonResponse), Error> {
        self.get_contact_unit_with_opt(req, Default::default())
            .await
    }

    /// 参见 [get_contact_unit](#method.get_contact_unit) 函数
    pub async fn get_contact_unit_with_opt(
        &self,
        req: GetContactUnitReq,
        method_option: MethodOption,
    ) -> Result<(GetContactUnitResp, CommonResponse), Error> {
        #[cfg(feature = "test-util")]
        {
            if let Some(f) = self.mock().get_mock_get_contact_unit(&req) {
                tracing::info!("[lark] Contact#GetContactUnit **mocking** api");
                return f(req);
            }
        }

        tracing::info!("[lark] Contact#GetContactUnit call api");

        let req = ApiRequest {
            scope: "Contact",
            api: "GetContactUnit",
            method: http::Method::GET,
            url: String::new()
                + self.cli.open_base_url.as_ref()
                + "/open-apis/contact/v3/unit/:unit_id",
            param_data: req.gen_param(),
            method_option,
            need_tenant_access_token: true,
            ..Default::default()
        };

        let (resp, common_resp): (GetContactUnitRespInner, _) = self.cli.do_req(req).await?;
        let data = match resp.data {
            Some(data) => data,
            None => {
                return Err(Error::ErrResponse(
                    anyhow::anyhow!("missing response data"),
                    common_resp,
                ));
            }
        };
        Ok((data, common_resp))
    }
}

#[derive(Debug, Serialize, Deserialize, Clone, Default, lark_bot_sdk_macros::ApiReqParams)]
pub struct GetContactUnitReq {
    /// 单位 ID。
    ///
    /// 当你在创建单位时，可以在返回结果中获取单位 ID。你也可以调用[获取单位列表](https://open.feishu.cn/document/uAjLw4CM/ukTMukTMukTM/reference/contact-v3/unit/list)接口，获取单位 ID。
    ///
    /// **是否必填**: 是
    ///
    /// **示例值**: "BU121"
    #[api(kind = "path", name = "unit_id")]
    pub unit_id: String,
}

#[derive(Debug, Serialize, Deserialize, Clone, lark_bot_sdk_macros::ApiBaseResp)]
struct GetContactUnitRespInner {
    #[serde(flatten)]
    data: Option<GetContactUnitResp>,
    #[serde(flatten)]
    base: BaseResp,
}

#[derive(Debug, Serialize, Deserialize, Clone, Default)]
pub struct GetContactUnitResp {
    /// \-
    #[serde(
        rename = "data",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub data: DataSubResp,
}

#[derive(Debug, Serialize, Deserialize, Clone, Default)]
#[serde(default)]
pub struct DataSubResp {
    /// 单位信息。
    ///
    /// **是否必填**: 是
    #[serde(
        rename = "unit",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub unit: UnitSubResp,
}

#[derive(Debug, Serialize, Deserialize, Clone, Default)]
#[serde(default)]
pub struct UnitSubResp {
    /// 单位 ID。
    ///
    /// **是否必填**: 是
    ///
    /// **示例值**: "BU121"
    #[serde(
        rename = "unit_id",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub unit_id: String,
    /// 单位名字。
    ///
    /// **是否必填**: 是
    ///
    /// **示例值**: "消费者事业部"
    #[serde(
        rename = "name",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub name: String,
    /// 单位类型。
    ///
    /// **是否必填**: 是
    ///
    /// **示例值**: "事业部"
    #[serde(
        rename = "unit_type",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub unit_type: String,
}

#[cfg(feature = "test-util")]
mod test_utils {
    use std::sync::Arc;

    use self::gen::contact::ContactServiceMocker;
    use crate::core::mocker::*;

    use super::*;
    use crate::api::*;
    use crate::core::model::*;

    pub struct Mocker;

    pub trait MockFunc:
        Fn(GetContactUnitReq) -> Result<(GetContactUnitResp, CommonResponse), Error>
        + Send
        + Sync
        + 'static
    {
    }
    impl<
            T: Fn(GetContactUnitReq) -> Result<(GetContactUnitResp, CommonResponse), Error>
                + Send
                + Sync
                + 'static,
        > MockFunc for T
    {
    }

    impl<'c, IStore: Store, IClient: HttpClient> ContactServiceMocker<'c, IStore, IClient> {
        pub fn mock_get_contact_unit<F: MockFunc>(
            &self,
            f: F,
        ) -> MockerBuilder<Mocker, GetContactUnitReq, GetContactUnitResp, Arc<dyn MockFunc>>
        {
            MockerBuilder::new(self.cli.instance_id, Arc::new(f))
        }

        pub(super) fn get_mock_get_contact_unit(
            &self,
            req: &GetContactUnitReq,
        ) -> Option<Arc<dyn MockFunc>> {
            do_mock::<Mocker, GetContactUnitReq, GetContactUnitResp, Arc<dyn MockFunc>>(
                self.cli.instance_id,
                req,
            )
        }
    }
}

#[cfg(feature = "test-util")]
#[cfg(test)]
mod test {
    use crate::{
        api::gen::contact::get_contact_unit::{
            GetContactUnitReq, GetContactUnitResp, GetContactUnitRespInner,
        },
        core::{model::CommonResponse, Lark},
    };

    #[tokio::test]
    async fn test_mock() {
        let lark = Lark::new("".to_owned(), "".into());
        let mocker = lark
            .contact()
            .mock()
            .mock_get_contact_unit(|_| {
                Ok((GetContactUnitResp::default(), CommonResponse::default()))
            })
            .build();
        let res = lark
            .contact()
            .get_contact_unit(GetContactUnitReq::default())
            .await;
        assert!(res.is_ok());
        mocker.clear();
        let res = lark
            .contact()
            .get_contact_unit(GetContactUnitReq::default())
            .await;
        assert!(res.is_err());
    }

    const REQ: &str = "{}";

    #[test]
    fn test_req() {
        if REQ == "{}" {
            return;
        }
        if let Err(e) = serde_json::from_str::<()>(REQ) {
            panic!("{}", e);
        }
    }

    const RESP: &str = r#"{
    "code": 0,
    "msg": "success",
    "data": {
        "unit": {
            "unit_id": "BU121",
            "name": "消费者事业部",
            "unit_type": "事业部"
        }
    }
}"#;
    #[test]
    fn test_resp() {
        let res = serde_json::from_str::<GetContactUnitRespInner>(RESP);
        if let Err(e) = res {
            panic!("{}", e);
        }
        if let Ok(v) = serde_json::from_str::<serde_json::Value>(RESP) {
            if v.get("data").is_some() {
                assert!(res.unwrap().data.is_some());
            }
        }
    }
}
