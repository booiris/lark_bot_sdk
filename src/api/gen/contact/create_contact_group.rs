//! doc url: <https://open.larkoffice.com/document/uAjLw4CM/ukTMukTMukTM/reference/contact-v3/group/create>
// Code generated by gen_api. DO NOT EDIT.

use serde::{Deserialize, Serialize};

use crate::api::BaseResp;
use crate::api::HasBaseResp;
use crate::core::model::{ApiRequest, CommonResponse, MethodOption, ReqParam, StreamReqParam};
use crate::{
    core::{http_client::HttpClient, store::Store},
    error::Error,
};

use crate::api::gen::contact::ContactService;

impl<'c, IStore: Store, IClient: HttpClient> ContactService<'c, IStore, IClient> {
    /// **api 版本: 2024-07-05T08:08:02+00:00**
    ///
    /// ## 创建用户组
    ///
    /// 调用该接口创建一个用户组。用户组是飞书通讯录中基础实体之一，在用户组内可添加用户或部门资源。各类业务权限管控可以与用户组关联，从而实现高效便捷的成员权限管控。
    ///

    ///
    /// doc: <https://open.larkoffice.com/document/uAjLw4CM/ukTMukTMukTM/reference/contact-v3/group/create>
    ///
    /// new doc: <https://open.larkoffice.com/document/server-docs/contact-v3/group/create>
    ///
    /// api url: <https://open.feishu.cn/document_portal/v1/document/get_detail?fullPath=%2Fserver-docs%2Fcontact-v3%2Fgroup%2Fcreate>
    pub async fn create_contact_group(
        &self,
        req: CreateContactGroupReq,
    ) -> Result<(CreateContactGroupResp, CommonResponse), Error> {
        self.create_contact_group_with_opt(req, Default::default())
            .await
    }

    /// 参见 [create_contact_group](#method.create_contact_group) 函数
    pub async fn create_contact_group_with_opt(
        &self,
        req: CreateContactGroupReq,
        method_option: MethodOption,
    ) -> Result<(CreateContactGroupResp, CommonResponse), Error> {
        #[cfg(feature = "test-util")]
        {
            if let Some(f) = self.mock().get_mock_create_contact_group(&req) {
                tracing::info!("[lark] Contact#CreateContactGroup **mocking** api");
                return f(req);
            }
        }

        tracing::info!("[lark] Contact#CreateContactGroup call api");

        let req = ApiRequest {
            scope: "Contact",
            api: "CreateContactGroup",
            method: http::Method::POST,
            url: String::new() + self.cli.open_base_url.as_ref() + "/open-apis/contact/v3/group",
            param_data: req.gen_param(),
            method_option,
            need_tenant_access_token: true,
            ..Default::default()
        };

        let (resp, common_resp): (CreateContactGroupRespInner, _) = self.cli.do_req(req).await?;
        let data = match resp.data {
            Some(data) => data,
            None => {
                return Err(Error::ErrResponse(
                    anyhow::anyhow!("missing response data"),
                    common_resp,
                ));
            }
        };
        Ok((data, common_resp))
    }
}

#[derive(Debug, Serialize, Deserialize, Clone, Default, lark_bot_sdk_macros::ApiReqParams)]
pub struct CreateContactGroupReq {
    /// 用户 ID 类型
    ///
    /// **示例值**: "open_id"
    ///
    /// **可选值**:
    ///
    /// `open_id`: 标识一个用户在某个应用中的身份。同一个用户在不同应用中的 Open ID 不同。[了解更多：如何获取 Open ID](https://open.feishu.cn/document/uAjLw4CM/ugTN1YjL4UTN24CO1UjN/trouble-shooting/how-to-obtain-openid)
    ///
    /// `union_id`: 标识一个用户在某个应用开发商下的身份。同一用户在同一开发商下的应用中的 Union ID 是相同的，在不同开发商下的应用中的 Union ID 是不同的。通过 Union ID，应用开发商可以把同个用户在多个应用中的身份关联起来。[了解更多：如何获取 Union ID？](https://open.feishu.cn/document/uAjLw4CM/ugTN1YjL4UTN24CO1UjN/trouble-shooting/how-to-obtain-union-id)
    ///
    /// `user_id`: 标识一个用户在某个租户内的身份。同一个用户在租户 A 和租户 B 内的 User ID 是不同的。在同一个租户内，一个用户的 User ID 在所有应用（包括商店应用）中都保持一致。User ID 主要用于在不同的应用间打通用户数据。[了解更多：如何获取 User ID？](https://open.feishu.cn/document/uAjLw4CM/ugTN1YjL4UTN24CO1UjN/trouble-shooting/how-to-obtain-user-id)
    #[api(
        kind = "query",
        name = "user_id_type",
        v_type = "var",
        option = "false"
    )]
    pub user_id_type: String,
    /// 此次调用中的部门 ID 类型。关于部门 ID 的详细介绍，可参见[部门 ID 说明](https://open.feishu.cn/document/uAjLw4CM/ukTMukTMukTM/reference/contact-v3/department/field-overview#23857fe0)。
    ///
    /// **示例值**: "open_department_id"
    ///
    /// **可选值**:
    ///
    /// `open_department_id`: 由系统自动生成的部门 ID，ID 前缀固定为 `od-`，在租户内全局唯一。
    ///
    /// `department_id`: 支持用户自定义配置的部门 ID。自定义配置时可复用已删除的 department_id，因此在未删除的部门范围内 department_id 具有唯一性。
    #[api(
        kind = "query",
        name = "department_id_type",
        v_type = "var",
        option = "false"
    )]
    pub department_id_type: String,
    /// 用户组名字，长度不能超过 100 字符。
    ///
    /// **说明**：用户组名称企业内唯一，如重复设置则会创建失败。
    ///
    /// **是否必填**: 是
    ///
    /// **示例值**: "IT 外包组"
    #[api(kind = "body", name = "name")]
    pub name: String,
    /// 用户组描述，长度不能超过 500 字符。
    ///
    /// **默认值**：空
    ///
    /// **示例值**: "IT服务人员的集合"
    #[api(kind = "body", name = "description")]
    pub description: Option<String>,
    /// 用户组的类型。默认取值 `1`，表示普通用户组。
    ///
    /// **示例值**: "1"
    ///
    /// **可选值**:
    ///
    /// `Assign`: 普通用户组
    ///
    /// `Dynamic`: 暂不支持使用该值
    #[api(kind = "body", name = "type")]
    pub body_type: Option<i64>,
    /// 自定义用户组 ID。
    ///
    /// **数据校验规则：**
    ///
    /// - 最大长度：64 字符
    ///
    /// - 校验规则：数字、大小写字母的组合，不能包含空格
    ///
    /// **默认值**：空，由系统生成一个默认 ID。
    ///
    /// **示例值**: "g122817"
    #[api(kind = "body", name = "group_id")]
    pub group_id: Option<String>,
}

#[derive(Debug, Serialize, Deserialize, Clone, lark_bot_sdk_macros::ApiBaseResp)]
struct CreateContactGroupRespInner {
    #[serde(flatten)]
    data: Option<CreateContactGroupResp>,
    #[serde(flatten)]
    base: BaseResp,
}

#[derive(Debug, Serialize, Deserialize, Clone, Default)]
pub struct CreateContactGroupResp {
    /// \-
    #[serde(
        rename = "data",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub data: DataSubResp,
}

#[derive(Debug, Serialize, Deserialize, Clone, Default)]
#[serde(default)]
pub struct DataSubResp {
    /// 用户组 ID。后续可使用该 ID 更新、删除、查询用户组。
    ///
    /// **是否必填**: 是
    ///
    /// **示例值**: "g122817"
    #[serde(
        rename = "group_id",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub group_id: String,
}

#[cfg(feature = "test-util")]
mod test_utils {
    use std::sync::Arc;

    use self::gen::contact::ContactServiceMocker;
    use crate::core::mocker::*;

    use super::*;
    use crate::api::*;
    use crate::core::model::*;

    pub struct Mocker;

    pub trait MockFunc:
        Fn(CreateContactGroupReq) -> Result<(CreateContactGroupResp, CommonResponse), Error>
        + Send
        + Sync
        + 'static
    {
    }
    impl<
            T: Fn(CreateContactGroupReq) -> Result<(CreateContactGroupResp, CommonResponse), Error>
                + Send
                + Sync
                + 'static,
        > MockFunc for T
    {
    }

    impl<'c, IStore: Store, IClient: HttpClient> ContactServiceMocker<'c, IStore, IClient> {
        pub fn mock_create_contact_group<F: MockFunc>(
            &self,
            f: F,
        ) -> MockerBuilder<Mocker, CreateContactGroupReq, CreateContactGroupResp, Arc<dyn MockFunc>>
        {
            MockerBuilder::new(self.cli.instance_id, Arc::new(f))
        }

        pub(super) fn get_mock_create_contact_group(
            &self,
            req: &CreateContactGroupReq,
        ) -> Option<Arc<dyn MockFunc>> {
            do_mock::<Mocker, CreateContactGroupReq, CreateContactGroupResp, Arc<dyn MockFunc>>(
                self.cli.instance_id,
                req,
            )
        }
    }
}

#[cfg(feature = "test-util")]
#[cfg(test)]
mod test {
    use crate::{
        api::gen::contact::create_contact_group::{
            CreateContactGroupReq, CreateContactGroupResp, CreateContactGroupRespInner,
        },
        core::{model::CommonResponse, Lark},
    };

    #[tokio::test]
    async fn test_mock() {
        let lark = Lark::new("".to_owned(), "".into());
        let mocker = lark
            .contact()
            .mock()
            .mock_create_contact_group(|_| {
                Ok((CreateContactGroupResp::default(), CommonResponse::default()))
            })
            .build();
        let res = lark
            .contact()
            .create_contact_group(CreateContactGroupReq::default())
            .await;
        assert!(res.is_ok());
        mocker.clear();
        let res = lark
            .contact()
            .create_contact_group(CreateContactGroupReq::default())
            .await;
        assert!(res.is_err());
    }

    const REQ: &str = r#"{
    "name": "IT 外包组",
    "description": "IT服务人员的集合",
    "type": 1,
    "group_id": "g122817"
}"#;

    #[test]
    fn test_req() {
        if REQ == "{}" {
            return;
        }
        if let Err(e) = serde_json::from_str::<super::CreateContactGroupReqBody>(REQ) {
            panic!("{}", e);
        }
    }

    const RESP: &str = r#"{
    "code": 0,
    "msg": "success",
    "data": {
        "group_id": "g122817"
    }
}"#;
    #[test]
    fn test_resp() {
        let res = serde_json::from_str::<CreateContactGroupRespInner>(RESP);
        if let Err(e) = res {
            panic!("{}", e);
        }
        if let Ok(v) = serde_json::from_str::<serde_json::Value>(RESP) {
            if v.get("data").is_some() {
                assert!(res.unwrap().data.is_some());
            }
        }
    }
}
