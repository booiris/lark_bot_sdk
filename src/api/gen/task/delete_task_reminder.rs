//! doc url: <https://open.larkoffice.com/document/uAjLw4CM/ukTMukTMukTM/reference/task-v1/task-reminder/delete>
// Code generated by gen_api. DO NOT EDIT.

use serde::{Deserialize, Serialize};

use crate::api::BaseResp;
use crate::api::HasBaseResp;
use crate::core::model::{ApiRequest, CommonResponse, MethodOption, ReqParam, StreamReqParam};
use crate::{
    core::{http_client::HttpClient, store::Store},
    error::Error,
};

use crate::api::gen::task::TaskService;

impl<'c, IStore: Store, IClient: HttpClient> TaskService<'c, IStore, IClient> {
    /// **api 版本: 2023-07-13T02:33:25+00:00**
    ///
    /// ## 删除提醒时间
    ///
    /// 删除提醒时间，返回结果状态。
    ///

    ///
    /// doc: <https://open.larkoffice.com/document/uAjLw4CM/ukTMukTMukTM/reference/task-v1/task-reminder/delete>
    ///
    /// new doc: <https://open.larkoffice.com/document/server-docs/task-v1/task-reminder/delete>
    ///
    /// api url: <https://open.feishu.cn/document_portal/v1/document/get_detail?fullPath=%2Fserver-docs%2Ftask-v1%2Ftask-reminder%2Fdelete>
    pub async fn delete_task_reminder(
        &self,
        req: DeleteTaskReminderReq,
    ) -> Result<(DeleteTaskReminderResp, CommonResponse), Error> {
        self.delete_task_reminder_with_opt(req, Default::default())
            .await
    }

    /// 参见 [delete_task_reminder](#method.delete_task_reminder) 函数
    pub async fn delete_task_reminder_with_opt(
        &self,
        req: DeleteTaskReminderReq,
        method_option: MethodOption,
    ) -> Result<(DeleteTaskReminderResp, CommonResponse), Error> {
        #[cfg(feature = "test-util")]
        {
            if let Some(f) = self.mock().get_mock_delete_task_reminder(&req) {
                tracing::info!("[lark] Task#DeleteTaskReminder **mocking** api");
                return f(req);
            }
        }

        tracing::info!("[lark] Task#DeleteTaskReminder call api");

        let req = ApiRequest {
            scope: "Task",
            api: "DeleteTaskReminder",
            method: http::Method::DELETE,
            url: String::new()
                + self.cli.open_base_url.as_ref()
                + "/open-apis/task/v1/tasks/:task_id/reminders/:reminder_id",
            param_data: req.gen_param(),
            method_option,
            need_tenant_access_token: true,
            need_user_access_token: true,
            ..Default::default()
        };

        let (resp, common_resp): (DeleteTaskReminderRespInner, _) = self.cli.do_req(req).await?;
        let data = match resp.data {
            Some(data) => data,
            None => {
                return Err(Error::ErrResponse(
                    anyhow::anyhow!("missing response data"),
                    common_resp,
                ));
            }
        };
        Ok((data, common_resp))
    }
}

#[derive(Debug, Serialize, Deserialize, Clone, Default, lark_bot_sdk_macros::ApiReqParams)]
pub struct DeleteTaskReminderReq {
    /// 任务 ID
    ///
    /// **是否必填**: 是
    ///
    /// **示例值**: "83912691-2e43-47fc-94a4-d512e03984fa"
    #[api(kind = "path", name = "task_id")]
    pub task_id: String,
    /// 任务提醒时间设置的 ID（即 reminder.id）
    ///
    /// **是否必填**: 是
    ///
    /// **示例值**: "1"
    #[api(kind = "path", name = "reminder_id")]
    pub reminder_id: String,
}

#[derive(Debug, Serialize, Deserialize, Clone, lark_bot_sdk_macros::ApiBaseResp)]
struct DeleteTaskReminderRespInner {
    #[serde(flatten)]
    data: Option<DeleteTaskReminderResp>,
    #[serde(flatten)]
    base: BaseResp,
}

#[derive(Debug, Serialize, Deserialize, Clone, Default)]
pub struct DeleteTaskReminderResp {
    /// \-
    #[serde(
        rename = "data",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub data: (),
}

#[cfg(feature = "test-util")]
mod test_utils {
    use std::sync::Arc;

    use self::gen::task::TaskServiceMocker;
    use crate::core::mocker::*;

    use super::*;
    use crate::api::*;
    use crate::core::model::*;

    pub struct Mocker;

    pub trait MockFunc:
        Fn(DeleteTaskReminderReq) -> Result<(DeleteTaskReminderResp, CommonResponse), Error>
        + Send
        + Sync
        + 'static
    {
    }
    impl<
            T: Fn(DeleteTaskReminderReq) -> Result<(DeleteTaskReminderResp, CommonResponse), Error>
                + Send
                + Sync
                + 'static,
        > MockFunc for T
    {
    }

    impl<'c, IStore: Store, IClient: HttpClient> TaskServiceMocker<'c, IStore, IClient> {
        pub fn mock_delete_task_reminder<F: MockFunc>(
            &self,
            f: F,
        ) -> MockerBuilder<Mocker, DeleteTaskReminderReq, DeleteTaskReminderResp, Arc<dyn MockFunc>>
        {
            MockerBuilder::new(self.cli.instance_id, Arc::new(f))
        }

        pub(super) fn get_mock_delete_task_reminder(
            &self,
            req: &DeleteTaskReminderReq,
        ) -> Option<Arc<dyn MockFunc>> {
            do_mock::<Mocker, DeleteTaskReminderReq, DeleteTaskReminderResp, Arc<dyn MockFunc>>(
                self.cli.instance_id,
                req,
            )
        }
    }
}

#[cfg(feature = "test-util")]
#[cfg(test)]
mod test {
    use crate::{
        api::gen::task::delete_task_reminder::{
            DeleteTaskReminderReq, DeleteTaskReminderResp, DeleteTaskReminderRespInner,
        },
        core::{model::CommonResponse, Lark},
    };

    #[tokio::test]
    async fn test_mock() {
        let lark = Lark::new("".to_owned(), "".into());
        let mocker = lark
            .task()
            .mock()
            .mock_delete_task_reminder(|_| {
                Ok((DeleteTaskReminderResp::default(), CommonResponse::default()))
            })
            .build();
        let res = lark
            .task()
            .delete_task_reminder(DeleteTaskReminderReq::default())
            .await;
        assert!(res.is_ok());
        mocker.clear();
        let res = lark
            .task()
            .delete_task_reminder(DeleteTaskReminderReq::default())
            .await;
        assert!(res.is_err());
    }

    const REQ: &str = "{}";

    #[test]
    fn test_req() {
        if REQ == "{}" {
            return;
        }
        if let Err(e) = serde_json::from_str::<()>(REQ) {
            panic!("{}", e);
        }
    }

    const RESP: &str = r#"{
    "code": 0,
    "msg": "success",
    "data": {}
}"#;
    #[test]
    fn test_resp() {
        let res = serde_json::from_str::<DeleteTaskReminderRespInner>(RESP);
        if let Err(e) = res {
            panic!("{}", e);
        }
        if let Ok(v) = serde_json::from_str::<serde_json::Value>(RESP) {
            if v.get("data").is_some() {
                assert!(res.unwrap().data.is_some());
            }
        }
    }
}
