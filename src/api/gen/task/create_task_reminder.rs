//! doc url: <https://open.larkoffice.com/document/uAjLw4CM/ukTMukTMukTM/reference/task-v1/task-reminder/create>
// Code generated by gen_api. DO NOT EDIT.

use serde::{Deserialize, Serialize};

use crate::api::BaseResp;
use crate::api::HasBaseResp;
use crate::core::model::{ApiRequest, CommonResponse, MethodOption, ReqParam, StreamReqParam};
use crate::{
    core::{http_client::HttpClient, store::Store},
    error::Error,
};

use crate::api::gen::task::TaskService;

impl<'c, IStore: Store, IClient: HttpClient> TaskService<'c, IStore, IClient> {
    /// **api 版本: 2023-07-13T02:33:25+00:00**
    ///
    /// ## 新增提醒时间
    ///
    /// 该接口用于创建任务的提醒时间。提醒时间在截止时间基础上做偏移，但是偏移后的结果不能早于当前时间。
    ///

    ///
    /// doc: <https://open.larkoffice.com/document/uAjLw4CM/ukTMukTMukTM/reference/task-v1/task-reminder/create>
    ///
    /// new doc: <https://open.larkoffice.com/document/server-docs/task-v1/task-reminder/create>
    ///
    /// api url: <https://open.feishu.cn/document_portal/v1/document/get_detail?fullPath=%2Fserver-docs%2Ftask-v1%2Ftask-reminder%2Fcreate>
    pub async fn create_task_reminder(
        &self,
        req: CreateTaskReminderReq,
    ) -> Result<(CreateTaskReminderResp, CommonResponse), Error> {
        self.create_task_reminder_with_opt(req, Default::default())
            .await
    }

    /// 参见 [create_task_reminder](#method.create_task_reminder) 函数
    pub async fn create_task_reminder_with_opt(
        &self,
        req: CreateTaskReminderReq,
        method_option: MethodOption,
    ) -> Result<(CreateTaskReminderResp, CommonResponse), Error> {
        #[cfg(feature = "test-util")]
        {
            if let Some(f) = self.mock().get_mock_create_task_reminder(&req) {
                tracing::info!("[lark] Task#CreateTaskReminder **mocking** api");
                return f(req);
            }
        }

        tracing::info!("[lark] Task#CreateTaskReminder call api");

        let req = ApiRequest {
            scope: "Task",
            api: "CreateTaskReminder",
            method: http::Method::POST,
            url: String::new()
                + self.cli.open_base_url.as_ref()
                + "/open-apis/task/v1/tasks/:task_id/reminders",
            param_data: req.gen_param(),
            method_option,
            need_tenant_access_token: true,
            need_user_access_token: true,
            ..Default::default()
        };

        let (resp, common_resp): (CreateTaskReminderRespInner, _) = self.cli.do_req(req).await?;
        let data = match resp.data {
            Some(data) => data,
            None => {
                return Err(Error::ErrResponse(
                    anyhow::anyhow!("missing response data"),
                    common_resp,
                ));
            }
        };
        Ok((data, common_resp))
    }
}

#[derive(Debug, Serialize, Deserialize, Clone, Default, lark_bot_sdk_macros::ApiReqParams)]
pub struct CreateTaskReminderReq {
    /// 任务 ID
    ///
    /// **是否必填**: 是
    ///
    /// **示例值**: "83912691-2e43-47fc-94a4-d512e03984fa"
    #[api(kind = "path", name = "task_id")]
    pub task_id: String,

    /// 相对于截止时间的提醒时间（如提前 30 分钟，截止时间后 30 分钟，则为 -30） 任务没有截止时间则为全天任务(截止时间为0)
    ///
    /// **是否必填**: 是
    ///
    /// **示例值**: "30"
    #[api(kind = "body", name = "relative_fire_minute")]
    pub relative_fire_minute: i64,
}

#[derive(Debug, Serialize, Deserialize, Clone, lark_bot_sdk_macros::ApiBaseResp)]
struct CreateTaskReminderRespInner {
    #[serde(flatten)]
    data: Option<CreateTaskReminderResp>,
    #[serde(flatten)]
    base: BaseResp,
}

#[derive(Debug, Serialize, Deserialize, Clone, Default)]
pub struct CreateTaskReminderResp {
    /// \-
    #[serde(
        rename = "data",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub data: DataSubResp,
}

#[derive(Debug, Serialize, Deserialize, Clone, Default)]
#[serde(default)]
pub struct DataSubResp {
    /// 返回创建成功的提醒时间
    #[serde(
        rename = "reminder",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub reminder: ReminderSubResp,
}

#[derive(Debug, Serialize, Deserialize, Clone, Default)]
#[serde(default)]
pub struct ReminderSubResp {
    /// 提醒时间设置的 ID（在删除时候需要使用这个）
    ///
    /// **示例值**: "1"
    #[serde(
        rename = "id",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub id: String,
    /// 相对于截止时间的提醒时间（如提前 30 分钟，截止时间后 30 分钟，则为 -30） 任务没有截止时间则为全天任务(截止时间为0)
    ///
    /// **是否必填**: 是
    ///
    /// **示例值**: "30"
    #[serde(
        rename = "relative_fire_minute",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub relative_fire_minute: i64,
}

#[cfg(feature = "test-util")]
mod test_utils {
    use std::sync::Arc;

    use self::gen::task::TaskServiceMocker;
    use crate::core::mocker::*;

    use super::*;
    use crate::api::*;
    use crate::core::model::*;

    pub struct Mocker;

    pub trait MockFunc:
        Fn(CreateTaskReminderReq) -> Result<(CreateTaskReminderResp, CommonResponse), Error>
        + Send
        + Sync
        + 'static
    {
    }
    impl<
            T: Fn(CreateTaskReminderReq) -> Result<(CreateTaskReminderResp, CommonResponse), Error>
                + Send
                + Sync
                + 'static,
        > MockFunc for T
    {
    }

    impl<'c, IStore: Store, IClient: HttpClient> TaskServiceMocker<'c, IStore, IClient> {
        pub fn mock_create_task_reminder<F: MockFunc>(
            &self,
            f: F,
        ) -> MockerBuilder<Mocker, CreateTaskReminderReq, CreateTaskReminderResp, Arc<dyn MockFunc>>
        {
            MockerBuilder::new(self.cli.instance_id, Arc::new(f))
        }

        pub(super) fn get_mock_create_task_reminder(
            &self,
            req: &CreateTaskReminderReq,
        ) -> Option<Arc<dyn MockFunc>> {
            do_mock::<Mocker, CreateTaskReminderReq, CreateTaskReminderResp, Arc<dyn MockFunc>>(
                self.cli.instance_id,
                req,
            )
        }
    }
}

#[cfg(feature = "test-util")]
#[cfg(test)]
mod test {
    use crate::{
        api::gen::task::create_task_reminder::{
            CreateTaskReminderReq, CreateTaskReminderResp, CreateTaskReminderRespInner,
        },
        core::{model::CommonResponse, Lark},
    };

    #[tokio::test]
    async fn test_mock() {
        let lark = Lark::new("".to_owned(), "".into());
        let mocker = lark
            .task()
            .mock()
            .mock_create_task_reminder(|_| {
                Ok((CreateTaskReminderResp::default(), CommonResponse::default()))
            })
            .build();
        let res = lark
            .task()
            .create_task_reminder(CreateTaskReminderReq::default())
            .await;
        assert!(res.is_ok());
        mocker.clear();
        let res = lark
            .task()
            .create_task_reminder(CreateTaskReminderReq::default())
            .await;
        assert!(res.is_err());
    }

    const REQ: &str = r#"{
    "relative_fire_minute": 30
}"#;

    #[test]
    fn test_req() {
        if REQ == "{}" {
            return;
        }
        if let Err(e) = serde_json::from_str::<super::CreateTaskReminderReqBody>(REQ) {
            panic!("{}", e);
        }
    }

    const RESP: &str = r#"{
    "code": 0,
    "msg": "success",
    "data": {
        "reminder": {
            "id": "1",
            "relative_fire_minute": 30
        }
    }
}"#;
    #[test]
    fn test_resp() {
        let res = serde_json::from_str::<CreateTaskReminderRespInner>(RESP);
        if let Err(e) = res {
            panic!("{}", e);
        }
        if let Ok(v) = serde_json::from_str::<serde_json::Value>(RESP) {
            if v.get("data").is_some() {
                assert!(res.unwrap().data.is_some());
            }
        }
    }
}
