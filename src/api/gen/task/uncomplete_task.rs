//! doc url: <https://open.larkoffice.com/document/uAjLw4CM/ukTMukTMukTM/reference/task-v1/task/uncomplete>
// Code generated by gen_api. DO NOT EDIT.

use serde::{Deserialize, Serialize};

use crate::api::BaseResp;
use crate::api::HasBaseResp;
use crate::core::model::{ApiRequest, CommonResponse, MethodOption, ReqParam, StreamReqParam};
use crate::{
    core::{http_client::HttpClient, store::Store},
    error::Error,
};

use crate::api::gen::task::TaskService;

impl<'c, IStore: Store, IClient: HttpClient> TaskService<'c, IStore, IClient> {
    /// **api 版本: 2023-07-13T02:33:24+00:00**
    ///
    /// ## 取消完成任务
    ///
    /// 该接口用于取消任务的已完成状态。
    ///

    ///
    /// doc: <https://open.larkoffice.com/document/uAjLw4CM/ukTMukTMukTM/reference/task-v1/task/uncomplete>
    ///
    /// new doc: <https://open.larkoffice.com/document/server-docs/task-v1/task/uncomplete>
    ///
    /// api url: <https://open.feishu.cn/document_portal/v1/document/get_detail?fullPath=%2Fserver-docs%2Ftask-v1%2Ftask%2Funcomplete>
    pub async fn uncomplete_task(
        &self,
        req: UncompleteTaskReq,
    ) -> Result<(UncompleteTaskResp, CommonResponse), Error> {
        self.uncomplete_task_with_opt(req, Default::default()).await
    }

    /// 参见 [uncomplete_task](#method.uncomplete_task) 函数
    pub async fn uncomplete_task_with_opt(
        &self,
        req: UncompleteTaskReq,
        method_option: MethodOption,
    ) -> Result<(UncompleteTaskResp, CommonResponse), Error> {
        #[cfg(feature = "test-util")]
        {
            if let Some(f) = self.mock().get_mock_uncomplete_task(&req) {
                tracing::info!("[lark] Task#UncompleteTask **mocking** api");
                return f(req);
            }
        }

        tracing::info!("[lark] Task#UncompleteTask call api");

        let req = ApiRequest {
            scope: "Task",
            api: "UncompleteTask",
            method: http::Method::POST,
            url: String::new()
                + self.cli.open_base_url.as_ref()
                + "/open-apis/task/v1/tasks/:task_id/uncomplete",
            param_data: req.gen_param(),
            method_option,
            need_tenant_access_token: true,
            need_user_access_token: true,
            ..Default::default()
        };

        let (resp, common_resp): (UncompleteTaskRespInner, _) = self.cli.do_req(req).await?;
        let data = match resp.data {
            Some(data) => data,
            None => {
                return Err(Error::ErrResponse(
                    anyhow::anyhow!("missing response data"),
                    common_resp,
                ));
            }
        };
        Ok((data, common_resp))
    }
}

#[derive(Debug, Serialize, Deserialize, Clone, Default, lark_bot_sdk_macros::ApiReqParams)]
pub struct UncompleteTaskReq {
    /// 任务 ID
    ///
    /// **是否必填**: 是
    ///
    /// **示例值**: "bb54ab99-d360-434f-bcaa-a4cc4c05840e"
    #[api(kind = "path", name = "task_id")]
    pub task_id: String,
}

#[derive(Debug, Serialize, Deserialize, Clone, lark_bot_sdk_macros::ApiBaseResp)]
struct UncompleteTaskRespInner {
    #[serde(flatten)]
    data: Option<UncompleteTaskResp>,
    #[serde(flatten)]
    base: BaseResp,
}

#[derive(Debug, Serialize, Deserialize, Clone, Default)]
pub struct UncompleteTaskResp {
    /// \-
    #[serde(
        rename = "data",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub data: (),
}

#[cfg(feature = "test-util")]
mod test_utils {
    use std::sync::Arc;

    use self::gen::task::TaskServiceMocker;
    use crate::core::mocker::*;

    use super::*;
    use crate::api::*;
    use crate::core::model::*;

    pub struct Mocker;

    pub trait MockFunc:
        Fn(UncompleteTaskReq) -> Result<(UncompleteTaskResp, CommonResponse), Error>
        + Send
        + Sync
        + 'static
    {
    }
    impl<
            T: Fn(UncompleteTaskReq) -> Result<(UncompleteTaskResp, CommonResponse), Error>
                + Send
                + Sync
                + 'static,
        > MockFunc for T
    {
    }

    impl<'c, IStore: Store, IClient: HttpClient> TaskServiceMocker<'c, IStore, IClient> {
        pub fn mock_uncomplete_task<F: MockFunc>(
            &self,
            f: F,
        ) -> MockerBuilder<Mocker, UncompleteTaskReq, UncompleteTaskResp, Arc<dyn MockFunc>>
        {
            MockerBuilder::new(self.cli.instance_id, Arc::new(f))
        }

        pub(super) fn get_mock_uncomplete_task(
            &self,
            req: &UncompleteTaskReq,
        ) -> Option<Arc<dyn MockFunc>> {
            do_mock::<Mocker, UncompleteTaskReq, UncompleteTaskResp, Arc<dyn MockFunc>>(
                self.cli.instance_id,
                req,
            )
        }
    }
}

#[cfg(feature = "test-util")]
#[cfg(test)]
mod test {
    use crate::{
        api::gen::task::uncomplete_task::{
            UncompleteTaskReq, UncompleteTaskResp, UncompleteTaskRespInner,
        },
        core::{model::CommonResponse, Lark},
    };

    #[tokio::test]
    async fn test_mock() {
        let lark = Lark::new("".to_owned(), "".into());
        let mocker = lark
            .task()
            .mock()
            .mock_uncomplete_task(|_| {
                Ok((UncompleteTaskResp::default(), CommonResponse::default()))
            })
            .build();
        let res = lark
            .task()
            .uncomplete_task(UncompleteTaskReq::default())
            .await;
        assert!(res.is_ok());
        mocker.clear();
        let res = lark
            .task()
            .uncomplete_task(UncompleteTaskReq::default())
            .await;
        assert!(res.is_err());
    }

    const REQ: &str = "{}";

    #[test]
    fn test_req() {
        if REQ == "{}" {
            return;
        }
        if let Err(e) = serde_json::from_str::<()>(REQ) {
            panic!("{}", e);
        }
    }

    const RESP: &str = r#"{
    "code": 0,
    "msg": "success",
    "data": {}
}"#;
    #[test]
    fn test_resp() {
        let res = serde_json::from_str::<UncompleteTaskRespInner>(RESP);
        if let Err(e) = res {
            panic!("{}", e);
        }
        if let Ok(v) = serde_json::from_str::<serde_json::Value>(RESP) {
            if v.get("data").is_some() {
                assert!(res.unwrap().data.is_some());
            }
        }
    }
}
