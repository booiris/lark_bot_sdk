//! doc url: <https://open.larkoffice.com/document/uAjLw4CM/ukTMukTMukTM/reference/vc-v1/room_level/search>
// Code generated by gen_api. DO NOT EDIT.

use serde::{Deserialize, Serialize};

use crate::api::BaseResp;
use crate::api::HasBaseResp;
use crate::core::model::{ApiRequest, CommonResponse, MethodOption, ReqParam, StreamReqParam};
use crate::{
    core::{http_client::HttpClient, store::Store},
    error::Error,
};

use crate::api::gen::vc::VcService;

impl<'c, IStore: Store, IClient: HttpClient> VcService<'c, IStore, IClient> {
    /// **api 版本: 2024-07-23T07:32:58+00:00**
    ///
    /// ## 搜索会议室层级
    ///
    /// 该接口可以用来搜索会议室层级，支持使用自定义会议室层级 ID 进行查询。
    ///

    ///
    /// doc: <https://open.larkoffice.com/document/uAjLw4CM/ukTMukTMukTM/reference/vc-v1/room_level/search>
    ///
    /// new doc: <https://open.larkoffice.com/document/server-docs/vc-v1/room_level/search>
    ///
    /// api url: <https://open.feishu.cn/document_portal/v1/document/get_detail?fullPath=%2Fserver-docs%2Fvc-v1%2Froom_level%2Fsearch>
    pub async fn search_vc_room_level(
        &self,
        req: SearchVcRoomLevelReq,
    ) -> Result<(SearchVcRoomLevelResp, CommonResponse), Error> {
        self.search_vc_room_level_with_opt(req, Default::default())
            .await
    }

    /// 参见 [search_vc_room_level](#method.search_vc_room_level) 函数
    pub async fn search_vc_room_level_with_opt(
        &self,
        req: SearchVcRoomLevelReq,
        method_option: MethodOption,
    ) -> Result<(SearchVcRoomLevelResp, CommonResponse), Error> {
        #[cfg(feature = "test-util")]
        {
            if let Some(f) = self.mock().get_mock_search_vc_room_level(&req) {
                tracing::info!("[lark] Vc#SearchVcRoomLevel **mocking** api");
                return f(req);
            }
        }

        tracing::info!("[lark] Vc#SearchVcRoomLevel call api");

        let req = ApiRequest {
            scope: "Vc",
            api: "SearchVcRoomLevel",
            method: http::Method::GET,
            url: String::new()
                + self.cli.open_base_url.as_ref()
                + "/open-apis/vc/v1/room_levels/search",
            param_data: req.gen_param(),
            method_option,
            need_tenant_access_token: true,
            ..Default::default()
        };

        let (resp, common_resp): (SearchVcRoomLevelRespInner, _) = self.cli.do_req(req).await?;
        let data = match resp.data {
            Some(data) => data,
            None => {
                return Err(Error::ErrResponse(
                    anyhow::anyhow!("missing response data"),
                    common_resp,
                ));
            }
        };
        Ok((data, common_resp))
    }
}

#[derive(Debug, Serialize, Deserialize, Clone, Default, lark_bot_sdk_macros::ApiReqParams)]
pub struct SearchVcRoomLevelReq {
    /// 用于查询指定会议室层级的自定义会议室层级ID
    ///
    /// **是否必填**: 是
    ///
    /// **示例值**: "1000,1001"
    #[api(
        kind = "query",
        name = "custom_level_ids",
        v_type = "var",
        option = "false"
    )]
    pub custom_level_ids: String,
}

#[derive(Debug, Serialize, Deserialize, Clone, lark_bot_sdk_macros::ApiBaseResp)]
struct SearchVcRoomLevelRespInner {
    #[serde(flatten)]
    data: Option<SearchVcRoomLevelResp>,
    #[serde(flatten)]
    base: BaseResp,
}

#[derive(Debug, Serialize, Deserialize, Clone, Default)]
pub struct SearchVcRoomLevelResp {
    /// \-
    #[serde(
        rename = "data",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub data: DataSubResp,
}

#[derive(Debug, Serialize, Deserialize, Clone, Default)]
#[serde(default)]
pub struct DataSubResp {
    /// 层级ID列表
    #[serde(
        rename = "level_ids",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub level_ids: Vec<String>,
}

#[cfg(feature = "test-util")]
mod test_utils {
    use std::sync::Arc;

    use self::gen::vc::VcServiceMocker;
    use crate::core::mocker::*;

    use super::*;
    use crate::api::*;
    use crate::core::model::*;

    pub struct Mocker;

    pub trait MockFunc:
        Fn(SearchVcRoomLevelReq) -> Result<(SearchVcRoomLevelResp, CommonResponse), Error>
        + Send
        + Sync
        + 'static
    {
    }
    impl<
            T: Fn(SearchVcRoomLevelReq) -> Result<(SearchVcRoomLevelResp, CommonResponse), Error>
                + Send
                + Sync
                + 'static,
        > MockFunc for T
    {
    }

    impl<'c, IStore: Store, IClient: HttpClient> VcServiceMocker<'c, IStore, IClient> {
        pub fn mock_search_vc_room_level<F: MockFunc>(
            &self,
            f: F,
        ) -> MockerBuilder<Mocker, SearchVcRoomLevelReq, SearchVcRoomLevelResp, Arc<dyn MockFunc>>
        {
            MockerBuilder::new(self.cli.instance_id, Arc::new(f))
        }

        pub(super) fn get_mock_search_vc_room_level(
            &self,
            req: &SearchVcRoomLevelReq,
        ) -> Option<Arc<dyn MockFunc>> {
            do_mock::<Mocker, SearchVcRoomLevelReq, SearchVcRoomLevelResp, Arc<dyn MockFunc>>(
                self.cli.instance_id,
                req,
            )
        }
    }
}

#[cfg(feature = "test-util")]
#[cfg(test)]
mod test {
    use crate::{
        api::gen::vc::search_vc_room_level::{
            SearchVcRoomLevelReq, SearchVcRoomLevelResp, SearchVcRoomLevelRespInner,
        },
        core::{model::CommonResponse, Lark},
    };

    #[tokio::test]
    async fn test_mock() {
        let lark = Lark::new("".to_owned(), "".into());
        let mocker = lark
            .vc()
            .mock()
            .mock_search_vc_room_level(|_| {
                Ok((SearchVcRoomLevelResp::default(), CommonResponse::default()))
            })
            .build();
        let res = lark
            .vc()
            .search_vc_room_level(SearchVcRoomLevelReq::default())
            .await;
        assert!(res.is_ok());
        mocker.clear();
        let res = lark
            .vc()
            .search_vc_room_level(SearchVcRoomLevelReq::default())
            .await;
        assert!(res.is_err());
    }

    const REQ: &str = "{}";

    #[test]
    fn test_req() {
        if REQ == "{}" {
            return;
        }
        if let Err(e) = serde_json::from_str::<()>(REQ) {
            panic!("{}", e);
        }
    }

    const RESP: &str = r#"{
    "code": 0,
    "msg": "success",
    "data": {
        "level_ids": [
            "omb_4ad1a2c7a2fbc5fc9570f38456931293"
        ]
    }
}"#;
    #[test]
    fn test_resp() {
        let res = serde_json::from_str::<SearchVcRoomLevelRespInner>(RESP);
        if let Err(e) = res {
            panic!("{}", e);
        }
        if let Ok(v) = serde_json::from_str::<serde_json::Value>(RESP) {
            if v.get("data").is_some() {
                assert!(res.unwrap().data.is_some());
            }
        }
    }
}
