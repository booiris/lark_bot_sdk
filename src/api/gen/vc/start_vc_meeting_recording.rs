//! doc url: <https://open.larkoffice.com/document/uAjLw4CM/ukTMukTMukTM/reference/vc-v1/meeting-recording/start>
// Code generated by gen_api. DO NOT EDIT.

use serde::{Deserialize, Serialize};

use crate::api::BaseResp;
use crate::api::HasBaseResp;
use crate::core::model::{ApiRequest, CommonResponse, MethodOption, ReqParam, StreamReqParam};
use crate::{
    core::{http_client::HttpClient, store::Store},
    error::Error,
};

use crate::api::gen::vc::VcService;

impl<'c, IStore: Store, IClient: HttpClient> VcService<'c, IStore, IClient> {
    /// **api 版本: 2024-06-14T06:54:22+00:00**
    ///
    /// ## 开始录制
    ///
    /// 在会议中开始录制。
    ///
    /// 会议正在进行中，且操作者具有相应权限（如果操作者为用户，必须是会中当前主持人）
    ///
    /// doc: <https://open.larkoffice.com/document/uAjLw4CM/ukTMukTMukTM/reference/vc-v1/meeting-recording/start>
    ///
    /// new doc: <https://open.larkoffice.com/document/server-docs/vc-v1/meeting-recording/start>
    ///
    /// api url: <https://open.feishu.cn/document_portal/v1/document/get_detail?fullPath=%2Fserver-docs%2Fvc-v1%2Fmeeting-recording%2Fstart>
    pub async fn start_vc_meeting_recording(
        &self,
        req: StartVcMeetingRecordingReq,
    ) -> Result<(StartVcMeetingRecordingResp, CommonResponse), Error> {
        self.start_vc_meeting_recording_with_opt(req, Default::default())
            .await
    }

    /// 参见 [start_vc_meeting_recording](#method.start_vc_meeting_recording) 函数
    pub async fn start_vc_meeting_recording_with_opt(
        &self,
        req: StartVcMeetingRecordingReq,
        method_option: MethodOption,
    ) -> Result<(StartVcMeetingRecordingResp, CommonResponse), Error> {
        #[cfg(feature = "test-util")]
        {
            if let Some(f) = self.mock().get_mock_start_vc_meeting_recording(&req) {
                tracing::info!("[lark] Vc#StartVcMeetingRecording **mocking** api");
                return f(req);
            }
        }

        tracing::info!("[lark] Vc#StartVcMeetingRecording call api");

        let req = ApiRequest {
            scope: "Vc",
            api: "StartVcMeetingRecording",
            method: http::Method::PATCH,
            url: String::new()
                + self.cli.open_base_url.as_ref()
                + "/open-apis/vc/v1/meetings/:meeting_id/recording/start",
            param_data: req.gen_param(),
            method_option,
            need_user_access_token: true,
            ..Default::default()
        };

        let (resp, common_resp): (StartVcMeetingRecordingRespInner, _) =
            self.cli.do_req(req).await?;
        let data = match resp.data {
            Some(data) => data,
            None => {
                return Err(Error::ErrResponse(
                    anyhow::anyhow!("missing response data"),
                    common_resp,
                ));
            }
        };
        Ok((data, common_resp))
    }
}

#[derive(Debug, Serialize, Deserialize, Clone, Default, lark_bot_sdk_macros::ApiReqParams)]
pub struct StartVcMeetingRecordingReq {
    /// 会议ID（视频会议的唯一标识，视频会议开始后才会产生）
    ///
    /// **示例值**: "6911188411932033028"
    #[api(kind = "path", name = "meeting_id")]
    pub meeting_id: String,

    /// 录制文件时间显示使用的时区[-12,12]
    ///
    /// **示例值**: "8"
    #[api(kind = "body", name = "timezone")]
    pub timezone: Option<i64>,
}

#[derive(Debug, Serialize, Deserialize, Clone, lark_bot_sdk_macros::ApiBaseResp)]
struct StartVcMeetingRecordingRespInner {
    #[serde(flatten)]
    data: Option<StartVcMeetingRecordingResp>,
    #[serde(flatten)]
    base: BaseResp,
}

#[derive(Debug, Serialize, Deserialize, Clone, Default)]
pub struct StartVcMeetingRecordingResp {
    /// \-
    #[serde(
        rename = "data",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub data: (),
}

#[cfg(feature = "test-util")]
mod test_utils {
    use std::sync::Arc;

    use self::gen::vc::VcServiceMocker;
    use crate::core::mocker::*;

    use super::*;
    use crate::api::*;
    use crate::core::model::*;

    pub struct Mocker;

    pub trait MockFunc:
        Fn(
            StartVcMeetingRecordingReq,
        ) -> Result<(StartVcMeetingRecordingResp, CommonResponse), Error>
        + Send
        + Sync
        + 'static
    {
    }
    impl<
            T: Fn(
                    StartVcMeetingRecordingReq,
                ) -> Result<(StartVcMeetingRecordingResp, CommonResponse), Error>
                + Send
                + Sync
                + 'static,
        > MockFunc for T
    {
    }

    impl<'c, IStore: Store, IClient: HttpClient> VcServiceMocker<'c, IStore, IClient> {
        pub fn mock_start_vc_meeting_recording<F: MockFunc>(
            &self,
            f: F,
        ) -> MockerBuilder<
            Mocker,
            StartVcMeetingRecordingReq,
            StartVcMeetingRecordingResp,
            Arc<dyn MockFunc>,
        > {
            MockerBuilder::new(self.cli.instance_id, Arc::new(f))
        }

        pub(super) fn get_mock_start_vc_meeting_recording(
            &self,
            req: &StartVcMeetingRecordingReq,
        ) -> Option<Arc<dyn MockFunc>> {
            do_mock::<
                Mocker,
                StartVcMeetingRecordingReq,
                StartVcMeetingRecordingResp,
                Arc<dyn MockFunc>,
            >(self.cli.instance_id, req)
        }
    }
}

#[cfg(feature = "test-util")]
#[cfg(test)]
mod test {
    use crate::{
        api::gen::vc::start_vc_meeting_recording::{
            StartVcMeetingRecordingReq, StartVcMeetingRecordingResp,
            StartVcMeetingRecordingRespInner,
        },
        core::{model::CommonResponse, Lark},
    };

    #[tokio::test]
    async fn test_mock() {
        let lark = Lark::new("".to_owned(), "".into());
        let mocker = lark
            .vc()
            .mock()
            .mock_start_vc_meeting_recording(|_| {
                Ok((
                    StartVcMeetingRecordingResp::default(),
                    CommonResponse::default(),
                ))
            })
            .build();
        let res = lark
            .vc()
            .start_vc_meeting_recording(StartVcMeetingRecordingReq::default())
            .await;
        assert!(res.is_ok());
        mocker.clear();
        let res = lark
            .vc()
            .start_vc_meeting_recording(StartVcMeetingRecordingReq::default())
            .await;
        assert!(res.is_err());
    }

    const REQ: &str = r#"{
    "timezone": 8
}"#;

    #[test]
    fn test_req() {
        if REQ == "{}" {
            return;
        }
        if let Err(e) = serde_json::from_str::<super::StartVcMeetingRecordingReqBody>(REQ) {
            panic!("{}", e);
        }
    }

    const RESP: &str = r#"{
    "code": 0,
    "msg": "success",
    "data": {}
}"#;
    #[test]
    fn test_resp() {
        let res = serde_json::from_str::<StartVcMeetingRecordingRespInner>(RESP);
        if let Err(e) = res {
            panic!("{}", e);
        }
        if let Ok(v) = serde_json::from_str::<serde_json::Value>(RESP) {
            if v.get("data").is_some() {
                assert!(res.unwrap().data.is_some());
            }
        }
    }
}
