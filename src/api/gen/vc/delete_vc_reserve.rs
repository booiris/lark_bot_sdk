//! doc url: <https://open.larkoffice.com/document/uAjLw4CM/ukTMukTMukTM/reference/vc-v1/reserve/delete>
// Code generated by gen_api. DO NOT EDIT.

use serde::{Deserialize, Serialize};

use crate::api::BaseResp;
use crate::api::HasBaseResp;
use crate::core::model::{ApiRequest, CommonResponse, MethodOption, ReqParam, StreamReqParam};
use crate::{
    core::{http_client::HttpClient, store::Store},
    error::Error,
};

use crate::api::gen::vc::VcService;

impl<'c, IStore: Store, IClient: HttpClient> VcService<'c, IStore, IClient> {
    /// **api 版本: 2024-07-23T07:32:55+00:00**
    ///
    /// ## 删除预约
    ///
    /// 删除一个预约。
    ///
    /// 只能删除归属于自己的预约；删除后数据不可恢复
    ///
    /// doc: <https://open.larkoffice.com/document/uAjLw4CM/ukTMukTMukTM/reference/vc-v1/reserve/delete>
    ///
    /// new doc: <https://open.larkoffice.com/document/server-docs/vc-v1/reserve/delete>
    ///
    /// api url: <https://open.feishu.cn/document_portal/v1/document/get_detail?fullPath=%2Fserver-docs%2Fvc-v1%2Freserve%2Fdelete>
    pub async fn delete_vc_reserve(
        &self,
        req: DeleteVcReserveReq,
    ) -> Result<(DeleteVcReserveResp, CommonResponse), Error> {
        self.delete_vc_reserve_with_opt(req, Default::default())
            .await
    }

    /// 参见 [delete_vc_reserve](#method.delete_vc_reserve) 函数
    pub async fn delete_vc_reserve_with_opt(
        &self,
        req: DeleteVcReserveReq,
        method_option: MethodOption,
    ) -> Result<(DeleteVcReserveResp, CommonResponse), Error> {
        #[cfg(feature = "test-util")]
        {
            if let Some(f) = self.mock().get_mock_delete_vc_reserve(&req) {
                tracing::info!("[lark] Vc#DeleteVcReserve **mocking** api");
                return f(req);
            }
        }

        tracing::info!("[lark] Vc#DeleteVcReserve call api");

        let req = ApiRequest {
            scope: "Vc",
            api: "DeleteVcReserve",
            method: http::Method::DELETE,
            url: String::new()
                + self.cli.open_base_url.as_ref()
                + "/open-apis/vc/v1/reserves/:reserve_id",
            param_data: req.gen_param(),
            method_option,
            need_tenant_access_token: true,
            need_user_access_token: true,
            ..Default::default()
        };

        let (resp, common_resp): (DeleteVcReserveRespInner, _) = self.cli.do_req(req).await?;
        let data = match resp.data {
            Some(data) => data,
            None => {
                return Err(Error::ErrResponse(
                    anyhow::anyhow!("missing response data"),
                    common_resp,
                ));
            }
        };
        Ok((data, common_resp))
    }
}

#[derive(Debug, Serialize, Deserialize, Clone, Default, lark_bot_sdk_macros::ApiReqParams)]
pub struct DeleteVcReserveReq {
    /// 预约ID（预约的唯一标识）
    ///
    /// **示例值**: "6911188411932033028"
    #[api(kind = "path", name = "reserve_id")]
    pub reserve_id: String,
}

#[derive(Debug, Serialize, Deserialize, Clone, lark_bot_sdk_macros::ApiBaseResp)]
struct DeleteVcReserveRespInner {
    #[serde(flatten)]
    data: Option<DeleteVcReserveResp>,
    #[serde(flatten)]
    base: BaseResp,
}

#[derive(Debug, Serialize, Deserialize, Clone, Default)]
pub struct DeleteVcReserveResp {
    /// \-
    #[serde(
        rename = "data",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub data: (),
}

#[cfg(feature = "test-util")]
mod test_utils {
    use std::sync::Arc;

    use self::gen::vc::VcServiceMocker;
    use crate::core::mocker::*;

    use super::*;
    use crate::api::*;
    use crate::core::model::*;

    pub struct Mocker;

    pub trait MockFunc:
        Fn(DeleteVcReserveReq) -> Result<(DeleteVcReserveResp, CommonResponse), Error>
        + Send
        + Sync
        + 'static
    {
    }
    impl<
            T: Fn(DeleteVcReserveReq) -> Result<(DeleteVcReserveResp, CommonResponse), Error>
                + Send
                + Sync
                + 'static,
        > MockFunc for T
    {
    }

    impl<'c, IStore: Store, IClient: HttpClient> VcServiceMocker<'c, IStore, IClient> {
        pub fn mock_delete_vc_reserve<F: MockFunc>(
            &self,
            f: F,
        ) -> MockerBuilder<Mocker, DeleteVcReserveReq, DeleteVcReserveResp, Arc<dyn MockFunc>>
        {
            MockerBuilder::new(self.cli.instance_id, Arc::new(f))
        }

        pub(super) fn get_mock_delete_vc_reserve(
            &self,
            req: &DeleteVcReserveReq,
        ) -> Option<Arc<dyn MockFunc>> {
            do_mock::<Mocker, DeleteVcReserveReq, DeleteVcReserveResp, Arc<dyn MockFunc>>(
                self.cli.instance_id,
                req,
            )
        }
    }
}

#[cfg(feature = "test-util")]
#[cfg(test)]
mod test {
    use crate::{
        api::gen::vc::delete_vc_reserve::{
            DeleteVcReserveReq, DeleteVcReserveResp, DeleteVcReserveRespInner,
        },
        core::{model::CommonResponse, Lark},
    };

    #[tokio::test]
    async fn test_mock() {
        let lark = Lark::new("".to_owned(), "".into());
        let mocker = lark
            .vc()
            .mock()
            .mock_delete_vc_reserve(|_| {
                Ok((DeleteVcReserveResp::default(), CommonResponse::default()))
            })
            .build();
        let res = lark
            .vc()
            .delete_vc_reserve(DeleteVcReserveReq::default())
            .await;
        assert!(res.is_ok());
        mocker.clear();
        let res = lark
            .vc()
            .delete_vc_reserve(DeleteVcReserveReq::default())
            .await;
        assert!(res.is_err());
    }

    const REQ: &str = "{}";

    #[test]
    fn test_req() {
        if REQ == "{}" {
            return;
        }
        if let Err(e) = serde_json::from_str::<()>(REQ) {
            panic!("{}", e);
        }
    }

    const RESP: &str = r#"{
    "code": 0,
    "msg": "success",
    "data": {}
}"#;
    #[test]
    fn test_resp() {
        let res = serde_json::from_str::<DeleteVcReserveRespInner>(RESP);
        if let Err(e) = res {
            panic!("{}", e);
        }
        if let Ok(v) = serde_json::from_str::<serde_json::Value>(RESP) {
            if v.get("data").is_some() {
                assert!(res.unwrap().data.is_some());
            }
        }
    }
}
