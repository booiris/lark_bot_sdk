//! doc url: <https://open.larkoffice.com/document/ukTMukTMukTM/uYDNxYjL2QTM24iN0EjN/event-v1/outbound_ip/list>
// Code generated by gen_api. DO NOT EDIT.

use serde::{Deserialize, Serialize};

use crate::api::BaseResp;
use crate::api::HasBaseResp;
use crate::core::model::{ApiRequest, CommonResponse, MethodOption, ReqParam, StreamReqParam};
use crate::{
    core::{http_client::HttpClient, store::Store},
    error::Error,
};

use crate::api::gen::event::EventService;

impl<'c, IStore: Store, IClient: HttpClient> EventService<'c, IStore, IClient> {
    /// **api 版本: 2023-11-09T08:03:21+00:00**
    ///
    /// ## 获取事件出口 IP
    ///
    /// 飞书开放平台向应用配置的回调地址推送事件时，是通过特定的 IP 发送出去的，应用可以通过本接口获取所有相关的 IP 地址。
    ///
    /// IP 地址有变更可能，建议应用每隔 6 小时定时拉取最新的 IP 地址，以免由于企业防火墙设置，导致应用无法及时接收到飞书开放平台推送的事件。
    ///
    /// doc: <https://open.larkoffice.com/document/ukTMukTMukTM/uYDNxYjL2QTM24iN0EjN/event-v1/outbound_ip/list>
    ///
    /// new doc: <https://open.larkoffice.com/document/server-docs/event-subscription-guide/list>
    ///
    /// api url: <https://open.feishu.cn/document_portal/v1/document/get_detail?fullPath=%2Fserver-docs%2Fevent-subscription-guide%2Flist>
    pub async fn get_event_outbound_ip_list(
        &self,
        req: GetEventOutboundIpListReq,
    ) -> Result<(GetEventOutboundIpListResp, CommonResponse), Error> {
        self.get_event_outbound_ip_list_with_opt(req, Default::default())
            .await
    }

    /// 参见 [get_event_outbound_ip_list](#method.get_event_outbound_ip_list) 函数
    pub async fn get_event_outbound_ip_list_with_opt(
        &self,
        req: GetEventOutboundIpListReq,
        method_option: MethodOption,
    ) -> Result<(GetEventOutboundIpListResp, CommonResponse), Error> {
        #[cfg(feature = "test-util")]
        {
            if let Some(f) = self.mock().get_mock_get_event_outbound_ip_list(&req) {
                tracing::info!("[lark] Event#GetEventOutboundIpList **mocking** api");
                return f(req);
            }
        }

        tracing::info!("[lark] Event#GetEventOutboundIpList call api");

        let req = ApiRequest {
            scope: "Event",
            api: "GetEventOutboundIpList",
            method: http::Method::GET,
            url: String::new()
                + self.cli.open_base_url.as_ref()
                + "/open-apis/event/v1/outbound_ip",
            param_data: req.gen_param(),
            method_option,
            need_tenant_access_token: true,
            ..Default::default()
        };

        let (resp, common_resp): (GetEventOutboundIpListRespInner, _) =
            self.cli.do_req(req).await?;
        let data = match resp.data {
            Some(data) => data,
            None => {
                return Err(Error::ErrResponse(
                    anyhow::anyhow!("missing response data"),
                    common_resp,
                ));
            }
        };
        Ok((data, common_resp))
    }
}

#[derive(Debug, Serialize, Deserialize, Clone, Default, lark_bot_sdk_macros::ApiReqParams)]
pub struct GetEventOutboundIpListReq {
    /// 分页大小，默认10，取值范围 10-50
    ///
    /// **示例值**: "10"
    #[api(kind = "query", name = "page_size", v_type = "var", option = "false")]
    pub page_size: i64,
    /// 分页标记，第一次请求不填，表示从头开始遍历；分页查询结果还有更多项时会同时返回新的 page_token，下次遍历可采用该 page_token 获取查询结果
    ///
    /// **示例值**: "xxx"
    #[api(kind = "query", name = "page_token", v_type = "var", option = "false")]
    pub page_token: String,
}

#[derive(Debug, Serialize, Deserialize, Clone, lark_bot_sdk_macros::ApiBaseResp)]
struct GetEventOutboundIpListRespInner {
    #[serde(flatten)]
    data: Option<GetEventOutboundIpListResp>,
    #[serde(flatten)]
    base: BaseResp,
}

#[derive(Debug, Serialize, Deserialize, Clone, Default)]
pub struct GetEventOutboundIpListResp {
    #[serde(
        rename = "data",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub data: DataSubResp,
}

#[derive(Debug, Serialize, Deserialize, Clone, Default)]
#[serde(default)]
pub struct DataSubResp {
    /// outbound ip
    ///
    /// **示例值**: "1.1.1.1"
    #[serde(
        rename = "ip_list",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub ip_list: Vec<String>,
    /// 分页下次调用的page_token值
    ///
    /// **示例值**: "xxx"
    #[serde(
        rename = "page_token",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub page_token: String,
    /// 是否还有分页数据
    ///
    /// **示例值**: "false"
    #[serde(
        rename = "has_more",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub has_more: bool,
}

#[cfg(feature = "test-util")]
mod test_utils {
    use std::sync::Arc;

    use self::gen::event::EventServiceMocker;
    use crate::core::mocker::*;

    use super::*;
    use crate::api::*;
    use crate::core::model::*;

    pub struct Mocker;

    pub trait MockFunc:
        Fn(GetEventOutboundIpListReq) -> Result<(GetEventOutboundIpListResp, CommonResponse), Error>
        + Send
        + Sync
        + 'static
    {
    }
    impl<
            T: Fn(
                    GetEventOutboundIpListReq,
                ) -> Result<(GetEventOutboundIpListResp, CommonResponse), Error>
                + Send
                + Sync
                + 'static,
        > MockFunc for T
    {
    }

    impl<'c, IStore: Store, IClient: HttpClient> EventServiceMocker<'c, IStore, IClient> {
        pub fn mock_get_event_outbound_ip_list<F: MockFunc>(
            &self,
            f: F,
        ) -> MockerBuilder<
            Mocker,
            GetEventOutboundIpListReq,
            GetEventOutboundIpListResp,
            Arc<dyn MockFunc>,
        > {
            MockerBuilder::new(self.cli.instance_id, Arc::new(f))
        }

        pub(super) fn get_mock_get_event_outbound_ip_list(
            &self,
            req: &GetEventOutboundIpListReq,
        ) -> Option<Arc<dyn MockFunc>> {
            do_mock::<
                Mocker,
                GetEventOutboundIpListReq,
                GetEventOutboundIpListResp,
                Arc<dyn MockFunc>,
            >(self.cli.instance_id, req)
        }
    }
}

#[cfg(feature = "test-util")]
#[cfg(test)]
mod test {
    use crate::{
        api::gen::event::get_event_outbound_ip_list::{
            GetEventOutboundIpListReq, GetEventOutboundIpListResp, GetEventOutboundIpListRespInner,
        },
        core::{model::CommonResponse, Lark},
    };

    #[tokio::test]
    async fn test_mock() {
        let lark = Lark::new("".to_owned(), "".into());
        let mocker = lark
            .event()
            .mock()
            .mock_get_event_outbound_ip_list(|_| {
                Ok((
                    GetEventOutboundIpListResp::default(),
                    CommonResponse::default(),
                ))
            })
            .build();
        let res = lark
            .event()
            .get_event_outbound_ip_list(GetEventOutboundIpListReq::default())
            .await;
        assert!(res.is_ok());
        mocker.clear();
        let res = lark
            .event()
            .get_event_outbound_ip_list(GetEventOutboundIpListReq::default())
            .await;
        assert!(res.is_err());
    }

    const REQ: &str = "{}";

    #[test]
    fn test_req() {
        if REQ == "{}" {
            return;
        }
        if let Err(e) = serde_json::from_str::<()>(REQ) {
            panic!("{}", e);
        }
    }

    const RESP: &str = "{}";
    #[test]
    fn test_resp() {
        let res = serde_json::from_str::<GetEventOutboundIpListRespInner>(RESP);
        if let Err(e) = res {
            panic!("{}", e);
        }
        if let Ok(v) = serde_json::from_str::<serde_json::Value>(RESP) {
            if v.get("data").is_some() {
                assert!(res.unwrap().data.is_some());
            }
        }
    }
}
