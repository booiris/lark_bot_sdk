//! doc url: <https://open.larkoffice.com/document/uAjLw4CM/ukTMukTMukTM/lingo-v1/entity/match>
// Code generated by gen_api. DO NOT EDIT.

use serde::{Deserialize, Serialize};

use crate::api::BaseResp;
use crate::api::HasBaseResp;
use crate::core::model::{ApiRequest, CommonResponse, MethodOption, ReqParam, StreamReqParam};
use crate::{
    core::{http_client::HttpClient, store::Store},
    error::Error,
};

use crate::api::gen::lingo::LingoService;

impl<'c, IStore: Store, IClient: HttpClient> LingoService<'c, IStore, IClient> {
    /// **api 版本: 2023-10-25T08:26:17+00:00**
    ///
    /// ## 精准搜索词条
    ///
    /// 将关键词与词条名、别名精准匹配，并返回对应的 词条 ID。
    ///

    ///
    /// doc: <https://open.larkoffice.com/document/uAjLw4CM/ukTMukTMukTM/lingo-v1/entity/match>
    ///
    /// new doc: <https://open.larkoffice.com/document/uAjLw4CM/ukTMukTMukTM/lingo-v1/entity/match>
    ///
    /// api url: <https://open.feishu.cn/document_portal/v1/document/get_detail?fullPath=%2FuAjLw4CM%2FukTMukTMukTM%2Flingo-v1%2Fentity%2Fmatch>
    pub async fn match_lingo_entity(
        &self,
        req: MatchLingoEntityReq,
    ) -> Result<(MatchLingoEntityResp, CommonResponse), Error> {
        self.match_lingo_entity_with_opt(req, Default::default())
            .await
    }

    /// 参见 [match_lingo_entity](#method.match_lingo_entity) 函数
    pub async fn match_lingo_entity_with_opt(
        &self,
        req: MatchLingoEntityReq,
        method_option: MethodOption,
    ) -> Result<(MatchLingoEntityResp, CommonResponse), Error> {
        #[cfg(feature = "test-util")]
        {
            if let Some(f) = self.mock().get_mock_match_lingo_entity(&req) {
                tracing::info!("[lark] Lingo#MatchLingoEntity **mocking** api");
                return f(req);
            }
        }

        tracing::info!("[lark] Lingo#MatchLingoEntity call api");

        let req = ApiRequest {
            scope: "Lingo",
            api: "MatchLingoEntity",
            method: http::Method::POST,
            url: String::new()
                + self.cli.open_base_url.as_ref()
                + "/open-apis/lingo/v1/entities/match",
            param_data: req.gen_param(),
            method_option,
            need_tenant_access_token: true,
            need_user_access_token: true,
            ..Default::default()
        };

        let (resp, common_resp): (MatchLingoEntityRespInner, _) = self.cli.do_req(req).await?;
        let data = match resp.data {
            Some(data) => data,
            None => {
                return Err(Error::ErrResponse(
                    anyhow::anyhow!("missing response data"),
                    common_resp,
                ));
            }
        };
        Ok((data, common_resp))
    }
}

#[derive(Debug, Serialize, Deserialize, Clone, Default, lark_bot_sdk_macros::ApiReqParams)]
pub struct MatchLingoEntityReq {
    /// 词库ID(不传时默认在全员词库内搜索)
    ///
    /// 如以应用身份搜索非全员词库中的词条，需要在“词库设置”页面添加应用；若以用户身份搜索非全员词库中的词条，该用户需要拥有对应词库的可见权限。
    ///
    /// **示例值**: "7202510112396640276"
    #[api(kind = "query", name = "repo_id", v_type = "var", option = "false")]
    pub repo_id: String,
    /// 搜索关键词，将与词条名、别名进行精准匹配
    ///
    /// **是否必填**: 是
    ///
    /// **示例值**: "飞书词典"
    ///
    /// **数据校验规则**：
    ///
    /// - **长度范围**: `1` 字符- `100` 字符
    #[api(kind = "body", name = "word")]
    pub word: String,
}

#[derive(Debug, Serialize, Deserialize, Clone, lark_bot_sdk_macros::ApiBaseResp)]
struct MatchLingoEntityRespInner {
    #[serde(flatten)]
    data: Option<MatchLingoEntityResp>,
    #[serde(flatten)]
    base: BaseResp,
}

#[derive(Debug, Serialize, Deserialize, Clone, Default)]
pub struct MatchLingoEntityResp {
    /// \-
    #[serde(
        rename = "data",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub data: DataSubResp,
}

#[derive(Debug, Serialize, Deserialize, Clone, Default)]
#[serde(default)]
pub struct DataSubResp {
    /// 搜索结果
    #[serde(
        rename = "results",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub results: Vec<MatchInfoSubResp>,
}

#[derive(Debug, Serialize, Deserialize, Clone, Default)]
#[serde(default)]
pub struct MatchInfoSubResp {
    /// 词条 ID
    ///
    /// **示例值**: "enterprise_34***584"
    #[serde(
        rename = "entity_id",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub entity_id: String,
    /// 匹配中的字段
    ///
    /// **示例值**: "0"
    ///
    /// **可选值**:
    ///
    /// `MainKey`: 词条名
    ///
    /// `FullName`: 全称
    ///
    /// `Alias`: 别名
    #[serde(
        rename = "type",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub body_type: i64,
}

#[cfg(feature = "test-util")]
mod test_utils {
    use std::sync::Arc;

    use self::gen::lingo::LingoServiceMocker;
    use crate::core::mocker::*;

    use super::*;
    use crate::api::*;
    use crate::core::model::*;

    pub struct Mocker;

    pub trait MockFunc:
        Fn(MatchLingoEntityReq) -> Result<(MatchLingoEntityResp, CommonResponse), Error>
        + Send
        + Sync
        + 'static
    {
    }
    impl<
            T: Fn(MatchLingoEntityReq) -> Result<(MatchLingoEntityResp, CommonResponse), Error>
                + Send
                + Sync
                + 'static,
        > MockFunc for T
    {
    }

    impl<'c, IStore: Store, IClient: HttpClient> LingoServiceMocker<'c, IStore, IClient> {
        pub fn mock_match_lingo_entity<F: MockFunc>(
            &self,
            f: F,
        ) -> MockerBuilder<Mocker, MatchLingoEntityReq, MatchLingoEntityResp, Arc<dyn MockFunc>>
        {
            MockerBuilder::new(self.cli.instance_id, Arc::new(f))
        }

        pub(super) fn get_mock_match_lingo_entity(
            &self,
            req: &MatchLingoEntityReq,
        ) -> Option<Arc<dyn MockFunc>> {
            do_mock::<Mocker, MatchLingoEntityReq, MatchLingoEntityResp, Arc<dyn MockFunc>>(
                self.cli.instance_id,
                req,
            )
        }
    }
}

#[cfg(feature = "test-util")]
#[cfg(test)]
mod test {
    use crate::{
        api::gen::lingo::match_lingo_entity::{
            MatchLingoEntityReq, MatchLingoEntityResp, MatchLingoEntityRespInner,
        },
        core::{model::CommonResponse, Lark},
    };

    #[tokio::test]
    async fn test_mock() {
        let lark = Lark::new("".to_owned(), "".into());
        let mocker = lark
            .lingo()
            .mock()
            .mock_match_lingo_entity(|_| {
                Ok((MatchLingoEntityResp::default(), CommonResponse::default()))
            })
            .build();
        let res = lark
            .lingo()
            .match_lingo_entity(MatchLingoEntityReq::default())
            .await;
        assert!(res.is_ok());
        mocker.clear();
        let res = lark
            .lingo()
            .match_lingo_entity(MatchLingoEntityReq::default())
            .await;
        assert!(res.is_err());
    }

    const REQ: &str = r#"{
    "word": "飞书词典"
}"#;

    #[test]
    fn test_req() {
        if REQ == "{}" {
            return;
        }
        if let Err(e) = serde_json::from_str::<super::MatchLingoEntityReqBody>(REQ) {
            panic!("{}", e);
        }
    }

    const RESP: &str = r#"{
    "code": 0,
    "msg": "success",
    "data": {
        "results": [
            {
                "entity_id": "enterprise_34***584",
                "type": 0
            }
        ]
    }
}"#;
    #[test]
    fn test_resp() {
        let res = serde_json::from_str::<MatchLingoEntityRespInner>(RESP);
        if let Err(e) = res {
            panic!("{}", e);
        }
        if let Ok(v) = serde_json::from_str::<serde_json::Value>(RESP) {
            if v.get("data").is_some() {
                assert!(res.unwrap().data.is_some());
            }
        }
    }
}
