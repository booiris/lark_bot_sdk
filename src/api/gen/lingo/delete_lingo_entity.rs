//! doc url: <https://open.larkoffice.com/document/uAjLw4CM/ukTMukTMukTM/lingo-v1/entity/delete>
// Code generated by gen_api. DO NOT EDIT.

use serde::{Deserialize, Serialize};

use crate::api::BaseResp;
use crate::api::HasBaseResp;
use crate::core::model::{ApiRequest, CommonResponse, MethodOption, ReqParam, StreamReqParam};
use crate::{
    core::{http_client::HttpClient, store::Store},
    error::Error,
};

use crate::api::gen::lingo::LingoService;

impl<'c, IStore: Store, IClient: HttpClient> LingoService<'c, IStore, IClient> {
    /// **api 版本: 2024-03-08T03:32:51+00:00**
    ///
    /// ## 删除免审词条
    ///
    /// 通过 entity_id 删除已有的词条，无需经过词典管理员审核。因此，调用该接口时应当慎重操作。
    ///
    /// 也支持通过 provider 和 outer_id 删除对应的词条。此时路径中的 entity_id 为固定的 enterprise_0
    ///
    /// doc: <https://open.larkoffice.com/document/uAjLw4CM/ukTMukTMukTM/lingo-v1/entity/delete>
    ///
    /// new doc: <https://open.larkoffice.com/document/uAjLw4CM/ukTMukTMukTM/lingo-v1/entity/delete>
    ///
    /// api url: <https://open.feishu.cn/document_portal/v1/document/get_detail?fullPath=%2FuAjLw4CM%2FukTMukTMukTM%2Flingo-v1%2Fentity%2Fdelete>
    pub async fn delete_lingo_entity(
        &self,
        req: DeleteLingoEntityReq,
    ) -> Result<(DeleteLingoEntityResp, CommonResponse), Error> {
        self.delete_lingo_entity_with_opt(req, Default::default())
            .await
    }

    /// 参见 [delete_lingo_entity](#method.delete_lingo_entity) 函数
    pub async fn delete_lingo_entity_with_opt(
        &self,
        req: DeleteLingoEntityReq,
        method_option: MethodOption,
    ) -> Result<(DeleteLingoEntityResp, CommonResponse), Error> {
        #[cfg(feature = "test-util")]
        {
            if let Some(f) = self.mock().get_mock_delete_lingo_entity(&req) {
                tracing::info!("[lark] Lingo#DeleteLingoEntity **mocking** api");
                return f(req);
            }
        }

        tracing::info!("[lark] Lingo#DeleteLingoEntity call api");

        let req = ApiRequest {
            scope: "Lingo",
            api: "DeleteLingoEntity",
            method: http::Method::DELETE,
            url: String::new()
                + self.cli.open_base_url.as_ref()
                + "/open-apis/lingo/v1/entities/:entity_id",
            param_data: req.gen_param(),
            method_option,
            need_tenant_access_token: true,
            ..Default::default()
        };

        let (resp, common_resp): (DeleteLingoEntityRespInner, _) = self.cli.do_req(req).await?;
        let data = match resp.data {
            Some(data) => data,
            None => {
                return Err(Error::ErrResponse(
                    anyhow::anyhow!("missing response data"),
                    common_resp,
                ));
            }
        };
        Ok((data, common_resp))
    }
}

#[derive(Debug, Serialize, Deserialize, Clone, Default, lark_bot_sdk_macros::ApiReqParams)]
pub struct DeleteLingoEntityReq {
    /// 词条 ID
    ///
    /// **是否必填**: 是
    ///
    /// **示例值**: "enterprise_43742132363"
    #[api(kind = "path", name = "entity_id")]
    pub entity_id: String,
    /// 外部系统（使用时需要将路径中的词条 ID 固定为：enterprise_0，且提供 provider 和 outer_id）
    ///
    /// **示例值**: "星云"
    ///
    /// **数据校验规则**：
    ///
    /// - **长度范围**: `2` 字符- `32` 字符
    #[api(kind = "query", name = "provider", v_type = "var", option = "false")]
    pub provider: String,
    /// 词条在外部系统中对应的唯一 ID（使用时需要将路径中的词条 ID 固定为：enterprise_0，且提供 provider 和 outer_id）
    ///
    /// **示例值**: "123aaa"
    ///
    /// **数据校验规则**：
    ///
    /// - **长度范围**: `1` 字符- `64` 字符
    #[api(kind = "query", name = "outer_id", v_type = "var", option = "false")]
    pub outer_id: String,
}

#[derive(Debug, Serialize, Deserialize, Clone, lark_bot_sdk_macros::ApiBaseResp)]
struct DeleteLingoEntityRespInner {
    #[serde(flatten)]
    data: Option<DeleteLingoEntityResp>,
    #[serde(flatten)]
    base: BaseResp,
}

#[derive(Debug, Serialize, Deserialize, Clone, Default)]
pub struct DeleteLingoEntityResp {
    /// \-
    #[serde(
        rename = "data",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub data: (),
}

#[cfg(feature = "test-util")]
mod test_utils {
    use std::sync::Arc;

    use self::gen::lingo::LingoServiceMocker;
    use crate::core::mocker::*;

    use super::*;
    use crate::api::*;
    use crate::core::model::*;

    pub struct Mocker;

    pub trait MockFunc:
        Fn(DeleteLingoEntityReq) -> Result<(DeleteLingoEntityResp, CommonResponse), Error>
        + Send
        + Sync
        + 'static
    {
    }
    impl<
            T: Fn(DeleteLingoEntityReq) -> Result<(DeleteLingoEntityResp, CommonResponse), Error>
                + Send
                + Sync
                + 'static,
        > MockFunc for T
    {
    }

    impl<'c, IStore: Store, IClient: HttpClient> LingoServiceMocker<'c, IStore, IClient> {
        pub fn mock_delete_lingo_entity<F: MockFunc>(
            &self,
            f: F,
        ) -> MockerBuilder<Mocker, DeleteLingoEntityReq, DeleteLingoEntityResp, Arc<dyn MockFunc>>
        {
            MockerBuilder::new(self.cli.instance_id, Arc::new(f))
        }

        pub(super) fn get_mock_delete_lingo_entity(
            &self,
            req: &DeleteLingoEntityReq,
        ) -> Option<Arc<dyn MockFunc>> {
            do_mock::<Mocker, DeleteLingoEntityReq, DeleteLingoEntityResp, Arc<dyn MockFunc>>(
                self.cli.instance_id,
                req,
            )
        }
    }
}

#[cfg(feature = "test-util")]
#[cfg(test)]
mod test {
    use crate::{
        api::gen::lingo::delete_lingo_entity::{
            DeleteLingoEntityReq, DeleteLingoEntityResp, DeleteLingoEntityRespInner,
        },
        core::{model::CommonResponse, Lark},
    };

    #[tokio::test]
    async fn test_mock() {
        let lark = Lark::new("".to_owned(), "".into());
        let mocker = lark
            .lingo()
            .mock()
            .mock_delete_lingo_entity(|_| {
                Ok((DeleteLingoEntityResp::default(), CommonResponse::default()))
            })
            .build();
        let res = lark
            .lingo()
            .delete_lingo_entity(DeleteLingoEntityReq::default())
            .await;
        assert!(res.is_ok());
        mocker.clear();
        let res = lark
            .lingo()
            .delete_lingo_entity(DeleteLingoEntityReq::default())
            .await;
        assert!(res.is_err());
    }

    const REQ: &str = "{}";

    #[test]
    fn test_req() {
        if REQ == "{}" {
            return;
        }
        if let Err(e) = serde_json::from_str::<()>(REQ) {
            panic!("{}", e);
        }
    }

    const RESP: &str = r#"{
    "code": 0,
    "msg": "success",
    "data": {}
}"#;
    #[test]
    fn test_resp() {
        let res = serde_json::from_str::<DeleteLingoEntityRespInner>(RESP);
        if let Err(e) = res {
            panic!("{}", e);
        }
        if let Ok(v) = serde_json::from_str::<serde_json::Value>(RESP) {
            if v.get("data").is_some() {
                assert!(res.unwrap().data.is_some());
            }
        }
    }
}
