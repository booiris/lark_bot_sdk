//! doc url: <https://open.larkoffice.com/document/uAjLw4CM/ukTMukTMukTM/helpdesk-v1/agent_skill/delete>
// Code generated by gen_api. DO NOT EDIT.

use serde::{Deserialize, Serialize};

use crate::api::BaseResp;
use crate::api::HasBaseResp;
use crate::core::model::{ApiRequest, CommonResponse, MethodOption, ReqParam, StreamReqParam};
use crate::{
    core::{http_client::HttpClient, store::Store},
    error::Error,
};

use crate::api::gen::helpdesk::HelpdeskService;

impl<'c, IStore: Store, IClient: HttpClient> HelpdeskService<'c, IStore, IClient> {
    /// **api 版本: 2024-03-06T11:33:58+00:00**
    ///
    /// ## 删除客服技能
    ///
    /// 该接口用于删除客服技能。
    ///

    ///
    /// doc: <https://open.larkoffice.com/document/uAjLw4CM/ukTMukTMukTM/helpdesk-v1/agent_skill/delete>
    ///
    /// new doc: <https://open.larkoffice.com/document/server-docs/helpdesk-v1/agent-function/agent_skill/delete>
    ///
    /// api url: <https://open.feishu.cn/document_portal/v1/document/get_detail?fullPath=%2Fserver-docs%2Fhelpdesk-v1%2Fagent-function%2Fagent_skill%2Fdelete>
    pub async fn delete_helpdesk_agent_skill(
        &self,
        req: DeleteHelpdeskAgentSkillReq,
    ) -> Result<(DeleteHelpdeskAgentSkillResp, CommonResponse), Error> {
        self.delete_helpdesk_agent_skill_with_opt(req, Default::default())
            .await
    }

    /// 参见 [delete_helpdesk_agent_skill](#method.delete_helpdesk_agent_skill) 函数
    pub async fn delete_helpdesk_agent_skill_with_opt(
        &self,
        req: DeleteHelpdeskAgentSkillReq,
        method_option: MethodOption,
    ) -> Result<(DeleteHelpdeskAgentSkillResp, CommonResponse), Error> {
        #[cfg(feature = "test-util")]
        {
            if let Some(f) = self.mock().get_mock_delete_helpdesk_agent_skill(&req) {
                tracing::info!("[lark] Helpdesk#DeleteHelpdeskAgentSkill **mocking** api");
                return f(req);
            }
        }

        tracing::info!("[lark] Helpdesk#DeleteHelpdeskAgentSkill call api");

        let req = ApiRequest {
            scope: "Helpdesk",
            api: "DeleteHelpdeskAgentSkill",
            method: http::Method::DELETE,
            url: String::new()
                + self.cli.open_base_url.as_ref()
                + "/open-apis/helpdesk/v1/agent_skills/:agent_skill_id",
            param_data: req.gen_param(),
            method_option,
            need_user_access_token: true,
            ..Default::default()
        };

        let (resp, common_resp): (DeleteHelpdeskAgentSkillRespInner, _) =
            self.cli.do_req(req).await?;
        let data = match resp.data {
            Some(data) => data,
            None => {
                return Err(Error::ErrResponse(
                    anyhow::anyhow!("missing response data"),
                    common_resp,
                ));
            }
        };
        Ok((data, common_resp))
    }
}

#[derive(Debug, Serialize, Deserialize, Clone, Default, lark_bot_sdk_macros::ApiReqParams)]
pub struct DeleteHelpdeskAgentSkillReq {
    /// agent group id
    ///
    /// **示例值**: "test-skill-id"
    #[api(kind = "path", name = "agent_skill_id")]
    pub agent_skill_id: String,
}

#[derive(Debug, Serialize, Deserialize, Clone, lark_bot_sdk_macros::ApiBaseResp)]
struct DeleteHelpdeskAgentSkillRespInner {
    #[serde(flatten)]
    data: Option<DeleteHelpdeskAgentSkillResp>,
    #[serde(flatten)]
    base: BaseResp,
}

#[derive(Debug, Serialize, Deserialize, Clone, Default)]
pub struct DeleteHelpdeskAgentSkillResp {
    /// \-
    #[serde(
        rename = "data",
        deserialize_with = "crate::utils::serde_helper::null_to_default"
    )]
    pub data: (),
}

#[cfg(feature = "test-util")]
mod test_utils {
    use std::sync::Arc;

    use self::gen::helpdesk::HelpdeskServiceMocker;
    use crate::core::mocker::*;

    use super::*;
    use crate::api::*;
    use crate::core::model::*;

    pub struct Mocker;

    pub trait MockFunc:
        Fn(
            DeleteHelpdeskAgentSkillReq,
        ) -> Result<(DeleteHelpdeskAgentSkillResp, CommonResponse), Error>
        + Send
        + Sync
        + 'static
    {
    }
    impl<
            T: Fn(
                    DeleteHelpdeskAgentSkillReq,
                ) -> Result<(DeleteHelpdeskAgentSkillResp, CommonResponse), Error>
                + Send
                + Sync
                + 'static,
        > MockFunc for T
    {
    }

    impl<'c, IStore: Store, IClient: HttpClient> HelpdeskServiceMocker<'c, IStore, IClient> {
        pub fn mock_delete_helpdesk_agent_skill<F: MockFunc>(
            &self,
            f: F,
        ) -> MockerBuilder<
            Mocker,
            DeleteHelpdeskAgentSkillReq,
            DeleteHelpdeskAgentSkillResp,
            Arc<dyn MockFunc>,
        > {
            MockerBuilder::new(self.cli.instance_id, Arc::new(f))
        }

        pub(super) fn get_mock_delete_helpdesk_agent_skill(
            &self,
            req: &DeleteHelpdeskAgentSkillReq,
        ) -> Option<Arc<dyn MockFunc>> {
            do_mock::<
                Mocker,
                DeleteHelpdeskAgentSkillReq,
                DeleteHelpdeskAgentSkillResp,
                Arc<dyn MockFunc>,
            >(self.cli.instance_id, req)
        }
    }
}

#[cfg(feature = "test-util")]
#[cfg(test)]
mod test {
    use crate::{
        api::gen::helpdesk::delete_helpdesk_agent_skill::{
            DeleteHelpdeskAgentSkillReq, DeleteHelpdeskAgentSkillResp,
            DeleteHelpdeskAgentSkillRespInner,
        },
        core::{model::CommonResponse, Lark},
    };

    #[tokio::test]
    async fn test_mock() {
        let lark = Lark::new("".to_owned(), "".into());
        let mocker = lark
            .helpdesk()
            .mock()
            .mock_delete_helpdesk_agent_skill(|_| {
                Ok((
                    DeleteHelpdeskAgentSkillResp::default(),
                    CommonResponse::default(),
                ))
            })
            .build();
        let res = lark
            .helpdesk()
            .delete_helpdesk_agent_skill(DeleteHelpdeskAgentSkillReq::default())
            .await;
        assert!(res.is_ok());
        mocker.clear();
        let res = lark
            .helpdesk()
            .delete_helpdesk_agent_skill(DeleteHelpdeskAgentSkillReq::default())
            .await;
        assert!(res.is_err());
    }

    const REQ: &str = "{}";

    #[test]
    fn test_req() {
        if REQ == "{}" {
            return;
        }
        if let Err(e) = serde_json::from_str::<()>(REQ) {
            panic!("{}", e);
        }
    }

    const RESP: &str = r#"{
    "code": 0,
    "msg": "success",
    "data": {}
}"#;
    #[test]
    fn test_resp() {
        let res = serde_json::from_str::<DeleteHelpdeskAgentSkillRespInner>(RESP);
        if let Err(e) = res {
            panic!("{}", e);
        }
        if let Ok(v) = serde_json::from_str::<serde_json::Value>(RESP) {
            if v.get("data").is_some() {
                assert!(res.unwrap().data.is_some());
            }
        }
    }
}
